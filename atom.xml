<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>孙同生的博客</title>
  
  <subtitle>为美好的世界献上祝福</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://developerdoc.com/"/>
  <updated>2019-05-08T02:44:11.482Z</updated>
  <id>https://developerdoc.com/</id>
  
  <author>
    <name>SunTongSheng</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>fishhook源码学习</title>
    <link href="https://developerdoc.com/essay/LLDB/fishhook_code_analyze/"/>
    <id>https://developerdoc.com/essay/LLDB/fishhook_code_analyze/</id>
    <published>2019-05-08T02:29:22.000Z</published>
    <updated>2019-05-08T02:44:11.482Z</updated>
    
    <content type="html"><![CDATA[<p>上一篇 <a href="/essay/LLDB/Mach-O_fishhook/">Mach-O应用 fishhook动态修改C函数</a> 了解了fishhook的原理，现在来看一下它的代码，看它是如何一步一步替换原有函数实现的。</p><a id="more"></a><p>我们再来看看rebind_symbols这个对外的接口，其中应用到的C函数作用如下：</p><ul><li><code>_dyld_image_count(void)</code> 当前dyld装载的image数量</li><li><code>_dyld_get_image_header(unit32_t image_index)</code> 返回image对应的Mach Header地址</li><li><code>_dyld_get_image_vmaddr_slide(unit32_t image_index)</code> 虚拟内存中的地址偏移量</li></ul><p>对实现的分析会 rebind_symbols 函数为入口，首先看一下函数的调用栈：</p><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">rebind_symbols</span><span class="params">(struct rebinding rebindings[], <span class="keyword">size_t</span> rebindings_nel)</span></span>;</span><br><span class="line">└── <span class="keyword">extern</span> <span class="keyword">void</span> _dyld_register_func_for_add_image(<span class="keyword">void</span> (*func)(<span class="keyword">const</span> struct mach_header* mh, <span class="keyword">intptr_t</span> vmaddr_slide));</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> _rebind_symbols_for_image(<span class="keyword">const</span> struct mach_header *header, <span class="keyword">intptr_t</span> slide)</span><br><span class="line">└── <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">rebind_symbols_for_image</span><span class="params">(struct rebindings_entry *rebindings, <span class="keyword">const</span> struct mach_header *header, <span class="keyword">intptr_t</span> slide)</span></span></span><br><span class="line">└── static void perform_rebinding_with_section(struct rebindings_entry *rebindings, section_t *section, intptr_t slide, nlist_t *symtab, char *strtab, uint32_t *indirect_symtab)</span><br></pre></td></tr></table></figure><p>其实函数调用栈非常简单，因为整个库中也没有几个函数，rebind_symbols 作为接口，其主要作用就是注册一个函数并在镜像加载时回调：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">rebind_symbols</span><span class="params">(struct rebinding rebindings[], <span class="keyword">size_t</span> rebindings_nel)</span> </span>&#123;</span><br><span class="line"><span class="keyword">int</span> retval = prepend_rebindings(&amp;_rebindings_head, rebindings, rebindings_nel);</span><br><span class="line"><span class="keyword">if</span> (retval &lt; <span class="number">0</span>) <span class="keyword">return</span> retval;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!_rebindings_head-&gt;next) &#123;</span><br><span class="line">_dyld_register_func_for_add_image(_rebind_symbols_for_image);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="keyword">uint32_t</span> c = _dyld_image_count();</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">uint32_t</span> i = <span class="number">0</span>; i &lt; c; i++) &#123;</span><br><span class="line">_rebind_symbols_for_image(_dyld_get_image_header(i), _dyld_get_image_vmaddr_slide(i));</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> retval;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 rebind_symbols 最开始执行时，会先调用一个 prepend_rebindings 的函数，将整个 rebindings 数组添加到 _rebindings_head 这个私有数据结构的头部：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">prepend_rebindings</span><span class="params">(struct rebindings_entry **rebindings_head,</span></span></span><br><span class="line"><span class="function"><span class="params">  struct rebinding rebindings[],</span></span></span><br><span class="line"><span class="function"><span class="params">  <span class="keyword">size_t</span> nel)</span> </span>&#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rebindings_entry</span> *<span class="title">new_entry</span> = <span class="title">malloc</span>(<span class="title">sizeof</span>(<span class="title">struct</span> <span class="title">rebindings_entry</span>));</span></span><br><span class="line"><span class="keyword">if</span> (!new_entry) &#123;</span><br><span class="line"><span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br><span class="line">new_entry-&gt;rebindings = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(struct rebinding) * nel);</span><br><span class="line"><span class="keyword">if</span> (!new_entry-&gt;rebindings) &#123;</span><br><span class="line"><span class="built_in">free</span>(new_entry);</span><br><span class="line"><span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">memcpy</span>(new_entry-&gt;rebindings, rebindings, <span class="keyword">sizeof</span>(struct rebinding) * nel);</span><br><span class="line">new_entry-&gt;rebindings_nel = nel;</span><br><span class="line">new_entry-&gt;next = *rebindings_head;</span><br><span class="line">*rebindings_head = new_entry;</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>也就是说每次调用的 rebind_symbols 方法传入的 rebindings 数组以及数组的长度都会以 rebindings_entry 的形式添加到 _rebindings_head 这个私有链表的首部：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rebindings_entry</span> &#123;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rebinding</span> *<span class="title">rebindings</span>;</span></span><br><span class="line"><span class="keyword">size_t</span> rebindings_nel;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rebindings_entry</span> *<span class="title">next</span>;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">struct</span> <span class="title">rebindings_entry</span> *_<span class="title">rebindings_head</span>;</span></span><br></pre></td></tr></table></figure><p>这样可以通过判断 _rebindings_head-&gt;next 的值来判断是否为第一次调用，然后使用 _dyld_register_func_for_add_image 将 _rebind_symbols_for_image 注册为回调或者为所有存在的镜像单独调用 _rebind_symbols_for_image：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> _rebind_symbols_for_image(<span class="keyword">const</span> struct mach_header *header, <span class="keyword">intptr_t</span> slide) &#123;</span><br><span class="line">rebind_symbols_for_image(_rebindings_head, header, slide);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>_rebind_symbols_for_image 只是对另一个名字非常相似的函数 rebind_symbols_for_image 的封装，从这个函数开始，就到了重绑定符号的过程；不过由于这个方法的实现比较长，具体分析会分成三个部分并省略一些不影响理解的代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">rebind_symbols_for_image</span><span class="params">(struct rebindings_entry *rebindings,</span></span></span><br><span class="line"><span class="function"><span class="params"> <span class="keyword">const</span> struct mach_header *header,</span></span></span><br><span class="line"><span class="function"><span class="params"> <span class="keyword">intptr_t</span> slide)</span> </span>&#123;</span><br><span class="line"><span class="keyword">segment_command_t</span> *cur_seg_cmd;</span><br><span class="line"><span class="keyword">segment_command_t</span> *linkedit_segment = <span class="literal">NULL</span>;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">symtab_command</span>* <span class="title">symtab_cmd</span> = <span class="title">NULL</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">dysymtab_command</span>* <span class="title">dysymtab_cmd</span> = <span class="title">NULL</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">uintptr_t</span> cur = (<span class="keyword">uintptr_t</span>)header + <span class="keyword">sizeof</span>(<span class="keyword">mach_header_t</span>);</span><br><span class="line"><span class="keyword">for</span> (uint i = <span class="number">0</span>; i &lt; header-&gt;ncmds; i++, cur += cur_seg_cmd-&gt;cmdsize) &#123;</span><br><span class="line">cur_seg_cmd = (<span class="keyword">segment_command_t</span> *)cur;</span><br><span class="line"><span class="keyword">if</span> (cur_seg_cmd-&gt;cmd == LC_SEGMENT_ARCH_DEPENDENT) &#123;</span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">strcmp</span>(cur_seg_cmd-&gt;segname, SEG_LINKEDIT) == <span class="number">0</span>) &#123;</span><br><span class="line">linkedit_segment = cur_seg_cmd;</span><br><span class="line">&#125;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (cur_seg_cmd-&gt;cmd == LC_SYMTAB) &#123;</span><br><span class="line">symtab_cmd = (struct symtab_command*)cur_seg_cmd;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (cur_seg_cmd-&gt;cmd == LC_DYSYMTAB) &#123;</span><br><span class="line">dysymtab_cmd = (struct dysymtab_command*)cur_seg_cmd;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这部分的代码主要功能是从镜像中查找 linkedit_segment symtab_command 和 dysymtab_command；在开始查找之前，要先跳过 mach_header_t 长度的位置，然后将当前指针强转成 segment_command_t，通过对比 cmd 的值，来找到需要的 segment_command_t。</p><p>在查找了几个关键的 segment 之后，我们可以根据几个 segment 获取对应表的内存地址：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">rebind_symbols_for_image</span><span class="params">(struct rebindings_entry *rebindings, <span class="keyword">const</span> struct mach_header *header, <span class="keyword">intptr_t</span> slide)</span> </span>&#123;</span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">uintptr_t</span> linkedit_base = (<span class="keyword">uintptr_t</span>)slide + linkedit_segment-&gt;vmaddr - linkedit_segment-&gt;fileoff;</span><br><span class="line"><span class="keyword">nlist_t</span> *symtab = (<span class="keyword">nlist_t</span> *)(linkedit_base + symtab_cmd-&gt;symoff);</span><br><span class="line"><span class="keyword">char</span> *strtab = (<span class="keyword">char</span> *)(linkedit_base + symtab_cmd-&gt;stroff);</span><br><span class="line"></span><br><span class="line"><span class="keyword">uint32_t</span> *indirect_symtab = (<span class="keyword">uint32_t</span> *)(linkedit_base + dysymtab_cmd-&gt;indirectsymoff);</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 linkedit_segment 结构体中获得其虚拟地址以及文件偏移量，然后通过一下公式来计算当前 __LINKEDIT 段的位置：</p><p><code>slide + vmaffr - fileoff</code></p><p>类似地，在 symtab_command 中获取符号表偏移量和字符串表偏移量，从 dysymtab_command 中获取间接符号表（indirect symbol table）偏移量，就能够获得<em>符号表</em>、<em>字符串表</em>以及<em>间接符号表</em>的引用了。</p><ul><li>间接符号表中的元素都是 uint32_t *，指针的值是对应条目 n_list 在符号表中的位置</li><li><p>符号表中的元素都是 nlist_t 结构体，其中包含了当前符号在字符串表中的下标</p>  <figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">nlist_64</span> &#123;</span></span><br><span class="line">    <span class="keyword">union</span> &#123;</span><br><span class="line">    <span class="keyword">uint32_t</span>  n_strx; <span class="comment">/* index into the string table */</span></span><br><span class="line">&#125; n_un;</span><br><span class="line"><span class="keyword">uint8_t</span> n_type;        <span class="comment">/* type flag, see below */</span></span><br><span class="line"><span class="keyword">uint8_t</span> n_sect;        <span class="comment">/* section number or NO_SECT */</span></span><br><span class="line"><span class="keyword">uint16_t</span> n_desc;       <span class="comment">/* see &lt;mach-o/stab.h&gt; */</span></span><br><span class="line"><span class="keyword">uint64_t</span> n_value;      <span class="comment">/* value of this symbol (or stab offset) */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li><li><p>字符串表中的元素是 char 字符</p></li></ul><p>该函数的最后一部分就开启了遍历模式，查找整个镜像中的 SECTION_TYPE 为 S_LAZY_SYMBOL_POINTERS 或者 S_NON_LAZY_SYMBOL_POINTERS 的 section，然后调用下一个函数 perform_rebinding_with_section 来对 section 中的符号进行处理：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">perform_rebinding_with_section</span><span class="params">(struct rebindings_entry *rebindings, <span class="keyword">section_t</span> *section, <span class="keyword">intptr_t</span> slide, <span class="keyword">nlist_t</span> *symtab, <span class="keyword">char</span> *strtab, <span class="keyword">uint32_t</span> *indirect_symtab)</span> </span>&#123;</span><br><span class="line"><span class="keyword">uint32_t</span> *indirect_symbol_indices = indirect_symtab + section-&gt;reserved1;</span><br><span class="line"><span class="keyword">void</span> **indirect_symbol_bindings = (<span class="keyword">void</span> **)((<span class="keyword">uintptr_t</span>)slide + section-&gt;addr);</span><br><span class="line"><span class="keyword">for</span> (uint i = <span class="number">0</span>; i &lt; section-&gt;size / <span class="keyword">sizeof</span>(<span class="keyword">void</span> *); i++) &#123;</span><br><span class="line"><span class="keyword">uint32_t</span> symtab_index = indirect_symbol_indices[i];</span><br><span class="line"><span class="keyword">uint32_t</span> strtab_offset = symtab[symtab_index].n_un.n_strx;</span><br><span class="line"><span class="keyword">char</span> *symbol_name = strtab + strtab_offset;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rebindings_entry</span> *<span class="title">cur</span> = <span class="title">rebindings</span>;</span></span><br><span class="line"><span class="keyword">while</span> (cur) &#123;</span><br><span class="line"><span class="keyword">for</span> (uint j = <span class="number">0</span>; j &lt; cur-&gt;rebindings_nel; j++) &#123;</span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">strcmp</span>(&amp;symbol_name[<span class="number">1</span>], cur-&gt;rebindings[j].name) == <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (cur-&gt;rebindings[j].replaced != <span class="literal">NULL</span> &amp;&amp;</span><br><span class="line">indirect_symbol_bindings[i] != cur-&gt;rebindings[j].replacement) &#123;</span><br><span class="line">*(cur-&gt;rebindings[j].replaced) = indirect_symbol_bindings[i];</span><br><span class="line">&#125;</span><br><span class="line">indirect_symbol_bindings[i] = cur-&gt;rebindings[j].replacement;</span><br><span class="line"><span class="keyword">goto</span> symbol_loop;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">cur = cur-&gt;next;</span><br><span class="line">&#125;</span><br><span class="line">symbol_loop:;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>该函数的实现的核心内容就是将符号表中的 symbol_name 与 rebinding 中的名字 name 进行比较，如果出现了匹配，就会将原函数的实现传入 origian_open 函数指针的地址，并使用新的函数实现 new_open 代替原实现：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (cur-&gt;rebindings[j].replaced != <span class="literal">NULL</span> &amp;&amp;</span><br><span class="line">indirect_symbol_bindings[i] != cur-&gt;rebindings[j].replacement) &#123;</span><br><span class="line">*(cur-&gt;rebindings[j].replaced) = indirect_symbol_bindings[i]; <span class="comment">// 将原函数的实现传入 original_open 函数指针的地址</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>indirect_symbol_bindings[i] = cur-&gt;rebindings[j].replacement; // 使用新的函数实现 new_open 替换原实现<br>如果你理解了上面的实现代码，该函数的其它代码就很好理解了：</p><ol><li>通过 indirect_symtab + section-&gt;reserved1 获取 indirect_symbol_indices *，也就是符号表的数组</li><li>通过 (void **)((uintptr_t)slide + section-&gt;addr) 获取函数指针列表 indirect_symbol_bindings</li><li>遍历符号表数组 indirect_symbol_indices * 中的所有符号表中，获取其中的符号表索引 symtab_index</li><li>通过符号表索引 symtab_index 获取符号表中某一个 n_list 结构体，得到字符串表中的索引 symtab[symtab_index].n_un.n_strx</li><li>最后在字符串表中获得符号的名字 char *symbol_name</li></ol><p>到这里比较前的准备工作就完成了，剩下的代码会遍历整个 rebindings_entry 数组，在其中查找匹配的符号，完成函数实现的替换：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (cur) &#123;</span><br><span class="line"><span class="keyword">for</span> (uint j = <span class="number">0</span>; j &lt; cur-&gt;rebindings_nel; j++) &#123;</span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">strcmp</span>(&amp;symbol_name[<span class="number">1</span>], cur-&gt;rebindings[j].name) == <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (cur-&gt;rebindings[j].replaced != <span class="literal">NULL</span> &amp;&amp;</span><br><span class="line">indirect_symbol_bindings[i] != cur-&gt;rebindings[j].replacement) &#123;</span><br><span class="line">*(cur-&gt;rebindings[j].replaced) = indirect_symbol_bindings[i];</span><br><span class="line">&#125;</span><br><span class="line">indirect_symbol_bindings[i] = cur-&gt;rebindings[j].replacement;</span><br><span class="line"><span class="keyword">goto</span> symbol_loop;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">cur = cur-&gt;next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在之后对某一函数的调用（例如 open），当查找其函数实现时，都会查找到 new_open 的函数指针；在 new_open 调用 origianl_open 时，同样也会执行原有的函数实现，因为我们通过 *(cur-&gt;rebindings[j].replaced) = indirect_symbol_bindings[i] 将原函数实现绑定到了新的函数指针上。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;上一篇 &lt;a href=&quot;/essay/LLDB/Mach-O_fishhook/&quot;&gt;Mach-O应用 fishhook动态修改C函数&lt;/a&gt; 了解了fishhook的原理，现在来看一下它的代码，看它是如何一步一步替换原有函数实现的。&lt;/p&gt;
    
    </summary>
    
      <category term="重学iOS" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/"/>
    
    
      <category term="iOS" scheme="https://developerdoc.com/tags/iOS/"/>
    
      <category term="fishhook" scheme="https://developerdoc.com/tags/fishhook/"/>
    
  </entry>
  
  <entry>
    <title>Mach-O应用 fishhook动态修改C函数</title>
    <link href="https://developerdoc.com/essay/LLDB/Mach-O_fishhook/"/>
    <id>https://developerdoc.com/essay/LLDB/Mach-O_fishhook/</id>
    <published>2019-04-29T12:10:20.000Z</published>
    <updated>2019-05-08T02:29:30.143Z</updated>
    
    <content type="html"><![CDATA[<h2 id="fishhook简介"><a href="#fishhook简介" class="headerlink" title="fishhook简介"></a>fishhook简介</h2><p>C 语言往往会给我们留下不可修改的这一印象,而 <a href="https://github.com/facebook/fishhook" target="_blank" rel="noopener">fishhook</a> 是一个由 facebook 开源的第三方框架，其主要作用就是<strong>动态修改 C 语言函数实现</strong>。</p><p>这个框架的代码其实非常的简单，只包含两个文件：fishhook.c 以及 fishhook.h；两个文件所有的代码加起来也不超过 300 行。不过它的实现原理是非常有意思并且精妙的</p><p>fishhook 提供非常简单的两个接口以及一个结构体：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rebinding</span> &#123;</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> *name;</span><br><span class="line"><span class="keyword">void</span> *replacement;</span><br><span class="line"><span class="keyword">void</span> **replaced;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">rebind_symbols</span><span class="params">(struct rebinding rebindings[], <span class="keyword">size_t</span> rebindings_nel)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">rebind_symbols_image</span><span class="params">(<span class="keyword">void</span> *header,<span class="keyword">intptr_t</span> slide,</span></span></span><br><span class="line"><span class="function"><span class="params">struct rebinding rebindings[],</span></span></span><br><span class="line"><span class="function"><span class="params"><span class="keyword">size_t</span> rebindings_nel)</span></span>;</span><br></pre></td></tr></table></figure><p>我们可以从 fishhook 提供的demo中上手实践一下,这里的demo对 <code>close</code> 进行修改:</p><a id="more"></a><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//main.m</span></span><br><span class="line"><span class="meta">#import <span class="meta-string">&lt;UIKit/UIKit.h&gt;</span></span></span><br><span class="line"><span class="meta">#import <span class="meta-string">"AppDelegate.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#import <span class="meta-string">"dlfcn.h"</span></span></span><br><span class="line"><span class="meta">#import <span class="meta-string">"fishhook.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">//声明一个与原函数签名相同的函数指针,用保存原始的函数的地址</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> (*orig_close)(<span class="keyword">int</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">//新的close</span></span><br><span class="line"><span class="keyword">int</span> my_close(<span class="keyword">int</span> fd) &#123;</span><br><span class="line">    printf(<span class="string">"做一些额外操作\n"</span>);</span><br><span class="line">    printf(<span class="string">"调用原 close(%d)\n"</span>, fd);</span><br><span class="line">    <span class="comment">//调用的 orig_close 其实相当于执行原 close</span></span><br><span class="line">    <span class="keyword">return</span> orig_close(fd);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> main(<span class="keyword">int</span> argc, <span class="keyword">char</span> * argv[]) &#123;</span><br><span class="line">    <span class="keyword">@autoreleasepool</span> &#123;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">struct</span> rebinding closeBind;</span><br><span class="line">        <span class="comment">//函数的名称</span></span><br><span class="line">        closeBind.name = <span class="string">"close"</span>;</span><br><span class="line">        <span class="comment">//新的函数地址</span></span><br><span class="line">        closeBind.replacement = my_close;</span><br><span class="line">        <span class="comment">//保存原始函数地址的变量的指针</span></span><br><span class="line">        closeBind.replaced = (<span class="keyword">void</span> *)&amp;orig_close;</span><br><span class="line">        <span class="comment">//定义数组</span></span><br><span class="line">        <span class="keyword">struct</span> rebinding rebs[] = &#123;closeBind&#125;;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         对符号进行重绑定 arg1 : 存放rebinding结构体的数组 arg2 : 数组的长度</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        rebind_symbols(rebs, <span class="number">1</span>);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 开始测试:</span></span><br><span class="line">        <span class="keyword">int</span> fd = open(argv[<span class="number">0</span>], O_RDONLY);</span><br><span class="line">        uint32_t magic_number = <span class="number">0</span>;</span><br><span class="line">        read(fd, &amp;magic_number, <span class="number">4</span>);</span><br><span class="line">        printf(<span class="string">"Mach-O Magic Number: %x \n"</span>, magic_number);</span><br><span class="line">        close(fd);</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">UIApplicationMain</span>(argc, argv, <span class="literal">nil</span>, <span class="built_in">NSStringFromClass</span>([AppDelegate <span class="keyword">class</span>]));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>最后查看输出的信息可以看到 在对符号进行重绑定之后，所有调用 <code>close</code> 函数的地方实际上都会执行 <code>my_close</code> 的实现，也就完成了对 <code>close</code> 的修改。</p><p>那么 fishhook 是如何做到的呢？</p><h2 id="fishhook-原理"><a href="#fishhook-原理" class="headerlink" title="fishhook 原理"></a>fishhook 原理</h2><p>fishhook 是 FaceBook 开源的可以动态修改 MachO 符号表的工具。fishhook 的强大之处在于它可以 HOOK 系统的静态 C 函数。</p><p>大家都知道 OC 的方法之所以可以 HOOK 是因为它的运行时特性，OC 的方法调用在底层都是 msg_send（id,SEL）的形式，这为我们提供了交换方法实现（IMP）的机会，但 C 函数在编译链接时就确定了函数指针的地址偏移量（Offset），这个偏移量在编译好的可执行文件中是固定的。既然 C 函数的指针地址是相对固定且不可修改的，那么 fishhook 又是怎么实现 对 C 函数的 HOOK 呢？<strong>其实内部/自定义的 C 函数 fishhook 也 HOOK 不了，它只能HOOK Mach-O 外部（共享缓存库中）的函数</strong>。</p><p>现在先来看一下以下这几个知识点:</p><h3 id="动态链接"><a href="#动态链接" class="headerlink" title="动态链接"></a>动态链接</h3><p>动态链接就是负责将各种各样程序需要的镜像加载到程序运行的内存空间中，这个过程发生的时间非常早： <em>在 objc 运行时初始化之前</em> 。</p><p>先来看一个简单的demo为例：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// helloFishhook.m</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">hello_world</span><span class="params">()</span> </span>&#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"Hello, World!\n"</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">const</span> <span class="keyword">char</span> * argv[])</span> </span>&#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"Hello, World!\n"</span>);</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如我们在 <a href="/essay/LLDB/iOS_Compiler/">iOS编译过程</a> 中实践的一样 ，先用clang编译一下，再用<code>nm</code>命令查看符号:</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#clang helloFishhook.m</span></span><br><span class="line"><span class="comment">#nm -nm a.out </span></span><br><span class="line">                 (undefined) external _printf (from libSystem)</span><br><span class="line">                 (undefined) external dyld_stub_binder (from libSystem)</span><br><span class="line">0000000100000000 (__TEXT,__text) [referenced dynamically] external __mh_execute_header</span><br><span class="line">0000000100000f30 (__TEXT,__text) external _hello_world</span><br><span class="line">0000000100000f50 (__TEXT,__text) external _main</span><br></pre></td></tr></table></figure><p>可以看到自己写的方法 <code>_hello_world</code> ,它包含一个内存地址以及 __TEXT 段。也就是说手写的一些函数，在编译之后，其地址并不是未定义的</p><p>与之对比的是看到 <code>_printf</code> 这个符号是未定义的(undefined),<code>dyld_stub_binder</code> 会在目标符号（例如 printf）被调用时，将其链接到指定的动态链接库 libSystem，再执行 printf 的实现</p><p>每一个镜像中的 <strong>DATA 端都包含两个与动态链接有关的表，其中一个是 </strong>nl_symbol_ptr，另一个是 <code>__la_symbol_ptr</code>：</p><ul><li><code>__nl_symbol_ptr</code> 中的 non-lazy 符号是在动态链接库绑定的时候进行加载的</li><li><code>__la_symbol_ptr</code> 中的符号会在该符号被第一次调用时，通过 dyld 中的 dyld_stub_binder 过程来进行加载</li></ul><p>了解这两点的区别，你也大概可以猜出 fishhook 为什么能替换原C函数了。</p><h3 id="PIC（Position-independent-code"><a href="#PIC（Position-independent-code" class="headerlink" title="PIC（Position-independent code)"></a>PIC（Position-independent code)</h3><p>为什么 printf 是未定义的？</p><p>ASLR技术：是一种针对缓冲区溢出的安全保护技术，通过对堆、栈、共享库映射等线性区布局的随机化，通过增加攻击者预测目的地址的难度。对于我们APP而言，它保证每次MachO文件加载的时候是随机地址  <strong>这个我们可以通过LLDB指令的image list去查看</strong></p><p>苹果采用了PIC（Position-independent code）技术成功让 C 的底层也能有动态的表现：</p><ul><li>编译时在 Mach-O 文件 _DATA 段的符号表中为每一个被引用的系统 C 函数建立一个指针（8字节的数据，放的全是0），这个指针用于动态绑定时重定位到共享库中的函数实现。</li><li>在运行时当系统 C 函数被第一次调用时会动态绑定一次，然后将 Mach-O 中的 _DATA 段符号表中对应的指针，指向外部函数（其在共享库中的实际内存地址）。</li></ul><p>fishhook 正是利用了 PIC 技术做了这么两个操作：</p><ul><li>将指向系统方法（外部函数）的指针重新进行绑定指向内部函数/自定义 C 函数。</li><li>将内部函数的指针在动态链接时指向系统方法的地址。</li></ul><p>这样就把系统方法与自己定义的方法进行了交换，达到 HOOK 系统 C 函数（共享库中的）的目的。</p><h3 id="dyld-加载回调"><a href="#dyld-加载回调" class="headerlink" title="dyld 加载回调"></a>dyld 加载回调</h3><p>在 dyld 加载镜像时，会执行注册过的回调函数; 对于每一个已经存在的镜像，当它被动态链接时，都会执行回调 </p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> (*func)(<span class="keyword">const</span> struct mach_header* mh, <span class="keyword">intptr_t</span> vmaddr_slide)</span><br></pre></td></tr></table></figure><p>传入文件的 <code>mach_header</code> 以及一个虚拟内存地址 <code>intptr_t</code>。</p><p>dyld 通过更新 Mach-O 二进制文件 __DATA 段中的一些指针来绑定 lazy 和 non-lazy 的符号；</p><p>而 fishhook 先确定某一个符号在 __DATA 段中的位置，<strong>然后保存原符号对应的函数指针，并使用新的函数指针覆盖原有符号的函数指针，实现重绑定。</strong></p><h2 id="fishhook-是如何根据字符串找到对应指针在符号表中的偏移值的"><a href="#fishhook-是如何根据字符串找到对应指针在符号表中的偏移值的" class="headerlink" title="fishhook 是如何根据字符串找到对应指针在符号表中的偏移值的"></a>fishhook 是如何根据字符串找到对应指针在符号表中的偏移值的</h2><p>直接上fishhook 的 README 中的流程图：</p><p><img src="/res/MachO/fishhook1.png" width="60%"></p><p>这张图初看很复杂，不过它演示的是寻找符号的过程，我们根据这张图来分析一下这个过程：</p><ol><li>从 __DATA 段中的 lazy 符号指针表中查找某个符号，获得这个符号的偏移量 1061，然后在每一个 section_64 中查找 reserved1，通过这两个值找到 Indirect Symbol Table 中符号对应的条目</li><li>在 Indirect Symbol Table 找到符号表指针以及对应的索引 16343 之后，就需要访问符号表</li><li>然后通过符号表中的偏移量，获取字符串表中的符号 <code>_close</code></li></ol><p>图示中，1061 是间接符号表的偏移量，*（偏移量+间接符号地址）=16343，即符号表偏移量。符号表中每一个结构都是一个 nlist 结构体，其中包含字符表偏移量。通过字符表偏移量最终确定函数指针。</p><p>fishhook 就是对间接符号表的偏移量动的手脚，提供一个假的 nlist 结构体，从而达到 hook 的目的。</p><p>上面的流程图其实已经显示的很清楚了，这里我们重新来走一遍，一步步找到其在 MachO 文件里对应指针的偏移值，大致步骤如下：</p><p><strong>1.在 String Table 中找到该字符串在 Symbols Table -&gt; Symbols 中的位置：</strong></p><p><img src="/res/MachO/fishhook2.png" alt=""></p><p>用 0x9832 - 0x9780 = 0xB2</p><p><strong>2.在 Symbols Table -&gt; Symbols 中找到Data = 0xB2 的符号，其对应的 offset 值 0x16F 就是该符号在 Dynamic Symbols Table -&gt; Indirect Symbols 表中的 Data 值</strong></p><p><img src="/res/MachO/fishhook3.png" alt=""></p><p><strong>3.在 Dynamic Symbols Table -&gt; Indirect Symbols 表中找到 Data 值为 0x16F 的符号，其位于该表中的位置（第一个）就是它在懒加载表中对应的位置。</strong></p><p><img src="/res/MachO/fishhook4.png" alt=""></p><p><strong>4.懒加载表中对应位置的 Offset 值就是该指针最终的偏移量：</strong></p><p><img src="/res/MachO/fishhook5.png" alt=""></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;fishhook简介&quot;&gt;&lt;a href=&quot;#fishhook简介&quot; class=&quot;headerlink&quot; title=&quot;fishhook简介&quot;&gt;&lt;/a&gt;fishhook简介&lt;/h2&gt;&lt;p&gt;C 语言往往会给我们留下不可修改的这一印象,而 &lt;a href=&quot;https://github.com/facebook/fishhook&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;fishhook&lt;/a&gt; 是一个由 facebook 开源的第三方框架，其主要作用就是&lt;strong&gt;动态修改 C 语言函数实现&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;这个框架的代码其实非常的简单，只包含两个文件：fishhook.c 以及 fishhook.h；两个文件所有的代码加起来也不超过 300 行。不过它的实现原理是非常有意思并且精妙的&lt;/p&gt;
&lt;p&gt;fishhook 提供非常简单的两个接口以及一个结构体：&lt;/p&gt;
&lt;figure class=&quot;highlight c&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;6&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;7&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;8&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;9&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;10&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;11&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;class&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;struct&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;rebinding&lt;/span&gt; &amp;#123;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;	&lt;span class=&quot;keyword&quot;&gt;const&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;char&lt;/span&gt; *name;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;	&lt;span class=&quot;keyword&quot;&gt;void&lt;/span&gt; *replacement;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;	&lt;span class=&quot;keyword&quot;&gt;void&lt;/span&gt; **replaced;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;int&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;rebind_symbols&lt;/span&gt;&lt;span class=&quot;params&quot;&gt;(struct rebinding rebindings[], &lt;span class=&quot;keyword&quot;&gt;size_t&lt;/span&gt; rebindings_nel)&lt;/span&gt;&lt;/span&gt;;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;int&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;rebind_symbols_image&lt;/span&gt;&lt;span class=&quot;params&quot;&gt;(&lt;span class=&quot;keyword&quot;&gt;void&lt;/span&gt; *header,&lt;span class=&quot;keyword&quot;&gt;intptr_t&lt;/span&gt; slide,&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;params&quot;&gt;struct rebinding rebindings[],&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;params&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;size_t&lt;/span&gt; rebindings_nel)&lt;/span&gt;&lt;/span&gt;;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;我们可以从 fishhook 提供的demo中上手实践一下,这里的demo对 &lt;code&gt;close&lt;/code&gt; 进行修改:&lt;/p&gt;
    
    </summary>
    
      <category term="重学iOS" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/"/>
    
    
      <category term="iOS" scheme="https://developerdoc.com/tags/iOS/"/>
    
      <category term="fishhook" scheme="https://developerdoc.com/tags/fishhook/"/>
    
  </entry>
  
  <entry>
    <title>Mach-O文件格式字段简介</title>
    <link href="https://developerdoc.com/essay/LLDB/Mach-O_file/"/>
    <id>https://developerdoc.com/essay/LLDB/Mach-O_file/</id>
    <published>2019-04-18T06:36:20.000Z</published>
    <updated>2019-05-07T07:51:02.511Z</updated>
    
    <content type="html"><![CDATA[<h2 id="概览"><a href="#概览" class="headerlink" title="概览"></a>概览</h2><p>本次来学习下Mach-O文件的格式,Mach-O（Mach Object File Format）是 macOS 上的可执行文件格式。</p><p>Mach-O 文件格式如下:</p><p><img src="/res/MachO/macho_struct.png" width="70%"></p><p>新建一个单页面工程 使用 <a href="https://sourceforge.net/projects/machoview/" target="_blank" rel="noopener">MachOView</a> 来查看.app包内的mach-o文件:<br><a id="more"></a></p><p><img src="/res/MachO/macho1.png" alt=""></p><p>结合可知 Mach-O 文件包含了三部分内容：</p><ul><li>Header（头部），指明了 cpu 架构、大小端序、文件类型、Load Commands 个数等一些基本信息</li><li>Load Commands（加载命令)，正如官方的图所示，描述了怎样加载每个 Segment 的信息。在 Mach-O 文件中可以有多个 Segment，每个 Segment 可能包含一个或多个 Section。</li><li>Data（数据区），Segment 的具体数据，包含了代码和数据等。</li></ul><h3 id="Headers"><a href="#Headers" class="headerlink" title="Headers"></a>Headers</h3><p>Mach-O 文件的头部定义如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/include / mach-o / loader.h</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mach_header_64</span> &#123;</span></span><br><span class="line"><span class="keyword">uint32_t</span>magic;<span class="comment">/* 标志符 0xfeedface 是 32 位， 0xfeedfacf 是 64 位。*/</span></span><br><span class="line"><span class="keyword">cpu_type_t</span>cputype;<span class="comment">/* cpu 类型、平台 */</span></span><br><span class="line"><span class="keyword">cpu_subtype_t</span>cpusubtype;<span class="comment">/* cpu 类型、平台 */</span></span><br><span class="line"><span class="keyword">uint32_t</span>filetype;<span class="comment">/* 文件类型，可执行文件、符号文件（DSYM）、内核扩展等 */</span></span><br><span class="line"><span class="keyword">uint32_t</span>ncmds;<span class="comment">/* 加载 Load Commands 的数量 */</span></span><br><span class="line"><span class="keyword">uint32_t</span>sizeofcmds;<span class="comment">/* 加载 Load Commands 的大小 */</span></span><br><span class="line"><span class="keyword">uint32_t</span>flags;<span class="comment">/* dyld 动态链接器 加载的标志*/</span></span><br><span class="line"><span class="keyword">uint32_t</span>reserved;<span class="comment">/* 64 位的保留字段 */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>从上图中看出<code>asdasd</code>这个文件header对应的是 <code>MH_MAGIC_64</code> 、<code>CPU_TYPE_X86_64_ALL</code> 、<code>MH_EXECUTE</code>…等</p><p>filetype的定义有：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/include / mach-o / loader.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_OBJECT    0x1        <span class="comment">/* Target 文件：编译器对源码编译后得到的中间结果 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_EXECUTE    0x2        <span class="comment">/* 可执行二进制文件 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_FVMLIB    0x3        <span class="comment">/* VM 共享库文件（还不清楚是什么东西） */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_CORE        0x4        <span class="comment">/* Core 文件，一般在 App Crash 产生 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_PRELOAD    0x5        <span class="comment">/* preloaded executable file */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_DYLIB    0x6        <span class="comment">/* 动态库 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_DYLINKER    0x7        <span class="comment">/* 动态连接器 /usr/lib/dyld */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_BUNDLE    0x8        <span class="comment">/* 非独立的二进制文件，往往通过 gcc-bundle 生成 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_DYLIB_STUB    0x9        <span class="comment">/* 静态链接文件（还不清楚是什么东西） */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_DSYM        0xa        <span class="comment">/* 符号文件以及调试信息，在解析堆栈符号中常用 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_KEXT_BUNDLE    0xb        <span class="comment">/* x86_64 内核扩展 */</span></span></span><br></pre></td></tr></table></figure><p>和部分flags定义：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_NOUNDEFS    0x1        <span class="comment">/* Target 文件中没有带未定义的符号，常为静态二进制文件 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MH_SPLIT_SEGS    0x20  <span class="comment">/* Target 文件中的只读 Segment 和可读写 Segment 分开  */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MH_TWOLEVEL    0x80        <span class="comment">/* 该 Image 使用二级命名空间(two name space binding)绑定方案 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MH_FORCE_FLAT    0x100 <span class="comment">/* 使用扁平命名空间(flat name space binding)绑定（与 MH_TWOLEVEL 互斥） */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MH_WEAK_DEFINES    0x8000 <span class="comment">/* 二进制文件使用了弱符号 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MH_BINDS_TO_WEAK 0x10000 <span class="comment">/* 二进制文件链接了弱符号 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MH_ALLOW_STACK_EXECUTION 0x20000<span class="comment">/* 允许 Stack 可执行 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    MH_PIE 0x200000  <span class="comment">/* 对可执行的文件类型启用地址空间 layout 随机化 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MH_NO_HEAP_EXECUTION 0x1000000 <span class="comment">/* 将 Heap 标记为不可执行，可防止 heap spray 攻击 */</span></span></span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>Mach-O 文件头主要目的是为加载命令提供信息。加载命令过程紧跟在头之后，并且 ncmds 和 sizeofcmds 来能个字段将会用在加载命令的过程中。</p><h3 id="Load-Commands"><a href="#Load-Commands" class="headerlink" title="Load Commands"></a>Load Commands</h3><p>Headers之后就是Load Commands,其占用的内存和加载命令总数已经在Header中 ncmds/sizeofcmds 中指出。</p><p><img src="/res/MachO/loadcommand1.png" alt=""></p><p>load commands的定义如下:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/include / mach-o / loader.h</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">load_command</span> &#123;</span></span><br><span class="line"><span class="keyword">uint32_t</span> cmd;<span class="comment">/*  load command 类型*/</span></span><br><span class="line"><span class="keyword">uint32_t</span> cmdsize;<span class="comment">/* command大小 用于计算出到下一个 command 的偏移量*/</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>cmd 字段指出了 command 类型,主要有：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/include / mach-o / loader.h</span></span><br><span class="line">LC_SEGMENT、LC_SEGMENT_64 将 segment 映射到进程的内存空间，</span><br><span class="line">LC_UUID 二进制文件 id，与符号表 uuid 对应，可用作符号表匹配，</span><br><span class="line">LC_LOAD_DYLINKER 启动动态加载器，</span><br><span class="line">LC_SYMTAB 描述在 __LINKEDIT 段的哪找字符串表、符号表，</span><br><span class="line">LC_CODE_SIGNATURE 代码签名等</span><br></pre></td></tr></table></figure><p>LC_SEGMENT_64和LC_SEGMENT是加载的主要命令，它负责指导内核来设置进程的内存空间。一般Mach-O文件有多个段(Segement)，段每个段有不同的功能，一般包括：</p><ol><li>__PAGEZERO: 空指针陷阱段，映射到虚拟内存空间的第一页，用于捕捉对NULL指针的引用；</li><li>__TEXT: 包含了执行代码以及其他只读数据。该段数据的保护级别为：VM_PROT_READ（读）、VM_PROT_EXECUTE(执行)，防止在内存中被修改；</li><li>__DATA: 包含了程序数据，该段可写；</li><li>__LINKEDIT: 链接器使用的符号以及其他表</li></ol><h3 id="Segment-amp-Section"><a href="#Segment-amp-Section" class="headerlink" title="Segment &amp; Section"></a>Segment &amp; Section</h3><p>Segment定义如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/include / mach-o / loader.h</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">segment_command_64</span> &#123;</span> </span><br><span class="line">    <span class="keyword">uint32_t</span>    cmd;        <span class="comment">/* LC_SEGMENT_64 */</span></span><br><span class="line">    <span class="keyword">uint32_t</span>    cmdsize;    <span class="comment">/* section_64 结构体所需要的空间 */</span></span><br><span class="line">    <span class="keyword">char</span>        segname[<span class="number">16</span>];    <span class="comment">/* segment 名字，上述宏中的定义 */</span></span><br><span class="line">    <span class="keyword">uint64_t</span>    vmaddr;        <span class="comment">/* 所描述段的虚拟内存地址 */</span></span><br><span class="line">    <span class="keyword">uint64_t</span>    vmsize;        <span class="comment">/* 为当前段分配的虚拟内存大小 */</span></span><br><span class="line">    <span class="keyword">uint64_t</span>    fileoff;    <span class="comment">/* 当前段在文件中的偏移量 */</span></span><br><span class="line">    <span class="keyword">uint64_t</span>    filesize;    <span class="comment">/* 当前段在文件中占用的字节 */</span></span><br><span class="line">    <span class="keyword">vm_prot_t</span>    maxprot;    <span class="comment">/* 段所在页所需要的最高内存保护，用八进制表示 */</span></span><br><span class="line">    <span class="keyword">vm_prot_t</span>    initprot;    <span class="comment">/* 段所在页原始内存保护 */</span></span><br><span class="line">    <span class="keyword">uint32_t</span>    nsects;        <span class="comment">/* 段中 Section 数量 */</span></span><br><span class="line">    <span class="keyword">uint32_t</span>    flags;        <span class="comment">/* 标识符 */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>其中 segname 在源码中定义的宏，有：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    SEG_PAGEZERO    <span class="meta-string">"__PAGEZERO"</span> <span class="comment">/* 当时 MH_EXECUTE 文件时，捕获到空指针 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    SEG_TEXT    <span class="meta-string">"__TEXT"</span> <span class="comment">/* 代码/只读数据段 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    SEG_DATA    <span class="meta-string">"__DATA"</span> <span class="comment">/* 数据段 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    SEG_OBJC    <span class="meta-string">"__OBJC"</span> <span class="comment">/* Objective-C runtime 段 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>    SEG_LINKEDIT    <span class="meta-string">"__LINKEDIT"</span> <span class="comment">/* 包含需要被动态链接器使用的符号和其他表，包括符号表、字符串表等 */</span></span></span><br></pre></td></tr></table></figure><p>部分的 Segment （主要指的 <code>__TEXT</code> 和 <code>__DATA</code>）可以进一步分解为 Section。之所以按照 Segment -&gt; Section 的结构组织方式，是因为在同一个 Segment 下的 Section，可以控制相同的权限，也可以不完全按照 Page 的大小进行内存对其，节省内存的空间。而 Segment 对外整体暴露，在程序载入阶段映射成一个完整的虚拟内存，更好的做到内存对齐（可以继续参考 OS X &amp; iOS Kernel Programming 一书的第一章内容)</p><p>Section定义如下</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/include / mach-o / loader.h</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">section_64</span> &#123;</span> </span><br><span class="line">    <span class="keyword">char</span>        sectname[<span class="number">16</span>];    <span class="comment">/* Section 名字 */</span></span><br><span class="line">    <span class="keyword">char</span>        segname[<span class="number">16</span>];    <span class="comment">/* Section 所在的 Segment 名称 */</span></span><br><span class="line">    <span class="keyword">uint64_t</span>    addr;        <span class="comment">/* Section 所在的内存地址 */</span></span><br><span class="line">    <span class="keyword">uint64_t</span>    size;        <span class="comment">/* Section 的大小 */</span></span><br><span class="line">    <span class="keyword">uint32_t</span>    offset;        <span class="comment">/* Section 所在的文件偏移 */</span></span><br><span class="line">    <span class="keyword">uint32_t</span>    align;        <span class="comment">/* Section 的内存对齐边界 (2 的次幂) */</span></span><br><span class="line">    <span class="keyword">uint32_t</span>    reloff;        <span class="comment">/* 重定位信息的文件偏移 */</span></span><br><span class="line">    <span class="keyword">uint32_t</span>    nreloc;        <span class="comment">/* 重定位条目的数目 */</span></span><br><span class="line">    <span class="keyword">uint32_t</span>    flags;        <span class="comment">/* 标志属性 */</span></span><br><span class="line">    <span class="keyword">uint32_t</span>    reserved1;    <span class="comment">/* 保留字段1 (for offset or index) */</span></span><br><span class="line">    <span class="keyword">uint32_t</span>    reserved2;    <span class="comment">/* 保留字段2 (for count or sizeof) */</span></span><br><span class="line">    <span class="keyword">uint32_t</span>    reserved3;    <span class="comment">/* 保留字段3 */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>下面列举一些常见的 Section。</p><table><thead><tr><th style="text-align:left">Section</th><th style="text-align:center">用途</th></tr></thead><tbody><tr><td style="text-align:left"><code>__TEXT.__text</code></td><td style="text-align:center">主程序代码</td></tr><tr><td style="text-align:left"><code>__TEXT.__cstring</code></td><td style="text-align:center">C 语言字符串</td></tr><tr><td style="text-align:left"><code>__TEXT.__const</code></td><td style="text-align:center">const 关键字修饰的常量</td></tr><tr><td style="text-align:left"><code>__TEXT.__stubs</code></td><td style="text-align:center">用于 Stub 的占位代码，很多地方称之为桩代码。</td></tr><tr><td style="text-align:left"><code>__TEXT.__stubs_helper</code></td><td style="text-align:center">当 Stub 无法找到真正的符号地址后的最终指向</td></tr><tr><td style="text-align:left"><code>__TEXT.__objc_methname</code></td><td style="text-align:center">Objective-C 方法名称</td></tr><tr><td style="text-align:left"><code>__TEXT.__objc_methtype</code></td><td style="text-align:center">Objective-C 方法类型</td></tr><tr><td style="text-align:left"><code>__TEXT.__objc_classname</code></td><td style="text-align:center">Objective-C 类名称</td></tr><tr><td style="text-align:left"><code>__DATA.__data</code></td><td style="text-align:center">初始化过的可变数据</td></tr><tr><td style="text-align:left"><code>__DATA.__la_symbol_ptr</code></td><td style="text-align:center">lazy binding 的指针表，表中的指针一开始都指向 __stub_helper</td></tr><tr><td style="text-align:left"><code>__DATA.__nl_symbol_ptr</code></td><td style="text-align:center">非 lazy binding 的指针表，每个表项中的指针都指向一个在装载过程中，被动态链机器搜索完成的符号</td></tr><tr><td style="text-align:left"><code>__DATA.__const</code></td><td style="text-align:center">没有初始化过的常量</td></tr><tr><td style="text-align:left"><code>__DATA.__cfstring</code></td><td style="text-align:center">程序中使用的 Core Foundation 字符串（CFStringRefs）</td></tr><tr><td style="text-align:left"><code>__DATA.__bss</code></td><td style="text-align:center">BSS，存放为初始化的全局变量，即常说的静态内存分配</td></tr><tr><td style="text-align:left"><code>__DATA.__common</code></td><td style="text-align:center">没有初始化过的符号声明</td></tr><tr><td style="text-align:left"><code>__DATA.__objc_classlist</code></td><td style="text-align:center">Objective-C 类列表</td></tr><tr><td style="text-align:left"><code>__DATA.__objc_protolist</code></td><td style="text-align:center">Objective-C 原型</td></tr><tr><td style="text-align:left"><code>__DATA.__objc_imginfo</code></td><td style="text-align:center">Objective-C 镜像信息</td></tr><tr><td style="text-align:left"><code>__DATA.__objc_selfrefs</code></td><td style="text-align:center">Objective-C self 引用</td></tr><tr><td style="text-align:left"><code>__DATA.__objc_protorefs</code></td><td style="text-align:center">Objective-C 原型引用</td></tr><tr><td style="text-align:left"><code>__DATA.__objc_superrefs</code></td><td style="text-align:center">Objective-C 超类引用</td></tr></tbody></table><p><code>__TEXT.__text</code> 这里存放的是汇编后的代码，当我们进行编译时，每个.m文件会经过预编译-&gt;编译-&gt;汇编形成.o文件，称之为目标文件。汇编后，所有的代码会形成汇编指令存储在.o文件的(<strong>TEXT,</strong>text)区（（<strong>DATA,</strong>data）也是类似）。链接后，所有的.o文件会合并成一个文件，所有.o文件的(<strong>TEXT,</strong>text)数据都会按链接顺序存放到应用文件的(<strong>TEXT,</strong>text)中。</p><p><code>__DATA.__data</code> 存储数据的section，static在进行非零赋值后会存储在这里，如果static 变量没有赋值或者赋值为0，那么它会存储在<code>__DATA.__bss</code>中。</p><p>另外还有 <code>Symbol Table</code>符号表，这个是重点中的重点，符号表是将地址和符号联系起来的桥梁。符号表并不能直接存储符号，而是存储符号位于字符串表的位置。</p><p><code>String Table</code>字符串表所有的变量名、函数名等，都以字符串的形式存储在字符串表中。</p><h3 id="实践：关联类的方法名"><a href="#实践：关联类的方法名" class="headerlink" title="实践：关联类的方法名"></a>实践：关联类的方法名</h3><p>上面说了一大堆定义，来查看使用Mach-O文件呢。这里来看一下如何用 MachO 文件关联类的方法名。</p><p>先来看看 <code>Load Commands</code>里的表示类名的 <code>__objc_classname</code> :</p><p><img src="/res/MachO/macho-find.png" alt=""></p><p>根据 offset字段 <code>0000425F</code> 值可以找到 section中对应的 <code>__TEXT,__objc_classname</code> 信息:</p><p><img src="/res/MachO/macho-find2.png" alt=""></p><p>同理可以找到对应方法名的 <code>__objc_methname</code> 和 表示类虚拟地址的 <code>__objc_classlist</code> :</p><p><img src="/res/MachO/macho-find3.png" alt=""></p><p>其中我们需要查看类的方法名信息就在<code>__objc_classlist</code>中，根据上图中 <code>_OBJC_CLASS_$_ViewController</code> 对应的data : <code>00000001000060E8</code>,可以在<code>__DATA,__objc_data</code>中找到类结构信息：</p><p><img src="/res/MachO/macho-find4.png" alt=""></p><p>其中data 是我们感兴趣的，它指向 class_ro_t， class_ro_t 存储了类在编译器就确定的属性、方法、协议等。根据data的值 <code>0x100005370</code>就找到了<code>__DATA,__objc_const</code>中：</p><p><img src="/res/MachO/macho-find5.png" alt=""></p><p>其中baseMethods <code>0x100005350</code> 指向了第一个方法。这样我们就就找到了类的方法名。</p><h2 id="使用命令查看Mach-O信息"><a href="#使用命令查看Mach-O信息" class="headerlink" title="使用命令查看Mach-O信息"></a>使用命令查看Mach-O信息</h2><p>上文是借助 MachOView 这个工具 和 <code>loader.h</code> 来了解Mach-O文件的大概定义，现在我们来使用<code>size</code>和<code>otool</code> 查看一下Mach-O文件。</p><p>如下这样一个 hello.c文件：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"hello world\n"</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用clang生成Mach-O文件：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#生成 hello.out 的 Mach-O 二进制文件</span></span><br><span class="line">xcrun clang hello.c -o hello.out</span><br></pre></td></tr></table></figure><p>可以通过 <code>file</code> 命令来查看简要的架构信息:</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">file hello.out </span><br><span class="line"><span class="comment">#输出：</span></span><br><span class="line">hello.out: Mach-O 64-bit executable x86_64</span><br></pre></td></tr></table></figure><h3 id="一、-Section"><a href="#一、-Section" class="headerlink" title="一、 Section"></a>一、 Section</h3><p>就像我们上面提到的一样，这里有些东西叫做 section。一个可执行文件包含多个段，也就是多个 section。可执行文件不同的部分将加载进不同的 section，并且每个 section 会转换进某个 segment 里。这个概念对于所有的可执行文件都是成立的。</p><p>我们来看看 hello.out 二进制中的 section。我们可以使用 size 工具来观察</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">size -x -l -m hello.out </span><br><span class="line"><span class="comment">#输出</span></span><br><span class="line">Segment __PAGEZERO: 0x100000000 (vmaddr 0x0 fileoff 0)</span><br><span class="line">Segment __TEXT: 0x1000 (vmaddr 0x100000000 fileoff 0)</span><br><span class="line">Section __text: 0x2a (addr 0x100000f60 offset 3936)</span><br><span class="line">Section __stubs: 0x6 (addr 0x100000f8a offset 3978)</span><br><span class="line">Section __stub_helper: 0x1a (addr 0x100000f90 offset 3984)</span><br><span class="line">Section __cstring: 0xd (addr 0x100000faa offset 4010)</span><br><span class="line">Section __unwind_info: 0x48 (addr 0x100000fb8 offset 4024)</span><br><span class="line">total 0x9f</span><br><span class="line">Segment __DATA: 0x1000 (vmaddr 0x100001000 fileoff 4096)</span><br><span class="line">Section __nl_symbol_ptr: 0x10 (addr 0x100001000 offset 4096)</span><br><span class="line">Section __la_symbol_ptr: 0x8 (addr 0x100001010 offset 4112)</span><br><span class="line">total 0x18</span><br><span class="line">Segment __LINKEDIT: 0x1000 (vmaddr 0x100002000 fileoff 8192)</span><br><span class="line">total 0x100003000</span><br></pre></td></tr></table></figure><p>如上代码所示，我们的 <code>hello.out</code> 文件有 4 个 segment。有些 segment 中有多个 section。</p><p>当运行一个可执行文件时，虚拟内存 (VM - virtual memory) 系统将 segment 映射到进程的地址空间上。映射完全不同于我们一般的认识，如果你对虚拟内存系统不熟悉，可以简单的想象虚拟内存系统将整个可执行文件加载进内存 – 虽然在实际上不是这样的。VM 使用了一些技巧来避免全部加载。</p><p>当虚拟内存系统进行映射时，segment 和 section 会以不同的参数和权限被映射。</p><p>上面的代码中，__TEXT segment 包含了被执行的代码。它被以只读和可执行的方式映射。进程被允许执行这些代码，但是不能修改。这些代码也不能对自己做出修改，因此这些被映射的页从来不会被改变。</p><p>__DATA segment 以可读写和不可执行的方式映射。它包含了将会被更改的数据。</p><p>第一个 segment 是 __PAGEZERO。它的大小为 4GB。这 4GB 并不是文件的真实大小，但是规定了进程地址空间的前 4GB 被映射为 不可执行、不可写和不可读。这就是为什么当读写一个 NULL 指针或更小的值时会得到一个 EXC_BAD_ACCESS 错误。这是操作系统在尝试防止引起系统崩溃。</p><p>在 segment中，一般都会有多个 section。它们包含了可执行文件的不同部分。在 <strong>TEXT segment 中，</strong>text section 包含了编译所得到的机器码。<strong>stubs 和 </strong>stub_helper 是给动态链接器 (dyld) 使用的。通过这两个 section，在动态链接代码中，可以允许延迟链接。<strong>const (在我们的代码中没有) 是常量，不可变的，就像 </strong>cstring (包含了可执行文件中的字符串常量 – 在源码中被双引号包含的字符串) 常量一样。</p><p><strong>DATA segment 中包含了可读写数据。在我们的程序中只有 </strong>nl_symbol_ptr 和 __la_symbol_ptr，它们分别是 non-lazy 和 lazy 符号指针。延迟符号指针用于可执行文件中调用未定义的函数，例如不包含在可执行文件中的函数，它们将会延迟加载。而针对非延迟符号指针，当可执行文件被加载同时，也会被加载。</p><p>在 _DATA segment 中的其它常见 section 包括 <strong>const，在这里面会包含一些需要重定向的常量数据。例如 char * const p = “foo”; – p 指针指向的数据是可变的。</strong>bss section 没有被初始化的静态变量，例如 static int a; – ANSI C 标准规定静态变量必须设置为 0。并且在运行时静态变量的值是可以修改的。<strong>common section 包含未初始化的外部全局变量，跟 static 变量类似。例如在函数外面定义的 int a;。最后，</strong>dyld 是一个 section 占位符，被用于动态链接器。</p><h3 id="二、Section-中的内容"><a href="#二、Section-中的内容" class="headerlink" title="二、Section 中的内容"></a>二、Section 中的内容</h3><p>可以通过 <code>otool</code> 来了解section中的内容</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">otool -s  __TEXT __text  hello.out </span><br><span class="line"><span class="comment">#输出</span></span><br><span class="line">hello.out:</span><br><span class="line">Contents of (__TEXT,__text) section</span><br><span class="line">0000000100000f6055 48 89 e5 48 83 ec 10 c7 45 <span class="built_in">fc</span> 00 00 00 00 48 </span><br><span class="line">0000000100000f708d 3d 34 00 00 00 b0 00 e8 0d 00 00 00 31 c9 89 </span><br><span class="line">0000000100000f8045 f8 89 c8 48 83 c4 10 5d c3</span><br></pre></td></tr></table></figure><p>我们还可以通过添加 -v 来查看反汇编代码 , 由于 -s <strong>TEXT </strong>text 很常见，otool 对其设置了一个缩写 -t ：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">otool -v -t  hello.out</span><br><span class="line"><span class="comment">#输出 </span></span><br><span class="line">hello.out:</span><br><span class="line">(__TEXT,__text) section</span><br><span class="line">_main:</span><br><span class="line">0000000100000f60pushq%rbp</span><br><span class="line">0000000100000f61movq%rsp, %rbp</span><br><span class="line">0000000100000f64subq<span class="variable">$0x10</span>, %rsp</span><br><span class="line">0000000100000f68movl<span class="variable">$0x0</span>, -0x4(%rbp)</span><br><span class="line">0000000100000f6fleaq0x34(%rip), %rdi</span><br><span class="line">0000000100000f76movb<span class="variable">$0x0</span>, %al</span><br><span class="line">0000000100000f78callq0x100000f8a</span><br><span class="line">0000000100000f7dxorl%ecx, %ecx</span><br><span class="line">0000000100000f7fmovl%eax, -0x8(%rbp)</span><br><span class="line">0000000100000f82movl%ecx, %eax</span><br><span class="line">0000000100000f84addq<span class="variable">$0x10</span>, %rsp</span><br><span class="line">0000000100000f88popq%rbp</span><br><span class="line">0000000100000f89retq</span><br></pre></td></tr></table></figure><p>上面的内容是一样的，只不过以反汇编形式显示出来。你应该感觉很熟悉，这就是我们在前面编译时候的代码。唯一的不同就是，在这里我们没有任何的汇编指令在里面。这是纯粹的二进制执行文件。</p><!-- ## 应用了解Mach-O文件的主要应用之一是代码大小性能优化。### 可执行文件瘦身__objc_classlist 和 __objc_classrefs### 获取调用堆栈 -->]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;概览&quot;&gt;&lt;a href=&quot;#概览&quot; class=&quot;headerlink&quot; title=&quot;概览&quot;&gt;&lt;/a&gt;概览&lt;/h2&gt;&lt;p&gt;本次来学习下Mach-O文件的格式,Mach-O（Mach Object File Format）是 macOS 上的可执行文件格式。&lt;/p&gt;
&lt;p&gt;Mach-O 文件格式如下:&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/res/MachO/macho_struct.png&quot; width=&quot;70%&quot;&gt;&lt;/p&gt;
&lt;p&gt;新建一个单页面工程 使用 &lt;a href=&quot;https://sourceforge.net/projects/machoview/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;MachOView&lt;/a&gt; 来查看.app包内的mach-o文件:&lt;br&gt;
    
    </summary>
    
      <category term="重学iOS" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/"/>
    
    
      <category term="iOS" scheme="https://developerdoc.com/tags/iOS/"/>
    
      <category term="Mach-O" scheme="https://developerdoc.com/tags/Mach-O/"/>
    
  </entry>
  
  <entry>
    <title>iOS编译过程</title>
    <link href="https://developerdoc.com/essay/LLDB/iOS_Compiler/"/>
    <id>https://developerdoc.com/essay/LLDB/iOS_Compiler/</id>
    <published>2019-04-10T02:54:52.000Z</published>
    <updated>2019-04-23T02:17:39.367Z</updated>
    
    <content type="html"><![CDATA[<h2 id="编译器"><a href="#编译器" class="headerlink" title="编译器"></a>编译器</h2><blockquote><p>把一种编程语言(原始语言)转换为另一种编程语言(目标语言)的程序叫做编译器</p></blockquote><p>大多数编译器由两部分组成：前端和后端。</p><ul><li>前端负责词法分析，语法分析，生成中间代码；</li><li>后端以中间代码作为输入，进行行架构无关的代码优化，接着针对不同架构生成不同的机器码。</li></ul><p>Objective C/C/C++使用的编译器前端是clang，swift是swift，后端都是LLVM。</p><h3 id="llvm介绍"><a href="#llvm介绍" class="headerlink" title="llvm介绍"></a>llvm介绍</h3><p>LLVM是什么，是low level virtual machine的简称，其实是一个编译器框架。LLVM 的优点主要得益于它的三层式架构 – 第一层支持多种语言作为输入(例如 C, ObjectiveC, C++ 和 Haskell)，第二层是一个共享式的优化器(对 LLVM IR 做优化处理)，第三层是许多不同的目标平台(例如 Intel, ARM 和 PowerPC)。编译器前端主要进行语法分析，语义分析，生成中间代码。编译器后端会进行机器无关的代码优化，生成机器语言，并且进行机器相关的代码优化，根据不同的系统架构生成不同的机器码。</p><p><img src="/res/LLVM/LLVMCompiler1.png" alt=""></p><p>现在，Xcode 的默认编译器是 clang。本文中我们提到的编译器都表示 clang。clang 的功能是首先对 Objective-C 代码做分析检查，然后将其转换为低级的类汇编代码：LLVM Intermediate Representation(LLVM 中间表达码)。接着 LLVM 会执行相关指令将 LLVM IR 编译成目标平台上的本地字节码，这个过程的完成方式可以是即时编译 (Just-in-time)，或在编译的时候完成。而Clang 是一个C、C++、Objective-C和Objective-C++编程语言的编译器前端。它采用了底层虚拟机（LLVM）作为其后端。它的目标是提供一个GNU编译器套装（GCC）的替代品。Clang项目包括Clang前端和Clang静态分析器等。</p><p>下图是iOS编译过程：<br><img src="/res/LLVM/iOSCompiler.png" alt=""></p><a id="more"></a><h2 id="使用llvm命令"><a href="#使用llvm命令" class="headerlink" title="使用llvm命令"></a>使用llvm命令</h2><p><strong><em>一、使用xcode自带</em></strong></p><p>使用xcrun来调用：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">xcrun clang -v</span><br><span class="line"><span class="comment">#Apple LLVM version 10.0.0 (clang-1000.11.45.5)</span></span><br><span class="line"><span class="comment">#Target: x86_64-apple-darwin17.7.0</span></span><br><span class="line"><span class="comment">#Thread model: posix</span></span><br><span class="line"><span class="comment">#InstalledDir: /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin</span></span><br></pre></td></tr></table></figure><p><strong><em>二、使用HomeBrew安装</em></strong></p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1.使用以下命令安装llvm</span></span><br><span class="line">brew install --with-toolchain llvm</span><br><span class="line"><span class="comment"># 若安装遇到问题 可以先升级brew试试</span></span><br><span class="line"><span class="comment">#   brew update 这会更新 Homebrew 自己</span></span><br><span class="line"><span class="comment">#   brew upgrade 升级所有可以升级的软件们</span></span><br></pre></td></tr></table></figure><p>安装后可以使用 <code>brew info llvm</code> 检查信息。brew安装llvm后，不会直接添加命令的快捷引用，需要自己手动来完成。</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看llvm的安装路径</span></span><br><span class="line">brew --prefix llvm</span><br><span class="line"><span class="comment"># 添加引用示例：</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">'export PATH="/usr/local/opt/llvm/bin:$PATH"'</span> &gt;&gt; ~/.bash_profile</span><br><span class="line"><span class="built_in">source</span> ~/.bash_profile</span><br></pre></td></tr></table></figure><!-- https://embeddedartistry.com/blog/2017/2/20/installing-clangllvm-on-osx --><p>这样之后就可以直接调用自己安装的llvm了：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">clang -v</span><br><span class="line"><span class="comment">#clang version 8.0.0 (tags/RELEASE_800/final)</span></span><br><span class="line"><span class="comment">#Target: x86_64-apple-darwin17.7.0</span></span><br><span class="line"><span class="comment">#Thread model: posix</span></span><br><span class="line"><span class="comment">#InstalledDir: /usr/local/opt/llvm/bin</span></span><br></pre></td></tr></table></figure><h2 id="编译器处理过程"><a href="#编译器处理过程" class="headerlink" title="编译器处理过程"></a>编译器处理过程</h2><p>在编译一个源文件时，编译器的处理过程分为几个阶段。要想查看编译 hello.m 源文件需要几个不同的阶段，上文中安装llvm后，我们可以让通过 clang 命令观察：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">sunTongShengdeMacBook-Pro:asdasd suntongsheng$ clang -ccc-print-phases ViewController.m</span><br><span class="line">0: input, &quot;ViewController.m&quot;, objective-c</span><br><span class="line">1: preprocessor, &#123;0&#125;, objective-c-cpp-output</span><br><span class="line">2: compiler, &#123;1&#125;, ir</span><br><span class="line">3: backend, &#123;2&#125;, assembler</span><br><span class="line">4: assembler, &#123;3&#125;, object</span><br><span class="line">5: linker, &#123;4&#125;, image</span><br><span class="line">6: bind-arch, &quot;x86_64&quot;, &#123;5&#125;, image</span><br></pre></td></tr></table></figure><p>可以看到clang将其分为 input、preprocessor 、compiler、backend、assembler、linker、bind-arch几个阶段。</p><ul><li>预处理阶段：符号化、宏定义展开头文件展开</li><li>语法和语义分析阶段：将符号化后的内容转化为一棵解析树、解析树做语义分析 输出一棵抽象语法树</li><li>生成代码和优化阶段：将 AST 转换为更低级的中间码 (LLVM IR)、对生成的中间码做优化、生成特定目标代码、输出汇编代码</li><li>汇编器阶段：将汇编代码转换为目标对象文件。</li><li>链接器：将多个目标对象文件合并为一个可执行文件 (或者一个动态库)</li></ul><h3 id="preprocessor-预处理"><a href="#preprocessor-预处理" class="headerlink" title="preprocessor 预处理"></a>preprocessor 预处理</h3><p>每当编源译文件的时候，编译器首先做的是一些预处理工作。具体表现为import头文件替换、macro宏展开、其他预编译指令，`#这个符号是编译器预处理的标志。</p><p><strong><em>一. 对头文件的处理：</em></strong></p><p>例如，如果在源文件中出现下述代码：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">&lt;Foundation/Foundation.h&gt;</span></span></span><br></pre></td></tr></table></figure><p>预处理器对这行代码的处理是用 Foundation.h 文件中的内容去替换这行代码，如果 Foundation.h 中也使用了类似的宏引入，则会按照同样的处理方式用各个宏对应的真正代码进行逐级替代。这也就是为什么人们主张头文件最好尽量少的去引入其他的类或库，因为引入的东西越多，编译器需要做的处理就越多</p><p><strong>示例</strong>：假设我们写了一个简单的 C 程序 hello.c:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"hello llvm\n"</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后给上面的代码执行以下预处理命令</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#  -E  Only run the preprocessor</span></span><br><span class="line">clang -E hello.c &gt;&gt; hello.o</span><br></pre></td></tr></table></figure><p>打开 hello.o ，发现有542行。但是如果在上述代码上加上 <code>#import &lt;Foundation/Foundation.h&gt;</code> ，hello.o 的行数暴增到9万多行。（当然对于这种情况引入了<a href="http://clang.llvm.org/docs/Modules.html" target="_blank" rel="noopener">模块 - modules</a>功能）</p><p>打开模块功能再试试：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#-fmodules 允许modules的语言特性</span></span><br><span class="line">clang  -fmodules -E  hello.c &gt;&gt; hello.1o</span><br><span class="line"></span><br><span class="line"><span class="comment">#hello.1o的内容：</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 1 "hello.c"</span></span><br><span class="line"><span class="comment"># 1 "&lt;built-in&gt;" 1</span></span><br><span class="line"><span class="comment"># 1 "&lt;built-in&gt;" 3</span></span><br><span class="line"><span class="comment"># 361 "&lt;built-in&gt;" 3</span></span><br><span class="line"><span class="comment"># 1 "&lt;command line&gt;" 1</span></span><br><span class="line"><span class="comment"># 1 "&lt;built-in&gt;" 2</span></span><br><span class="line"><span class="comment"># 1 "hello.c" 2</span></span><br><span class="line"><span class="comment">#pragma clang module import Darwin.C.stdio /* clang -E: implicit import for #include &lt;stdio.h&gt; */</span></span><br><span class="line"></span><br><span class="line">int <span class="function"><span class="title">main</span></span>() &#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"hello llvm\n"</span>);</span><br><span class="line">  <span class="built_in">return</span> 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong><em>二、对于宏的处理</em></strong></p><p>假设一段这样的代码:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// hello.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAX(a,b) a &gt; b ? a : b</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> i = <span class="number">200</span>;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"largest: %d\n"</span>, MAX(i++,<span class="number">100</span>));</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"i: %d\n"</span>, i);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>用 <code>clang -E hello.c</code> 进行宏展开的预处理结果是如下所示：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> i = <span class="number">200</span>;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"largest: %d\n"</span>, i++ &gt; <span class="number">100</span> ? i++ : <span class="number">100</span>);</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"i: %d\n"</span>, i);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>i++</code>被替换到了<code>a</code>中，而不是预想的 i++的值。</p><h3 id="compiler-词法分析解析标记"><a href="#compiler-词法分析解析标记" class="headerlink" title="compiler 词法分析解析标记"></a>compiler 词法分析解析标记</h3><h4 id="一、词法分析-Lexical-Analysis"><a href="#一、词法分析-Lexical-Analysis" class="headerlink" title="一、词法分析-Lexical Analysis"></a>一、词法分析-Lexical Analysis</h4><p>在compiler阶段，首先代码文本都会从 string 转化成特殊的标记流。将代码切成一个个 token，比如大小括号，等于号还有字符串等。是计算机科学中将字符序列转换为标记序列的过程。 例如，下面这段程序：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// hello.c</span></span><br><span class="line"><span class="keyword">int</span> main() &#123;</span><br><span class="line">  <span class="built_in">NSLog</span>(<span class="string">@"hello, %@"</span>, <span class="string">@"world"</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>利用 clang 命令 clang -Xclang -dump-tokens hello.m 来将上面代码的标记流导出：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#clang -Xclang -dump-tokens hello.c</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#控制台输出：</span></span><br><span class="line">int <span class="string">'int'</span> [StartOfLine]Loc=&lt;hello.c:1:1&gt;</span><br><span class="line">identifier <span class="string">'main'</span> [LeadingSpace]Loc=&lt;hello.c:1:5&gt;</span><br><span class="line">l_paren <span class="string">'('</span>Loc=&lt;hello.c:1:9&gt;</span><br><span class="line">r_paren <span class="string">')'</span>Loc=&lt;hello.c:1:10&gt;</span><br><span class="line">l_brace <span class="string">'&#123;'</span> [LeadingSpace]Loc=&lt;hello.c:1:12&gt;</span><br><span class="line">identifier <span class="string">'NSLog'</span> [StartOfLine] [LeadingSpace]Loc=&lt;hello.c:2:3&gt;</span><br><span class="line">l_paren <span class="string">'('</span>Loc=&lt;hello.c:2:8&gt;</span><br><span class="line">unknown <span class="string">'@'</span>Loc=&lt;hello.c:2:9&gt;</span><br><span class="line">string_literal <span class="string">'"hello, %@"'</span>Loc=&lt;hello.c:2:10&gt;</span><br><span class="line">comma <span class="string">','</span>Loc=&lt;hello.c:2:21&gt;</span><br><span class="line">unknown <span class="string">'@'</span> [LeadingSpace]Loc=&lt;hello.c:2:23&gt;</span><br><span class="line">string_literal <span class="string">'"world"'</span>Loc=&lt;hello.c:2:24&gt;</span><br><span class="line">r_paren <span class="string">')'</span>Loc=&lt;hello.c:2:31&gt;</span><br><span class="line">semi <span class="string">';'</span>Loc=&lt;hello.c:2:32&gt;</span><br><span class="line"><span class="built_in">return</span> <span class="string">'return'</span> [StartOfLine] [LeadingSpace]Loc=&lt;hello.c:3:3&gt;</span><br><span class="line">numeric_constant <span class="string">'0'</span> [LeadingSpace]Loc=&lt;hello.c:3:10&gt;</span><br><span class="line">semi <span class="string">';'</span>Loc=&lt;hello.c:3:11&gt;</span><br><span class="line">r_brace <span class="string">'&#125;'</span> [StartOfLine]Loc=&lt;hello.c:4:1&gt;</span><br><span class="line">eof <span class="string">''</span>Loc=&lt;hello.c:4:2&gt;</span><br></pre></td></tr></table></figure><p>每一个标记都包含了对应的源码内容和其在源码中的位置,如果编译过程中遇到什么问题，clang 能够在源码中指出出错的具体位置。</p><h4 id="二、语法分析-Semantic-Analysis"><a href="#二、语法分析-Semantic-Analysis" class="headerlink" title="二、语法分析 - Semantic Analysis"></a>二、语法分析 - Semantic Analysis</h4><p>之后上面的标记流会解析生成<a href="http://clang.llvm.org/docs/IntroductionToTheClangAST.html" target="_blank" rel="noopener">抽象语法树</a>，我们可以使用 <code>clang -Xclang -ast-dump -fsyntax-only hello.c</code> 来展现解析这个过程</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># clang -Xclang -ast-dump -fsyntax-only hello.c </span></span><br><span class="line"></span><br><span class="line"><span class="comment">#控制台输出：</span></span><br><span class="line">hello.c:2:3: warning: implicit declaration of <span class="keyword">function</span> <span class="string">'NSLog'</span> is invalid <span class="keyword">in</span> C99 [-Wimplicit-function-declaration]</span><br><span class="line">  NSLog(@<span class="string">"hello, %@"</span>, @<span class="string">"world"</span>);</span><br><span class="line">  ^</span><br><span class="line">hello.c:2:9: error: expected expression</span><br><span class="line">  NSLog(@<span class="string">"hello, %@"</span>, @<span class="string">"world"</span>);</span><br><span class="line">        ^</span><br><span class="line">hello.c:2:23: error: expected expression</span><br><span class="line">  NSLog(@<span class="string">"hello, %@"</span>, @<span class="string">"world"</span>);</span><br><span class="line">                      ^</span><br><span class="line">TranslationUnitDecl 0x7fe1d2816c08 &lt;&lt;invalid sloc&gt;&gt; &lt;invalid sloc&gt;</span><br><span class="line">|-TypedefDecl 0x7fe1d28174a0 &lt;&lt;invalid sloc&gt;&gt; &lt;invalid sloc&gt; implicit __int128_t <span class="string">'__int128'</span></span><br><span class="line">| `-BuiltinType 0x7fe1d28171a0 <span class="string">'__int128'</span></span><br><span class="line">|-TypedefDecl 0x7fe1d2817508 &lt;&lt;invalid sloc&gt;&gt; &lt;invalid sloc&gt; implicit __uint128_t <span class="string">'unsigned __int128'</span></span><br><span class="line">| `-BuiltinType 0x7fe1d28171c0 <span class="string">'unsigned __int128'</span></span><br><span class="line">|-TypedefDecl 0x7fe1d28177b8 &lt;&lt;invalid sloc&gt;&gt; &lt;invalid sloc&gt; implicit __NSConstantString <span class="string">'struct __NSConstantString_tag'</span></span><br><span class="line">| `-RecordType 0x7fe1d28175d0 <span class="string">'struct __NSConstantString_tag'</span></span><br><span class="line">|   `-Record 0x7fe1d2817558 <span class="string">'__NSConstantString_tag'</span></span><br><span class="line">|-TypedefDecl 0x7fe1d2817850 &lt;&lt;invalid sloc&gt;&gt; &lt;invalid sloc&gt; implicit __builtin_ms_va_list <span class="string">'char *'</span></span><br><span class="line">| `-PointerType 0x7fe1d2817810 <span class="string">'char *'</span></span><br><span class="line">|   `-BuiltinType 0x7fe1d2816ca0 <span class="string">'char'</span></span><br><span class="line">|-TypedefDecl 0x7fe1d2817af8 &lt;&lt;invalid sloc&gt;&gt; &lt;invalid sloc&gt; implicit __builtin_va_list <span class="string">'struct __va_list_tag [1]'</span></span><br><span class="line">| `-ConstantArrayType 0x7fe1d2817aa0 <span class="string">'struct __va_list_tag [1]'</span> 1 </span><br><span class="line">|   `-RecordType 0x7fe1d2817920 <span class="string">'struct __va_list_tag'</span></span><br><span class="line">|     `-Record 0x7fe1d28178a0 <span class="string">'__va_list_tag'</span></span><br><span class="line">`-FunctionDecl 0x7fe1d285d000 &lt;hello.c:1:1, line:4:1&gt; line:1:5 main <span class="string">'int ()'</span></span><br><span class="line">  `-CompoundStmt 0x7fe1d285d1f8 &lt;col:12, line:4:1&gt;</span><br><span class="line">    `-ReturnStmt 0x7fe1d285d1e8 &lt;line:3:3, col:10&gt;</span><br><span class="line">      `-IntegerLiteral 0x7fe1d285d1c8 &lt;col:10&gt; <span class="string">'int'</span> 0</span><br></pre></td></tr></table></figure><p>在抽象语法树中的每个节点都标注了其对应源码中的位置，同样的，如果产生了什么问题，clang 可以定位到问题所在处的源码位置。</p><h4 id="三、静态分析-Static-Analyzer"><a href="#三、静态分析-Static-Analyzer" class="headerlink" title="三、静态分析 - Static Analyzer"></a>三、静态分析 - Static Analyzer</h4><p>一旦编译器把源码生成了抽象语法树，编译器可以对这棵树做分析处理，以找出代码中的错误，比如类型检查：即检查程序中是否有类型错误。例如：如果代码中给某个对象发送了一个消息，编译器会检查这个对象是否实现了这个消息（函数、方法）。此外，clang 对整个程序还做了其它更高级的一些分析，以确保程序没有错误。</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#命令行执行 通过clang -cc1 -analyzer-checker-help可以列出能调用的 checker，但这些checker并不是所有都是默认开启的</span></span><br><span class="line">clang -cc1 -analyzer-checker-help</span><br><span class="line">OVERVIEW: Clang Static Analyzer Checkers List</span><br><span class="line"></span><br><span class="line">USAGE: -analyzer-checker &lt;CHECKER or PACKAGE,...&gt;</span><br><span class="line"></span><br><span class="line">CHECKERS:</span><br><span class="line">  alpha.clone.CloneChecker        Reports similar pieces of code.</span><br><span class="line">  alpha.core.BoolAssignment       Warn about assigning non-&#123;0,1&#125; values to Boolean variables</span><br><span class="line">  alpha.core.CallAndMessageUnInitRefArg</span><br><span class="line">                                  Check <span class="keyword">for</span> logical errors <span class="keyword">for</span> <span class="keyword">function</span> calls and Objective-C message expressions (e.g., uninitialized arguments, null <span class="keyword">function</span> pointers, and pointer to undefined variables)</span><br><span class="line">  alpha.core.CastSize             Check when castin</span><br><span class="line">  ...</span><br></pre></td></tr></table></figure><p>使用<a href="http://clang-analyzer.llvm.org.cn/scan-build.html" target="_blank" rel="noopener">scan-build</a>可以从命令行运行分析器,类似如：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">scan-build xcodebuild -project asdasd.xcodeproj</span><br><span class="line">scan-build: Using <span class="string">'/usr/local/Cellar/llvm/8.0.0/bin/clang-8'</span> <span class="keyword">for</span> static analysis</span><br><span class="line">Build settings from <span class="built_in">command</span> line:</span><br><span class="line">    CLANG_ANALYZER_EXEC = /usr/<span class="built_in">local</span>/Cellar/llvm/8.0.0/bin/clang-8</span><br><span class="line">    CLANG_ANALYZER_OTHER_FLAGS = </span><br><span class="line">    CLANG_ANALYZER_OUTPUT = plist-html</span><br><span class="line">    CLANG_ANALYZER_OUTPUT_DIR = /var/folders/s6/5jq8fd4x12v9blzt68qbv18m0000gn/T/scan-build-2019-04-18-141047-45509-1</span><br><span class="line">    RUN_CLANG_STATIC_ANALYZER = YES</span><br><span class="line"></span><br><span class="line">note: Using new build system</span><br><span class="line">note: Planning build</span><br><span class="line">note: Constructing build description</span><br><span class="line">Build system information</span><br><span class="line">error: Signing <span class="keyword">for</span> <span class="string">"asdasd"</span> requires a development team. Select a development team <span class="keyword">in</span> the project editor. (<span class="keyword">in</span> target <span class="string">'asdasd'</span>)</span><br><span class="line"></span><br><span class="line">** BUILD FAILED **</span><br><span class="line"></span><br><span class="line">scan-build: Removing directory <span class="string">'/var/folders/s6/5jq8fd4x12v9blzt68qbv18m0000gn/T/scan-build-2019-04-18-141047-45509-1'</span> because it contains no reports.</span><br><span class="line">scan-build: No bugs found.</span><br></pre></td></tr></table></figure><p>关于静态分析更多可以查看 ：<a href="http://clang-analyzer.llvm.org.cn/" target="_blank" rel="noopener">Clang 静态分析器</a></p><!-- https://juejin.im/post/5a30ea0ff265da43094526f9 --><h3 id="生成代码和优化阶段"><a href="#生成代码和优化阶段" class="headerlink" title="生成代码和优化阶段"></a>生成代码和优化阶段</h3><p><strong><em>一、生成 LLVM 代码</em></strong></p><p>clang 完成代码的标记，解析和分析后，接着就会生成 LLVM 代码。下面继续看看hello.c：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"hello world\n"</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>要把这段代码编译成 LLVM 字节码（绝大多数情况下是二进制码格式），我们可以执行下面的命令：</p><p><code>clang  -emit-llvm hello.c -c -o hello.bc</code></p><p>接着用另一个命令来查看刚刚生成的二进制文件：</p><p><code>llvm-dis &lt; hello.bc | less</code></p><p>部分输出如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">; ModuleID = '&lt;stdin&gt;'</span><br><span class="line">source_filename = <span class="string">"hello.c"</span></span><br><span class="line">target datalayout = <span class="string">"e-m:o-i64:64-f80:128-n8:16:32:64-S128"</span></span><br><span class="line">target triple = <span class="string">"x86_64-apple-macosx10.13.0"</span></span><br><span class="line"></span><br><span class="line">@.str = <span class="keyword">private</span> unnamed_addr constant [<span class="number">13</span> x i8] c<span class="string">"hello world\0A\00"</span>, align <span class="number">1</span></span><br><span class="line"></span><br><span class="line">; Function Attrs: noinline nounwind optnone ssp uwtable</span><br><span class="line">define i32 @main() #<span class="number">0</span> &#123;</span><br><span class="line">  %<span class="number">1</span> = alloca i32, align <span class="number">4</span></span><br><span class="line">  store i32 <span class="number">0</span>, i32* %<span class="number">1</span>, align <span class="number">4</span></span><br><span class="line">  %<span class="number">2</span> = call i32 (i8*, ...) @<span class="built_in">printf</span>(i8* getelementptr inbounds ([<span class="number">13</span> x i8], [<span class="number">13</span> x i8]* @.str, i32 <span class="number">0</span>, i32 <span class="number">0</span>))</span><br><span class="line">  ret i32 <span class="number">0</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">declare i32 @<span class="built_in">printf</span>(i8*, ...) #<span class="number">1</span></span><br><span class="line"></span><br><span class="line">...</span><br></pre></td></tr></table></figure><p><strong><em>二、优化 LLVM 代码</em></strong></p><p>LLVM 会去做些优化工作，在 Xcode 的编译设置里也可以设置优化级别-01，-03，-0s，还可以写些自己的 Pass，Pass就是LLVM系统转化和优化的工作的一个节点，每个节点做一些工作，这些工作加起来就构成了LLVM整个系统的优化和转化。官方有比较完整的 Pass 教程： <a href="https://releases.llvm.org/5.0.2/docs/WritingAnLLVMPass.html" target="_blank" rel="noopener">Writing an LLVM Pass — LLVM 5 documentation</a> 。如果开启了 bitcode 苹果会做进一步的优化</p><p><img src="/res/LLVM/xcode-optimize.png" alt=""></p><p><strong><em>三、输出汇编代码</em></strong></p><p>我们可以使用下面的命令让clang输出汇编代码：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#输入</span></span><br><span class="line"> clang -S -o - hello.c</span><br></pre></td></tr></table></figure><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#输出</span></span><br><span class="line">.section__TEXT,__text,regular,pure_instructions <span class="comment"># .section 指令指定接下来会执行哪一个段</span></span><br><span class="line">.macosx_version_min 10, 13</span><br><span class="line">.globl_main           <span class="comment"># .globl 指令说明 _main 是一个外部符号</span></span><br><span class="line">.p2align4, 0x90     <span class="comment"># .align 指令指出了后面代码的对齐方式 如果需要的话，用 0x90 补齐</span></span><br><span class="line">_main:                                  <span class="comment">## 接下来是 main 函数的头部：</span></span><br><span class="line">.cfi_startproc          <span class="comment"># .cfi_startproc 指令通常用于函数的开始处</span></span><br><span class="line"><span class="comment">## %bb.0:</span></span><br><span class="line">pushq%rbp    <span class="comment"># rbp 寄存器 (基础指针寄存器 base pointer register)</span></span><br><span class="line">.cfi_def_cfa_offset 16</span><br><span class="line">.cfi_offset %rbp, -16</span><br><span class="line">movq%rsp, %rbp</span><br><span class="line">.cfi_def_cfa_register %rbp</span><br><span class="line">subq<span class="variable">$16</span>, %rsp</span><br><span class="line">movl<span class="variable">$0</span>, -4(%rbp)</span><br><span class="line">leaqL_.str(%rip), %rdi</span><br><span class="line">movb<span class="variable">$0</span>, %al</span><br><span class="line">callq_printf <span class="comment">#调用了 printf</span></span><br><span class="line">xorl%ecx, %ecx</span><br><span class="line">movl%eax, -8(%rbp)          <span class="comment">## 4-byte Spill</span></span><br><span class="line">movl%ecx, %eax</span><br><span class="line">addq<span class="variable">$16</span>, %rsp</span><br><span class="line">popq%rbp <span class="comment"># 与pushq对应</span></span><br><span class="line">retq</span><br><span class="line">.cfi_endproc <span class="comment"># 与 .cfi_startproc 相匹配，以此标记出 main() 函数结束</span></span><br><span class="line">                                        <span class="comment">## -- End function</span></span><br><span class="line">.section__TEXT,__cstring,cstring_literals <span class="comment"># __TEXT __cstring 开启了一个新的段。</span></span><br><span class="line">L_.str:       <span class="comment">#L_.str 标记运行在实际的代码中获取到字符串的一个指针                          ## @.str</span></span><br><span class="line">.asciz<span class="string">"hello world\n"</span> <span class="comment">#.asciz 指令告诉编译器输出一个以 ‘\0’ (null) 结尾的字符串。</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">.subsections_via_symbolsv <span class="comment"># .subsections_via_symbols 指令是静态链接编辑器使用的。</span></span><br></pre></td></tr></table></figure><p>具体的代码解读，有兴趣的同学可以到<a href="https://developer.apple.com/library/archive/documentation/DeveloperTools/Reference/Assembler/000-Introduction/introduction.html" target="_blank" rel="noopener">苹果的 OS X Assembler Reference </a> 了解详细。</p><!-- https://objccn.io/issue-6-3/ --><h3 id="汇编器"><a href="#汇编器" class="headerlink" title="汇编器"></a>汇编器</h3><p>汇编器将可读的汇编代码转换为机器代码。它会创建一个目标对象文件，一般简称为 对象文件。这些文件以 .o 结尾。如果用 Xcode 构建应用程序，可以在工程的 derived data 目录中，Objects-normal 文件夹下找到这些文件。</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#例如 生成 main.o文件</span></span><br><span class="line">clang -fmodules -c main.c -o main.o</span><br></pre></td></tr></table></figure><h3 id="链接器"><a href="#链接器" class="headerlink" title="链接器"></a>链接器</h3><p>链接器解决了目标文件和库之间的链接。例如上面汇编代码中 <code>callq   _printf</code>， <code>printf()</code> 是 libc 库中的一个函数。无论怎样，最后的可执行文件需要能需要知道 printf() 在内存中的具体位置：例如，_printf 的地址符号是什么。链接器会读取所有的目标文件 (此处只有一个) 和库 (此处是 libc)，并解决所有未知符号 (此处是 _printf) 的问题。然后将它们编码进最后的可执行文件中 （可以在 libc 中找到符号 _printf），接着链接器会输出可以运行的执行文件。</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#生成 hello.out 的 Mach-O 二进制文件</span></span><br><span class="line">clang hello.c -o hello.out</span><br></pre></td></tr></table></figure><p>关于<a href="https://developer.apple.com/library/archive/documentation/Performance/Conceptual/CodeFootprint/Articles/MachOOverview.html" target="_blank" rel="noopener">Mach-O文件格式</a>有兴趣的同学可以通过apple文档来了解。</p><p>这里说 hello.out 是可执行文件，可以通过<code>file</code>来判断:</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">file hello.out</span><br><span class="line"><span class="comment">#输出</span></span><br><span class="line">hello.out: Mach-O 64-bit executable x86_64</span><br><span class="line"></span><br><span class="line"><span class="comment">#执行hello.out</span></span><br><span class="line">./hello.out</span><br><span class="line"><span class="comment">#输出 </span></span><br><span class="line">hello world</span><br></pre></td></tr></table></figure><h2 id="编译多个文件"><a href="#编译多个文件" class="headerlink" title="编译多个文件"></a>编译多个文件</h2><p>之前上面我们的实验都是使用单个hello.c文件。现在我们通过多个文件情况 了解下汇编器和链接器 ,如下三个文件：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//File1.h:</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#import <span class="meta-string">&lt;Foundation/Foundation.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Foo</span> : <span class="title">NSObject</span></span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)run;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//File1.m:</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#import <span class="meta-string">"File1.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">Foo</span></span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)run</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"%@"</span>, <span class="built_in">NSFullUserName</span>());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//File2.m:</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#import <span class="meta-string">"File1.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> main(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">@autoreleasepool</span> &#123;</span><br><span class="line">        Foo *foo = [[Foo alloc] init];</span><br><span class="line">        [foo run];</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面有三个文件，需要我们编译多个文件。我们需要让clang对输入每个文件生成对应的目标文件：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># clang  -c Only run preprocess, compile, and assemble steps</span></span><br><span class="line"></span><br><span class="line">clang -c File1.m <span class="comment">#生成 File1.o</span></span><br><span class="line">clang -c File2.m <span class="comment">#生成 File2.o</span></span><br></pre></td></tr></table></figure><p>这里我们加了<code>-c</code> 并没有编译头文件，现在我们来完成<code>链接器</code>这一步,将两个 .o 文件与Foundation库链接起来：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#  -Wl,&lt;arg&gt;               Pass the comma separated arguments in &lt;arg&gt; to the linker</span></span><br><span class="line">clang File1.o File2.o -Wl,`xcrun --show-sdk-path`/System/Library/Frameworks/Foundation.framework/Foundation</span><br><span class="line"><span class="comment"># 输出 a.out</span></span><br></pre></td></tr></table></figure><p>现在我们可以运行 <code>a.out</code> 了。</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">./a.out </span><br><span class="line">2019-04-16 20:49:35.070 a.out[71207:10299638] sunTongSheng</span><br></pre></td></tr></table></figure><h3 id="符号表和链接"><a href="#符号表和链接" class="headerlink" title="符号表和链接"></a>符号表和链接</h3><p>File1 和 File2 都使用了 Foundation framework。 File2.o 目标文件使用了它的 autorelease pool，并间接的使用了 libobjc.dylib 中的 Objective-C 运行时。它需要运行时函数来进行消息的调用。所有的这些关联的东西都被形象的称之为符号。</p><p>每个函数、全局变量和类等都是通过符号的形式来定义和使用的。当我们将目标文件链接为一个可执行文件时，链接器在目标文件和动态库之间对符号做了解析处理。</p><p>可执行文件和目标文件有一个符号表，这个符号表规定了它们的符号。如果我们用 nm 工具观察一下 File2.o 目标文件，可以看到如下内容：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#nm : llvm symbol table dumper</span></span><br><span class="line">nm -nm File2.o</span><br><span class="line">                 (undefined) external _OBJC_CLASS_<span class="variable">$_Foo</span></span><br><span class="line">                 (undefined) external _objc_alloc</span><br><span class="line">                 (undefined) external _objc_autoreleasePoolPop</span><br><span class="line">                 (undefined) external _objc_autoreleasePoolPush</span><br><span class="line">                 (undefined) external _objc_msgSend</span><br><span class="line">0000000000000000 (__TEXT,__text) external _main</span><br></pre></td></tr></table></figure><p>上面就是那个目标文件的所有符号。_OBJC_CLASS_$_Foo 是 Foo Objective-C 类的符号。该符号是 undefined, external 。External 的意思是指对于这个目标文件该类并不是私有的，相反，non-external 的符号则表示对于目标文件是私有的。我们的 File2.o 目标文件引用了类 Foo，不过这并没有实现它。因此符号表中将其标示为 undefined。接着是 4 个 Objective-C 运行时函数。它们同样是 undefined的，需要链接器进行符号解析。</p><p>接下来是 _main 符号，它是表示 main() 函数，同样为 external，这是因为该函数需要被调用，所以应该为可见的。由于在 helloworld.o 文件中实现了 这个 main 函数。这个函数地址位于 0处，并且需要转入到 <strong>TEXT,</strong>text section。</p><p>接下来看下 <code>File1.o</code>,看看有什么输出：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">nm -nm File1.o</span><br><span class="line">                 (undefined) external _NSFullUserName</span><br><span class="line">                 (undefined) external _NSLog</span><br><span class="line">                 (undefined) external _OBJC_CLASS_<span class="variable">$_NSObject</span></span><br><span class="line">                 (undefined) external _OBJC_METACLASS_<span class="variable">$_NSObject</span></span><br><span class="line">                 (undefined) external ___CFConstantStringClassReference</span><br><span class="line">                 (undefined) external __objc_empty_cache</span><br><span class="line">0000000000000000 (__TEXT,__text) non-external -[Foo run]</span><br><span class="line">0000000000000060 (__DATA,__objc_const) non-external l_OBJC_METACLASS_RO_<span class="variable">$_Foo</span></span><br><span class="line">00000000000000a8 (__DATA,__objc_const) non-external l_OBJC_<span class="variable">$_INSTANCE_METHODS_Foo</span></span><br><span class="line">00000000000000c8 (__DATA,__objc_const) non-external l_OBJC_CLASS_RO_<span class="variable">$_Foo</span></span><br><span class="line">0000000000000110 (__DATA,__objc_data) external _OBJC_METACLASS_<span class="variable">$_Foo</span></span><br><span class="line">0000000000000138 (__DATA,__objc_data) external _OBJC_CLASS_<span class="variable">$_Foo</span></span><br></pre></td></tr></table></figure><p>File1.o 同样有 undefined 的符号。首先是使用了符号 <code>NSFullUserName()</code>，<code>NSLog()</code>和 <code>NSObject</code>。接着显示了 <code>_OBJC_CLASS_$_Foo</code> 已经定义了，并且对于 File1.o 是一个外部符号 , <code>File1.o</code> 包含了这个类的实现。</p><p>最后先来同样看下 a.out 的输出：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">nm -nm a.out </span><br><span class="line">                 (undefined) external _NSFullUserName (from Foundation)</span><br><span class="line">                 (undefined) external _NSLog (from Foundation)</span><br><span class="line">                 (undefined) external _OBJC_CLASS_<span class="variable">$_NSObject</span> (from CoreFoundation)</span><br><span class="line">                 (undefined) external _OBJC_METACLASS_<span class="variable">$_NSObject</span> (from CoreFoundation)</span><br><span class="line">                 (undefined) external ___CFConstantStringClassReference (from CoreFoundation)</span><br><span class="line">                 (undefined) external __objc_empty_cache (from libobjc)</span><br><span class="line">                 (undefined) external _objc_alloc (from libobjc)</span><br><span class="line">                 (undefined) external _objc_autoreleasePoolPop (from libobjc)</span><br><span class="line">                 (undefined) external _objc_autoreleasePoolPush (from libobjc)</span><br><span class="line">                 (undefined) external _objc_msgSend (from libobjc)</span><br><span class="line">                 (undefined) external dyld_stub_binder (from libSystem)</span><br><span class="line">0000000100000000 (__TEXT,__text) [referenced dynamically] external __mh_execute_header</span><br><span class="line">0000000100000e90 (__TEXT,__text) non-external -[Foo run]</span><br><span class="line">0000000100000ec0 (__TEXT,__text) external _main</span><br><span class="line">0000000100001138 (__DATA,__objc_data) external _OBJC_METACLASS_<span class="variable">$_Foo</span></span><br><span class="line">0000000100001160 (__DATA,__objc_data) external _OBJC_CLASS_<span class="variable">$_Foo</span></span><br></pre></td></tr></table></figure><p>通过<code>a.out</code>的符号表，我们可以观察链接器是如何解析所有符号表的。当我们将这两个目标文件和 Foundation framework (是一个动态库) 进行链接处理时，链接器会尝试解析所有的 undefined 符号。它可以解析 _OBJC_CLASS_$_Foo。另外，它将使用 Foundation framework。当链接器通过动态库 (此处是 Foundation framework) 解析成功一个符号时，它会在最终的链接图中记录这个符号是通过动态库进行解析的。链接器会记录输出文件是依赖于哪个动态链接库，并连同其路径一起进行记录。在我们的例子中，_NSFullUserName，_NSLog，_OBJC_CLASS_$_NSObject，_objc_autoreleasePoolPop 等符号都是遵循这个过程。</p><p>虽然所有的 Foundation 和 Objective-C 运行时符号依旧是 undefined，不过现在的符号表中已经多了如何解析它们的信息，例如在哪个动态库中可以找到对应的符号。</p><p>可执行文件同样知道去哪里找到所需库：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">otool -L a.out </span><br><span class="line">a.out:</span><br><span class="line">/System/Library/Frameworks/Foundation.framework/Versions/C/Foundation (compatibility version 300.0.0, current version 1560.12.0)</span><br><span class="line">/usr/lib/libSystem.B.dylib (compatibility version 1.0.0, current version 1252.50.4)</span><br><span class="line">/System/Library/Frameworks/CoreFoundation.framework/Versions/A/CoreFoundation (compatibility version 150.0.0, current version 1454.90.0)</span><br><span class="line">/usr/lib/libobjc.A.dylib (compatibility version 1.0.0, current version 228.0.0)</span><br></pre></td></tr></table></figure><h3 id="动态链接器"><a href="#动态链接器" class="headerlink" title="动态链接器"></a>动态链接器</h3><p>在运行时，动态链接器dyld可以解析这些 <code>undefined</code> 符号，dyld将会确定好 <code>_NSFullUserName</code> 等符号，并指向它们在 <code>Foundation</code> 中的实现等。<br>上面提到文件符号表指向了需要的库，添加<code>DYLD_PRINT_LIBRARIES</code>环境变量可以打印出什么库被加载了：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">(<span class="built_in">export</span> DYLD_PRINT_LIBRARIES=; ./a.out )</span><br><span class="line">dyld: loaded: /Users/suntongsheng/Desktop/tmp/asdasd/Test/./a.out</span><br><span class="line">dyld: loaded: /System/Library/Frameworks/Foundation.framework/Versions/C/Foundation</span><br><span class="line">dyld: loaded: /usr/lib/libSystem.B.dylib</span><br><span class="line">dyld: loaded: /System/Library/Frameworks/CoreFoundation.framework/Versions/A/CoreFoundation</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>比如可以针对 Foundation 运行 <code>nm</code>，并检查这些符号的定义情况：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nm -nm /System/Library/Frameworks/Foundation.framework/Foundation | grep NSFullUserName</span><br><span class="line">00000000000bb1be (__TEXT,__text) external _NSFullUserName</span><br></pre></td></tr></table></figure><p>上面将会显示出在加载 Foundation 时，同时会加载的 70 个动态库。这是由于 Foundation 依赖于另外一些动态库。运行下面的命令：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">xcrun otool -L /System/Library/Frameworks/Foundation.framework/Foundation</span><br></pre></td></tr></table></figure><p>可以看到 Foundation 关联的库。</p><p><strong><em>动态链接器dyld 的共享缓存</em></strong></p><p>当你构建一个真正的程序时，将会链接各种各样的库。它们又会依赖其他一些 framework 和 动态库。需要加载的动态库会非常多。而对于相互依赖的符号就更多了。可能将会有上千个符号需要解析处理，这将花费很长的时间：一般是好几秒钟。</p><p>为了缩短这个处理过程所花费时间，在 OS X 和 iOS 上的动态链接器使用了共享缓存，共享缓存存于 /var/db/dyld/。对于每一种架构，操作系统都有一个单独的文件，文件中包含了绝大多数的动态库，这些库都已经链接为一个文件，并且已经处理好了它们之间的符号关系。当加载一个 Mach-O 文件 (一个可执行文件或者一个库) 时，动态链接器首先会检查 共享缓存 看看是否存在其中，如果存在，那么就直接从共享缓存中拿出来使用。每一个进程都把这个共享缓存映射到了自己的地址空间中。这个方法大大优化了 OS X 和 iOS 上程序的启动时间。</p><h2 id="最后总结一下就是以下编译过程："><a href="#最后总结一下就是以下编译过程：" class="headerlink" title="最后总结一下就是以下编译过程："></a>最后总结一下就是以下编译过程：</h2><p><img src="/res/LLVM/llvm1.png" alt=""></p><h2 id="Xcode-编译"><a href="#Xcode-编译" class="headerlink" title="Xcode 编译"></a>Xcode 编译</h2><p>以上说的是Clang如何编译C语言文件的过程，那么在Xcode里会经过哪些过程呢？</p><p>我们可以简单新建一个单页面工程，Build后在Report Navigation视图中查看详细日志：</p><p><img src="/res/LLVM/xcode.png" alt=""></p><p>详细的步骤如下：</p><ol><li>创建文件夹</li><li>把Entitlements.plist写入到DerivedData里，处理打包的时候需要的信息（比如application-identifier）。</li><li>创建一些辅助文件，比如各种.hmap (headermap是帮助编译器找到头文件的辅助文件：存储这头文件到其物理路径的映射关系)</li><li>编译.m文件，生成.o文件。</li><li>链接动态库，o文件，生成一个mach o格式的可执行文件。</li><li>编译assets，编译storyboard，链接storyboard</li><li>对App签名</li><li>生成 .app</li></ol><p>在 build 处理过程中，每个任务都会出现类似上面的这些 log 信息，我们就通过上面的 log 信息进一步了解详情。</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;编译器&quot;&gt;&lt;a href=&quot;#编译器&quot; class=&quot;headerlink&quot; title=&quot;编译器&quot;&gt;&lt;/a&gt;编译器&lt;/h2&gt;&lt;blockquote&gt;
&lt;p&gt;把一种编程语言(原始语言)转换为另一种编程语言(目标语言)的程序叫做编译器&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;大多数编译器由两部分组成：前端和后端。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;前端负责词法分析，语法分析，生成中间代码；&lt;/li&gt;
&lt;li&gt;后端以中间代码作为输入，进行行架构无关的代码优化，接着针对不同架构生成不同的机器码。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Objective C/C/C++使用的编译器前端是clang，swift是swift，后端都是LLVM。&lt;/p&gt;
&lt;h3 id=&quot;llvm介绍&quot;&gt;&lt;a href=&quot;#llvm介绍&quot; class=&quot;headerlink&quot; title=&quot;llvm介绍&quot;&gt;&lt;/a&gt;llvm介绍&lt;/h3&gt;&lt;p&gt;LLVM是什么，是low level virtual machine的简称，其实是一个编译器框架。LLVM 的优点主要得益于它的三层式架构 – 第一层支持多种语言作为输入(例如 C, ObjectiveC, C++ 和 Haskell)，第二层是一个共享式的优化器(对 LLVM IR 做优化处理)，第三层是许多不同的目标平台(例如 Intel, ARM 和 PowerPC)。编译器前端主要进行语法分析，语义分析，生成中间代码。编译器后端会进行机器无关的代码优化，生成机器语言，并且进行机器相关的代码优化，根据不同的系统架构生成不同的机器码。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/res/LLVM/LLVMCompiler1.png&quot; alt=&quot;&quot;&gt;&lt;/p&gt;
&lt;p&gt;现在，Xcode 的默认编译器是 clang。本文中我们提到的编译器都表示 clang。clang 的功能是首先对 Objective-C 代码做分析检查，然后将其转换为低级的类汇编代码：LLVM Intermediate Representation(LLVM 中间表达码)。接着 LLVM 会执行相关指令将 LLVM IR 编译成目标平台上的本地字节码，这个过程的完成方式可以是即时编译 (Just-in-time)，或在编译的时候完成。而Clang 是一个C、C++、Objective-C和Objective-C++编程语言的编译器前端。它采用了底层虚拟机（LLVM）作为其后端。它的目标是提供一个GNU编译器套装（GCC）的替代品。Clang项目包括Clang前端和Clang静态分析器等。&lt;/p&gt;
&lt;p&gt;下图是iOS编译过程：&lt;br&gt;&lt;img src=&quot;/res/LLVM/iOSCompiler.png&quot; alt=&quot;&quot;&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="重学iOS" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/"/>
    
    
      <category term="iOS" scheme="https://developerdoc.com/tags/iOS/"/>
    
      <category term="LLVM" scheme="https://developerdoc.com/tags/LLVM/"/>
    
  </entry>
  
  <entry>
    <title>LLDB教程-命令篇</title>
    <link href="https://developerdoc.com/essay/LLDB/LLDB_tutorial/"/>
    <id>https://developerdoc.com/essay/LLDB/LLDB_tutorial/</id>
    <published>2019-03-14T12:53:17.000Z</published>
    <updated>2019-04-23T02:18:02.429Z</updated>
    
    <content type="html"><![CDATA[<p>你是否曾经苦恼于理解项目的代码，而去尝试打印一个变量的值？然后使用NSLog 并且每次必须重新编译，从头开始？但是不一定要这么做。你可以使用调试器。而且即使你已经知道如何使用调试器检查变量，它可以做的还有很多。</p><h2 id="LLDB-是什么？"><a href="#LLDB-是什么？" class="headerlink" title="LLDB 是什么？"></a>LLDB 是什么？</h2><p><a href="http://lldb.llvm.org/" target="_blank" rel="noopener">LLDB</a>是Mac OS X上Xcode的默认调试器，支持再桌面和iOS设备和模拟器上调试C ，Objective-C和C++。它是新一代高性能调试器，它可以高效利用LLVM项目中的现有库，例如Clang表达式解析器和LLVM反汇编程序。</p><p>随着Xcode 5的发布，LLDB调试器已经取代了GDB，成为了Xcode工程中默认的调试器。它与LLVM编译器一起，带给我们更丰富的流程控制和数据检测的调试功能。LLDB为Xcode提供了底层调试环境，其中包括内嵌在Xcode IDE中的位于调试区域的控制面板。</p><p><a href="https://github.com/facebook/chisel" target="_blank" rel="noopener">Chisel</a> 是facebook下一个开源LLDB命令集合。</p><p>与此同时，让我们以在调试器中打印变量来开始我们的旅程吧。</p><a id="more"></a><h2 id="基础命令"><a href="#基础命令" class="headerlink" title="基础命令"></a>基础命令</h2><p><img src="/res/LLDB/1.png" alt=""></p><p>这是一个简单加了断点的程序，程序会在这一行停止运行，并且控制台会被打开，允许我们和调试器交互。这时候我们应该打些什么命令呢？</p><p><strong><em>帮助 help</em></strong></p><p>最简单命令是 <code>help</code>，它会列举出所有的命令。如果你忘记了一个命令是做什么的，或者想知道更多的话，你可以通过 <code>help &lt;command-name&gt;</code> 来了解更多细节，例如 <code>help print</code> 或者 <code>help thread</code>。只需要在控制台 上图lldb字样的地方键入 help即可。</p><p><img src="/res/LLDB/2.png" alt=""></p><p><strong><em>打印对象 print</em></strong><br>打印值很简单；只要试试 print 命令:</p><p><img src="/res/LLDB/3.png" alt=""></p><p>LLDB 实际上会作前缀匹配。所以你也可以使用 prin，pri，或者 p。但你不能使用 pr，因为 LLDB 不能消除和 process 的歧义 (幸运的是 p 并没有歧义)。而print 则是<code>expression --</code>的简写方式。</p><p>你可能还注意到了，结果中有个 $0。实际上你可以使用它来指向这个结果。试试 print $0 + 7，你会看到 130。任何以$符开头的东西都是存在于 LLDB 的命名空间的，它们是为了帮助你进行调试而存在的。</p><p>打印复杂对象时，print可能显得力不从心 ，我们想看的是对象的 description 方法的结果，这时可以使用 <code>po</code> ，po 其实是 <code>e -o --</code>的别名。</p><p>甚至可以给print 指定不同的打印格式。它们都是以 <code>print/&lt;fmt&gt;</code> 或者简化的 <code>p/&lt;fmt&gt;</code> 格式书写。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//默认的格式</span></span><br><span class="line">(lldb) p <span class="number">16</span></span><br><span class="line"><span class="number">16</span></span><br><span class="line"><span class="comment">//十六进制:</span></span><br><span class="line">(lldb) p/x <span class="number">16</span></span><br><span class="line"><span class="number">0x10</span></span><br><span class="line"><span class="comment">//二进制 (t 代表 two)：</span></span><br><span class="line">(lldb) p/t <span class="number">16</span></span><br><span class="line"><span class="number">0</span>b00000000000000000000000000010000</span><br><span class="line">(lldb) p/t (<span class="keyword">char</span>)<span class="number">16</span></span><br><span class="line"><span class="number">0</span>b00010000</span><br></pre></td></tr></table></figure><p><a href="https://sourceware.org/gdb/onlinedocs/gdb/Output-Formats.html" target="_blank" rel="noopener">这里是格式的完整清单</a></p><p><strong><em>修改对象 expression</em></strong></p><p>如果想改变一个值怎么办？我们要用到的是 <code>expression</code> 这个方便的命令。</p><p><img src="/res/LLDB/4.png" alt=""></p><p>上图中修改了num的值，断点步进后可以看到NSLog的对应值已经发生了变化。</p><h2 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h2><p>现在你已经可以打印对象和简单类型，并且知道如何使用 expression 命令在调试器中修改它们了。现在让我们使用一些变量来减少输入量。就像你可以在 C 语言中用 int a = 0 来声明一个变量一样，你也可以在 LLDB 中做同样的事情。不过为了能使用声明的变量，变量必须以$符开头。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">(lldb) e <span class="keyword">int</span> $a = <span class="number">2</span></span><br><span class="line">(lldb) p $a * <span class="number">19</span></span><br><span class="line"><span class="number">38</span></span><br><span class="line">(lldb) e <span class="built_in">NSArray</span> *$array = @[ <span class="string">@"Saturday"</span>, <span class="string">@"Sunday"</span>, <span class="string">@"Monday"</span> ]</span><br><span class="line">(lldb) p [$array count]</span><br><span class="line"><span class="number">2</span></span><br><span class="line">(lldb) po [[$array objectAtIndex:<span class="number">0</span>] uppercaseString]</span><br><span class="line">SATURDAY</span><br><span class="line">(lldb) p (<span class="keyword">char</span>)[[$array objectAtIndex:$a] characterAtIndex:<span class="number">0</span>]</span><br><span class="line"><span class="string">'M'</span></span><br><span class="line">(lldb) p/d (<span class="keyword">char</span>)[[$array objectAtIndex:$a] characterAtIndex:<span class="number">0</span>]</span><br><span class="line"><span class="number">77</span></span><br></pre></td></tr></table></figure><h3 id="UI调试"><a href="#UI调试" class="headerlink" title="UI调试"></a>UI调试</h3><p>因为全局变量是可访问的,可以像这样打印整个视图层级：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">(lldb) po [[[UIApplication sharedApplication] keyWindow] recursiveDescription]</span><br><span class="line">&lt;UIWindow: 0x7fe5ac70c6a0; frame = (0 0; 375 812); gestureRecognizers = &lt;NSArray: 0x6000034cb780&gt;; layer = &lt;UIWindowLayer: 0x600003a84680&gt;&gt;</span><br><span class="line">   | &lt;UIView: 0x7fe5ac5069b0; frame = (0 0; 375 812); autoresize = W+H; layer = &lt;CALayer: 0x600003ad3620&gt;&gt;</span><br></pre></td></tr></table></figure><p><strong><em>1、更新UI</em></strong></p><p>就像上文变量中提到那样，我们可以拿到这个view:</p><p><code>(lldb) expression id $myView = (id)0x7fe5ac5069b0</code></p><p>尝试做一些修改：</p><p><code>(lldb) expression (void)[$myView setBackgroundColor:[UIColor redColor]]</code></p><p>但是只有程序继续运行之后才会看到界面的变化。因为改变的内容必须被发送到渲染服务中，然后显示才会被更新。</p><p>渲染服务实际上是一个另外的进程 (被称作 backboardd)。这就是说即使我们正在调试的内容所在的进程被打断了，backboardd 也还是继续运行着的。</p><p>这意味着你可以运行下面的命令，而不用继续运行程序：</p><p><code>(lldb) expression (void)[CATransaction flush]</code></p><p>这个时候就能看到背景颜色的改变了。</p><h2 id="流程控制"><a href="#流程控制" class="headerlink" title="流程控制"></a>流程控制</h2><p>通过xcode加断点调试时，调试条上回出现四个可以控制程序执行流程的按钮：<br><img src="/res/LLDB/5.png" alt=""></p><p>从左到右分别是 continue program execution 、 step over 、 step into 和 step out。</p><p><strong><em>1、 continue program execution</em></strong> 按钮，会取消程序的暂停，允许程序正常执行 (要么一直执行下去，要么到达下一个断点)。<br>在 LLDB 中，你可以使用 <code>process continue</code> 或者 <code>thread continue</code> 命令来达到同样的效果。</p><p><strong><em>2、 step over</em></strong> 按钮，会以黑盒的方式执行一行代码。如果所在这行代码是一个函数调用，那么就不会跳进这个函数，而是会执行这个函数，然后继续。<br>LLDB 则可以使用 <code>thread step-over</code>，<code>next</code>，或者 <code>n</code> 命令。</p><p><strong><em>3、 step in</em></strong>按钮，可以跳进一个函数调用来调试或者检查程序的执行情况。<br>在LLDB中使用 <code>thread step-in</code>，<code>step</code>，或者 s 命令。注意，当前行不是函数调用时，next 和 step 效果是一样的。</p><p><strong><em>4、step out</em></strong>按钮 ，如果你曾经不小心跳进一个函数，但实际上你想跳过它，常见的反应是重复的运行 n 直到函数返回。其实这种情况，step out 按钮是你的救世主。它会继续执行到下一个返回语句 (直到一个堆栈帧结束) 然后再次停止。<br>在LLDB中使用 <code>thread step-out</code>，</p><p><strong><em>thread return</em></strong> 使用<code>help thread</code>可以看到这个比较实用的函数。它有一个可选参数，在执行时它会把可选参数加载进返回寄存器里，然后立刻执行返回命令，跳出当前栈帧。这意味这函数剩余的部分不会被执行。这会给 ARC 的引用计数造成一些问题，或者会使函数内的清理部分失效。但是在函数的开头执行这个命令，是个非常好的隔离这个函数，伪造返回值的方式 。</p><h2 id="断点"><a href="#断点" class="headerlink" title="断点"></a>断点</h2><p>Xcode在断点导航中提供了一系列工具创建和管理断点，我们可以来看LLDB中等价的命令,主要是<code>breakpoint</code>命令。</p><p><strong><em>1、查看 启用/禁用</em></strong></p><p><img src="/res/LLDB/6.png" alt=""></p><p>上图是xcode查看断点的地方，点击断点会开启或关闭断点。对应的LLDB如下：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//查看断点 命令输出列表显示每个逻辑断点都有一个整数标识</span></span><br><span class="line"><span class="comment">//输出列表中另一个信息是断点位置是否是已解析的(resolved)。这个标识表示当与之相关的文件地址被加载到程序进行调试时，其位置是已解析的。</span></span><br><span class="line">  <span class="comment">//例如，如果在共享库中设置的断点之后被卸载了，则断点的位置还会保留，但其不能再被解析。</span></span><br><span class="line">(lldb) breakpoint list</span><br><span class="line">Current breakpoints:</span><br><span class="line">1: file = '/Users/suntongsheng/Desktop/tmp/asdasd/asdasd/main.m', line = 23, exact_match = 0, locations = 1, resolved = 1, hit count = 1</span><br><span class="line"></span><br><span class="line">  <span class="number">1.1</span>: where = asdasd`main + <span class="number">51</span> at main.m:<span class="number">23</span>, address = <span class="number">0x00000001070195d3</span>, resolved, hit count = <span class="number">1</span> </span><br><span class="line"><span class="comment">//禁用断点</span></span><br><span class="line">(lldb) breakpoint disable <span class="number">1</span></span><br><span class="line"><span class="number">1</span> breakpoints disabled.</span><br><span class="line"><span class="comment">//启用断点</span></span><br><span class="line">(lldb) breakpoint enable <span class="number">1</span></span><br><span class="line"><span class="number">1</span> breakpoints enabled.</span><br></pre></td></tr></table></figure><p><strong><em>2、 创建/删除</em></strong></p><p>在Xcode创建断点的方式一种是 直接在代码左边的行数出点击 即可创建断点。对应的LLDB如下：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//在main.m的第24行创建断点</span></span><br><span class="line">(lldb) breakpoint set -f main.m -l <span class="number">24</span></span><br><span class="line">Breakpoint <span class="number">3</span>: where = asdasd`main + <span class="number">59</span> at main.m:<span class="number">24</span>, address = <span class="number">0x00000001070195db</span></span><br><span class="line"><span class="comment">//删除刚才的断点</span></span><br><span class="line">(lldb) breakpoint delete <span class="number">3</span></span><br><span class="line"><span class="number">1</span> breakpoints deleted; <span class="number">0</span> breakpoint locations disabled.</span><br></pre></td></tr></table></figure><p>还有一种是在断点导航，点击左下角的加号按钮，选择Symbolic BreakPoint会出现：</p><p><img src="/res/LLDB/7.png" alt=""></p><p>对应的LLDB如下：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//在一个符号 (C 语言函数) 上创建断点，而完全不用指定哪一行</span></span><br><span class="line">(lldb) breakpoint set  -F isEven</span><br><span class="line">Breakpoint <span class="number">6</span>: where = asdasd`isEven + <span class="number">16</span> at main.m:<span class="number">13</span>, address = <span class="number">0x0000000107019750</span></span><br><span class="line"><span class="comment">//Objective-C 的方法也完全可以</span></span><br><span class="line">(lldb) breakpoint set -F <span class="string">"-[NSArray objectAtIndex:]"</span></span><br><span class="line">Breakpoint <span class="number">5</span>: where = CoreFoundation`-[<span class="built_in">NSArray</span> objectAtIndex:], address = <span class="number">0x000000010ac7a950</span></span><br></pre></td></tr></table></figure><p>在 <code>[NSArray objectAtIndex:]</code>这个断点上，我们怎么能知道设置了什么呢？接下来我们可以用$arg1、$arg2等命令来打印出我们想要的信息。<br>在这里$arg1是指对象本身，$arg2是对象被调用的函数，po命令无法直接输出函数名，需要加上(SEL)，$arg3是被赋给函数的参数。</p><p><strong><em>3、 断点行为</em></strong></p><p>在Xcode中邮件断点可以编辑添加action信息，你可以添加多个行为：</p><p><img src="/res/LLDB/8.png" alt=""></p><p>对应的LLDB如下：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">(lldb) breakpoint set -f main.m -l <span class="number">31</span></span><br><span class="line">Breakpoint <span class="number">2</span>: where = asdasd`main + <span class="number">205</span> at main.m:<span class="number">31</span>, address = <span class="number">0x000000010ab2166d</span></span><br><span class="line"><span class="comment">//添加条件</span></span><br><span class="line">(lldb) breakpoint modify -c 'i == 80' 2</span><br><span class="line"><span class="comment">//添加行为</span></span><br><span class="line">(lldb) breakpoint command add <span class="number">2</span></span><br><span class="line">Enter your debugger command(s).  Type 'DONE' to end.</span><br><span class="line">&gt; po i</span><br><span class="line">&gt; DONE</span><br><span class="line"><span class="comment">//显示刚才的断点</span></span><br><span class="line">(lldb) breakpoint list <span class="number">2</span></span><br><span class="line">2: file = 'main.m', line = 31, exact_match = 0, locations = 1, resolved = 1, hit count = 0</span><br><span class="line">    Breakpoint commands:</span><br><span class="line">      po i</span><br><span class="line"></span><br><span class="line">Condition: i == <span class="number">80</span></span><br><span class="line"></span><br><span class="line">  <span class="number">2.1</span>: where = asdasd`main + <span class="number">205</span> at main.m:<span class="number">31</span>, address = <span class="number">0x000000010ab2166d</span>, resolved, hit count = <span class="number">0</span></span><br></pre></td></tr></table></figure><p>执行断点后自动继续运行，允许你完全通过断点来修改程序！你可以在某一行停止，运行一个 expression 命令来改变变量，然后继续运行。</p><h2 id="查看-线程-调用栈-状态"><a href="#查看-线程-调用栈-状态" class="headerlink" title="查看 线程/调用栈 状态"></a>查看 线程/调用栈 状态</h2><p>在进程停止后，LLDB会选择一个当前线程和线程中当前帧(frame)。很多检测状态的命令可以用于这个线程或帧。</p><p>为了检测进程的当前状态，可以从以下命令<code>thread list</code>开始：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">(lldb) thread list</span><br><span class="line">Process <span class="number">48541</span> stopped</span><br><span class="line">* thread <span class="meta">#1: tid = 0x3bd1d2, 0x000000010eae0b66 libsystem_kernel.dylib`__pthread_kill + 10, queue = 'com.apple.main-thread', stop reason = signal SIGABRT</span></span><br><span class="line">  thread <span class="meta">#2: tid = 0x3bd24c, 0x000000010eae128a libsystem_kernel.dylib`__workq_kernreturn + 10</span></span><br></pre></td></tr></table></figure><p>星号(*)表示thread #1为当前线程。为了获取线程的跟踪栈，可以使用以下命令<code>thread backtrace</code>：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//默认为当前线程 也可以指定线程 : thread backtrace 2</span></span><br><span class="line">(lldb) thread backtrace</span><br><span class="line">thread <span class="meta">#1: tid = 0x2c03, stop reason = breakpoint 1.1, queue = com.apple.main-thread</span></span><br><span class="line"> frame <span class="meta">#0: 0x0000000100010d5b, where = Sketch`-[SKTGraphicView alignLeftEdges:] + 33 at /Projects/Sketch/SKTGraphicView.m:1405</span></span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>如果想查看所有线程的调用栈，则可以使用以下命令：<code>(lldb) thread backtrace all</code></p><p>检查帧参数和本地变量的最简便的方式是使用<code>frame variable</code>命令：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">(<span class="keyword">int</span>) argc = <span class="number">1</span></span><br><span class="line">(<span class="keyword">char</span> **) argv = <span class="number">0x00007ffee4150ff0</span></span><br><span class="line">(<span class="built_in">NSUInteger</span>) num = <span class="number">123</span></span><br><span class="line">(__NSCFConstantString *) str = <span class="number">0x000000010bab00d8</span> <span class="string">@"learning LLDB"</span></span><br><span class="line">(__NSArrayI *) arr = <span class="number">0x0000600002982fa0</span> <span class="string">@"2 elements"</span></span><br><span class="line">(<span class="keyword">int</span>) i = <span class="number">80</span></span><br><span class="line">(<span class="built_in">BOOL</span>) result0 = <span class="literal">YES</span></span><br><span class="line">(<span class="built_in">BOOL</span>) result1 = <span class="literal">NO</span></span><br></pre></td></tr></table></figure><p>如果没有指定任何变量名，则会显示所有参数和本地变量。如果指定参数名或变量名，则只打印指定的值。如：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(lldb) frame variable <span class="keyword">self</span></span><br><span class="line">(<span class="built_in">SKTGraphicView</span> *) <span class="keyword">self</span> = <span class="number">0x0000000100208b40</span></span><br></pre></td></tr></table></figure><p>如果想查看另外一帧，可以使用<code>frame select</code>命令，如下所示：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(lldb) frame select <span class="number">2</span></span><br><span class="line">frame <span class="meta">#2: 0x000000010e88dc45 libsystem_c.dylib`abort + 127</span></span><br></pre></td></tr></table></figure><h2 id="image"><a href="#image" class="headerlink" title="image"></a>image</h2><p>image指令是target module指令的缩写，借助它我们能够查看当前的Binary Images相关的信息。日常开发我们主要利用它寻址。<code>image</code>命令的用法也挺多，首先可以用它来查看工程中使用的库，如下所示：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">(lldb) image list</span><br><span class="line">[  <span class="number">0</span>] <span class="number">9E11</span>F0C7<span class="number">-9</span>AB1<span class="number">-36</span>A8<span class="number">-8</span>FE4<span class="number">-8821</span>DBA05A2F <span class="number">0x000000010baad000</span> /Users/suntongsheng/Library/Developer/Xcode/DerivedData/asdasd-dufvhftdhjomdkcnrilgxlwykarv/Build/Products/Debug-iphonesimulator/asdasd.app/asdasd </span><br><span class="line">[  <span class="number">1</span>] <span class="number">8</span>A72DE9C-A136<span class="number">-3506</span>-AA02<span class="number">-4</span>BA2B82DCAF3 <span class="number">0x0000000115aa8000</span> /usr/lib/dyld</span><br></pre></td></tr></table></figure><p>我们还可以用它来查找可执行文件或共享库的原始地址，这一点还是很有用的，当我们的程序崩溃时，我们可以使用这条命令来查找崩溃所在的具体位置，如下所示：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">NSArray</span> *array = @[@<span class="number">1</span>, @<span class="number">2</span>];</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"array 3: %@"</span>, array[<span class="number">2</span>]);</span><br><span class="line"><span class="comment">//这段代码会抛出</span></span><br><span class="line">2019-03-19 16:10:32.841204+0800 asdasd[49351:4010863] *** Terminating app due to uncaught exception 'NSRangeException', reason: '*** -[__NSArrayI objectAtIndexedSubscript:]: index 2 beyond bounds [0 .. 1]'</span><br><span class="line">*** First throw call stack:</span><br><span class="line">(</span><br><span class="line"><span class="number">0</span>   CoreFoundation                      <span class="number">0x0000000102c7f1bb</span> __exceptionPreprocess + <span class="number">331</span></span><br><span class="line"><span class="number">1</span>   libobjc.A.dylib                     <span class="number">0x000000010221d735</span> objc_exception_throw + <span class="number">48</span></span><br><span class="line"><span class="number">2</span>   CoreFoundation                      <span class="number">0x0000000102bcb4ec</span> _CFThrowFormattedException + <span class="number">194</span></span><br><span class="line"><span class="number">3</span>   CoreFoundation                      <span class="number">0x0000000102d01b00</span> +[__NSArrayI allocWithZone:] + <span class="number">0</span></span><br><span class="line"><span class="number">4</span>   asdasd                              <span class="number">0x00000001019004ff</span> -[ViewController viewDidLoad] + <span class="number">287</span></span><br></pre></td></tr></table></figure><p>根据以上信息，我们可以判断崩溃位置是在ViewController中，要想知道具体在哪一行，可以使用以下命令<code>image lookup --address</code>：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">(lldb) image lookup --address <span class="number">0x00000001019004ff</span></span><br><span class="line">      Address: asdasd[<span class="number">0x00000001000014ff</span>] (asdasd.__TEXT.__text + <span class="number">287</span>)</span><br><span class="line">      Summary: asdasd`-[ViewController viewDidLoad] + <span class="number">287</span> at ViewController.m:<span class="number">23</span></span><br></pre></td></tr></table></figure><p>可以看到，最后定位到了ViewController.m:23行，正是我们代码所在的位置。</p><p>image更多用法可以参考： <a href="https://developer.apple.com/library/archive/documentation/IDEs/Conceptual/gdb_to_lldb_transition_guide/document/lldb-command-examples.html#//apple_ref/doc/uid/TP40012917-CH3-SW5" target="_blank" rel="noopener">Executable and Shared Library Query Commands。</a></p><!-- http://southpeak.github.io/2015/01/25/tool-lldb/ --><!-- https://juejin.im/post/5b1cd870e51d4506dc0ac76c --><!-- https://www.jianshu.com/p/67f08a4d8cf2 -->]]></content>
    
    <summary type="html">
    
      &lt;p&gt;你是否曾经苦恼于理解项目的代码，而去尝试打印一个变量的值？然后使用NSLog 并且每次必须重新编译，从头开始？但是不一定要这么做。你可以使用调试器。而且即使你已经知道如何使用调试器检查变量，它可以做的还有很多。&lt;/p&gt;
&lt;h2 id=&quot;LLDB-是什么？&quot;&gt;&lt;a href=&quot;#LLDB-是什么？&quot; class=&quot;headerlink&quot; title=&quot;LLDB 是什么？&quot;&gt;&lt;/a&gt;LLDB 是什么？&lt;/h2&gt;&lt;p&gt;&lt;a href=&quot;http://lldb.llvm.org/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;LLDB&lt;/a&gt;是Mac OS X上Xcode的默认调试器，支持再桌面和iOS设备和模拟器上调试C ，Objective-C和C++。它是新一代高性能调试器，它可以高效利用LLVM项目中的现有库，例如Clang表达式解析器和LLVM反汇编程序。&lt;/p&gt;
&lt;p&gt;随着Xcode 5的发布，LLDB调试器已经取代了GDB，成为了Xcode工程中默认的调试器。它与LLVM编译器一起，带给我们更丰富的流程控制和数据检测的调试功能。LLDB为Xcode提供了底层调试环境，其中包括内嵌在Xcode IDE中的位于调试区域的控制面板。&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/facebook/chisel&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Chisel&lt;/a&gt; 是facebook下一个开源LLDB命令集合。&lt;/p&gt;
&lt;p&gt;与此同时，让我们以在调试器中打印变量来开始我们的旅程吧。&lt;/p&gt;
    
    </summary>
    
      <category term="重学iOS" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/"/>
    
    
      <category term="iOS" scheme="https://developerdoc.com/tags/iOS/"/>
    
      <category term="LLDB" scheme="https://developerdoc.com/tags/LLDB/"/>
    
  </entry>
  
  <entry>
    <title>iOS直播教程</title>
    <link href="https://developerdoc.com/quick-start/iOS%E7%9B%B4%E6%92%AD%E6%95%99%E7%A8%8B/"/>
    <id>https://developerdoc.com/quick-start/iOS直播教程/</id>
    <published>2019-03-07T07:09:58.000Z</published>
    <updated>2019-03-07T08:06:11.330Z</updated>
    
    <content type="html"><![CDATA[<h2 id="直播原理"><a href="#直播原理" class="headerlink" title="直播原理"></a>直播原理</h2><p>一个简单的直播流程是: </p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">app        -&gt; 视频采集   -&gt;  编码    -&gt; 推流     -&gt; 服务器</span><br><span class="line">服务器      -&gt; 拉流      -&gt;  解码    -&gt; 播放     -&gt; app</span><br></pre></td></tr></table></figure><p>其中涉及到的技术点比较多:</p><p><img src="/res/live/直播技术点.png" alt=""></p><a id="more"></a><h3 id="流媒体传输协议"><a href="#流媒体传输协议" class="headerlink" title="流媒体传输协议"></a>流媒体传输协议</h3><h4 id="RTMP"><a href="#RTMP" class="headerlink" title="RTMP"></a>RTMP</h4><ul><li>RTMP:实时消息传输协议,Adobe Systems公司为Flash播放器和服务器之间音频、视频和数据传输开发的开放协议，因为是开放协议所以都可以使用了。<ul><li>RTMP协议用于对象、视频、音频的传输。<ul><li>这个协议建立在TCP协议或者轮询HTTP协议之上。</li><li>RTMP协议就像一个用来装数据包的容器，这些数据可以是FLV中的视音频数据。一个单一的连接可以通过不同的通道传输多路网络流，这些通道中的包都是按照固定大小的包传输的</li></ul></li></ul></li></ul><h4 id="HLS-HTTP-Live-Streaming"><a href="#HLS-HTTP-Live-Streaming" class="headerlink" title="HLS (HTTP Live Streaming)"></a>HLS (HTTP Live Streaming)</h4><p>HTTP Live Streaming（HLS）是苹果公司(Apple Inc.)实现的基于HTTP的流媒体传输协议，可实现流媒体的直播和点播，主要应用在iOS系统，为iOS设备（如iPhone、iPad）提供音视频直播和点播方案。HLS点播，基本上就是常见的分段HTTP点播，不同在于，它的分段非常小。</p><p>相对于常见的流媒体直播协议，例如RTMP协议等，HLS直播最大的不同在于，直播客户端获取到的，并不是一个完整的数据流。HLS协议在服务器端将直播数据流存储为连续的、很短时长的媒体文件（MPEG-TS格式），而客户端则不断的下载并播放这些小文件，因为服务器端总是会将最新的直播数据生成新的小文件，这样客户端只要不停的按顺序播放从服务器获取到的文件，就实现了直播。由此可见，基本上可以认为，HLS是以点播的技术方式来实现直播。由于数据通过HTTP协议传输，所以完全不用考虑防火墙或者代理的问题，而且分段文件的时长很短，客户端可以很快的选择和切换码率，以适应不同带宽条件下的播放。不过HLS的这种技术特点，决定了它的延迟一般总是会高于普通的流媒体直播协议。</p><p>m3u8，是HTTP Live Streaming直播的索引文件。</p><ul><li><a href="https://developer.apple.com/library/content/documentation/NetworkingInternet/Conceptual/StreamingMediaGuide/Introduction/Introduction.html#//apple_ref/doc/uid/TP40008332-CH1-SW1" target="_blank" rel="noopener">Apple: HTTP Live Streaming Overview </a></li><li><a href="https://developer.apple.com/streaming/" target="_blank" rel="noopener">Apple: HTTP Live Streaming(HLS) Overview</a></li></ul><h2 id="直播iOS实战"><a href="#直播iOS实战" class="headerlink" title="直播iOS实战"></a>直播iOS实战</h2><h3 id="服务端配置"><a href="#服务端配置" class="headerlink" title="服务端配置"></a>服务端配置</h3><h4 id="安装-nginx"><a href="#安装-nginx" class="headerlink" title="安装 nginx"></a>安装 nginx</h4><blockquote><p>使用HomeBrew 安装 nginx<br>没安装过HomwBrew的 看一下 <a href="https://brew.sh/" target="_blank" rel="noopener">HomeBrew主页</a> 安装一下</p></blockquote><p><strong>一：</strong> <em>增加home-brew对nginx的扩展</em></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew tap homebrew/nginx</span><br></pre></td></tr></table></figure><p><strong>二：</strong> <em>安装Nginx服务器和rtmp模块</em></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install nginx-full --with-rtmp-module</span><br></pre></td></tr></table></figure><p><strong>三：</strong> <em>查看是否安装成功</em></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew info nginx-full</span><br></pre></td></tr></table></figure><p><img src="/res/live/brewInfoNginx.png" alt="">    </p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">/usr/local/etc/nginx/nginx.conf （配置文件路径）</span><br><span class="line">/usr/local/var/www （服务器默认路径）</span><br><span class="line">/usr/local/Cellar/ （安装路径）</span><br><span class="line">open -t /usr/local/etc/nginx/nginx.conf 可以用文本编辑器打开配置文件</span><br><span class="line">nginx -s reload 重新加载配置</span><br><span class="line">nginx -s reopen 重新打开log</span><br><span class="line">nginx -s stop 停止nginx</span><br><span class="line">nginx -s quit 退出nginx</span><br></pre></td></tr></table></figure><p><strong>四：</strong> <em>启动nginx</em></p><p>输入 开启服务器</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nginx</span><br></pre></td></tr></table></figure><p>在浏览器中 输入 <a href="http://localhost:8080" target="_blank" rel="noopener">http://localhost:8080</a><br>如果出现下图, 则表示安装成功:</p><p><img src="/res/live/welcomeNginx.png" alt=""></p><h4 id="配置-nginx"><a href="#配置-nginx" class="headerlink" title="配置 nginx"></a>配置 nginx</h4><p><strong>一：</strong> <em>配置Nginx，支持http协议拉流</em></p><p>在终端输入</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">open -t  /usr/local/etc/nginx/nginx.conf</span><br></pre></td></tr></table></figure><p>打开nginx配置文件,在http属性下找到：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">location / &#123;</span><br><span class="line">    root   html;</span><br><span class="line">    index  index.html index.htm;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在下面添加，成为：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">location / &#123;</span><br><span class="line">root   html;</span><br><span class="line">index  index.html index.htm;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">location /hls &#123; </span><br><span class="line">#支持http协议拉流</span><br><span class="line">types &#123; </span><br><span class="line">application/vnd.apple.mpegurl m3u8; </span><br><span class="line">video/mp2t ts; </span><br><span class="line">&#125; </span><br><span class="line">root /usr/local/var/www; </span><br><span class="line">add_header Cache-Control no-cache; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>二：</strong> <em>配置Nginx，支持rtmp协议推流</em></p><p>在终端输入<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">open -t  /usr/local/etc/nginx/nginx.conf</span><br></pre></td></tr></table></figure></p><p>打开nginx配置文件, 文件的末尾(最后一个 “}” 下一行 )添加：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">rtmp &#123;</span><br><span class="line">    server &#123;</span><br><span class="line">        listen 1990;</span><br><span class="line">        </span><br><span class="line">        application hls&#123;</span><br><span class="line"></span><br><span class="line">          live on;</span><br><span class="line">          record off;</span><br><span class="line">          hls on;</span><br><span class="line">          hls_path /usr/local/var/www/hls;</span><br><span class="line">          hls_fragment 1s;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>三：</strong> 应用配置</p><p>重启下nginx就行 </p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nginx -s reload</span><br></pre></td></tr></table></figure><h4 id="使用-ffmepg-测试推流"><a href="#使用-ffmepg-测试推流" class="headerlink" title="使用 ffmepg 测试推流"></a>使用 ffmepg 测试推流</h4><h3 id="在iOS端使用-LFLiveKit-推流"><a href="#在iOS端使用-LFLiveKit-推流" class="headerlink" title="在iOS端使用 LFLiveKit 推流"></a>在iOS端使用 LFLiveKit 推流</h3><blockquote><p>LFLiveKit它已经帮我们实现了视频采集、后台录制、美颜功能、支持h264、AAC编码，动态改变速率，RTMP传输等<br><a href="https://github.com/LaiFengiOS/LFLiveKit" target="_blank" rel="noopener">LFLiveKit主页</a></p></blockquote><h4 id="使用LFLiveKit-demo-推流"><a href="#使用LFLiveKit-demo-推流" class="headerlink" title="使用LFLiveKit demo 推流"></a>使用LFLiveKit demo 推流</h4><p>下载源代码后，进入sample/LFLiveKitDemo 目录，运行下<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pod install</span><br></pre></td></tr></table></figure></p><p>打开LFLiveKitDemo.xcworkspace 工程</p><p>将<strong>LFLivePreview.m</strong>文件的362行改为：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">stream.url = <span class="string">@"rtmp://10.45.33.96:1990/hls/abc"</span>;</span><br></pre></td></tr></table></figure><p>url对应的是 rtmp://[nginx服务器地址] : [端口] / [配置的应用] / 随便写</p><p>然后真机运行效果如下:</p><p><img src="/res/live/LFLiveKitPreview.png" width="50%"></p><h4 id="测试LFLiveKit-demo-推流"><a href="#测试LFLiveKit-demo-推流" class="headerlink" title="测试LFLiveKit demo 推流"></a>测试LFLiveKit demo 推流</h4><blockquote><p>保证nginx服务器是开启状态<br>点击上面demo中的开始直播后：</p></blockquote><p>在mac <strong>Safari</strong> 浏览器中输入 <a href="http://localhost:8080/hls/abc.m3u8" target="_blank" rel="noopener">http://localhost:8080/hls/abc.m3u8</a> 查看效果：</p><p><img src="/res/live/LFLiveKitTest.png" width="80%"></p><p>相对应的，直接使用iOS自带的AVPlayer就可以播放该链接。</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;直播原理&quot;&gt;&lt;a href=&quot;#直播原理&quot; class=&quot;headerlink&quot; title=&quot;直播原理&quot;&gt;&lt;/a&gt;直播原理&lt;/h2&gt;&lt;p&gt;一个简单的直播流程是: &lt;/p&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;app        -&amp;gt; 视频采集   -&amp;gt;  编码    -&amp;gt; 推流     -&amp;gt; 服务器&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;服务器      -&amp;gt; 拉流      -&amp;gt;  解码    -&amp;gt; 播放     -&amp;gt; app&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;其中涉及到的技术点比较多:&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/res/live/直播技术点.png&quot; alt=&quot;&quot;&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="quick-start" scheme="https://developerdoc.com/categories/quick-start/"/>
    
    
      <category term="iOS" scheme="https://developerdoc.com/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>GitHub精选分享第四期</title>
    <link href="https://developerdoc.com/weekly-share/GitHub%E7%B2%BE%E9%80%89%E5%88%86%E4%BA%AB%E7%AC%AC%E5%9B%9B%E6%9C%9F/"/>
    <id>https://developerdoc.com/weekly-share/GitHub精选分享第四期/</id>
    <published>2019-03-07T02:14:42.000Z</published>
    <updated>2019-03-07T04:35:56.638Z</updated>
    
    <content type="html"><![CDATA[<p>记录这周看到值得分享的事，本期就分享点图片压缩工具类吧~</p><p><img src="/res/weeklyshare/image-tool-stack.png" alt="image-tool-stack"></p><p>上图是我的图片处理工具栈，矢量原型 Sketch，位图编辑 Pixelmator，搜集管理 Inboard，压缩优化 JPEGmini、ImageAlpha、ImageOptim。前四个是付费 App，后两个自由开源。</p><p>本次主要分享<strong>ImageOptim</strong> <strong>ImageAlpha</strong> <strong>ImageOptim-CLI</strong> 和 <strong>Squoosh</strong><br><a id="more"></a></p><h3 id="ImageOptim-JPG图片压缩利器"><a href="#ImageOptim-JPG图片压缩利器" class="headerlink" title="ImageOptim JPG图片压缩利器"></a><a href="https://github.com/ImageOptim/ImageOptim" target="_blank" rel="noopener">ImageOptim JPG图片压缩利器</a></h3><blockquote class="blockquote-center"><p><a href="https://imageoptim.com/mac" target="_blank" rel="noopener">ImageOptim下载链接</a><br>如果你是Mac OS X电脑的使用者，也可试试看另外这一套图片减肥、最佳化工具ImageOptim，它可以支持PNG，JPEG跟GIF等格式的图档，通过内置的PNGOUT、AdvPNG、Pngcrush、OptiPNG、JpegOptim、jpegrescan、jpegtran与Gifsicle等图片最佳化工具，帮我们全自动压缩、缩减图片的大小。</p><p>ImageOptim的使用方法也很简单，就是把图片拉到ImageOptim窗口中，它会自动执行最佳化，然后就没了。</p></blockquote><h3 id="ImageAlpha-PNG图片有损压缩"><a href="#ImageAlpha-PNG图片有损压缩" class="headerlink" title="ImageAlpha PNG图片有损压缩"></a><a href="https://pngmini.com/" target="_blank" rel="noopener">ImageAlpha PNG图片有损压缩</a></h3><blockquote class="blockquote-center"><p><a href="https://pngmini.com/" target="_blank" rel="noopener">ImageAlpha 下载链接</a><br>通过应用有损压缩和转换为更高效的PNG8 + alpha格式，mageAlpha大大减少了24位PNG文件的文件大小（包括alpha透明度）。这些图像与iOS，所有浏览器兼容，甚至在IE6中降级。</p><p>通过使用最新的pngquant和pngnq-s9以及Alpha通道感知后处理器，ImageAlpha可以在MacromediaAdobe Fireworks中获得比类似功能更好的质量。</p></blockquote><h3 id="ImageOptim-CLI-图片压缩工具调用"><a href="#ImageOptim-CLI-图片压缩工具调用" class="headerlink" title="ImageOptim-CLI 图片压缩工具调用"></a><a href="https://github.com/JamieMason/ImageOptim-CLI" target="_blank" rel="noopener">ImageOptim-CLI 图片压缩工具调用</a></h3><p>今天处理的图片较多，想能不能有个自动工作流来解放双手。一搜，恰巧找到了 <a href="https://github.com/JamieMason/ImageOptim-CLI" target="_blank" rel="noopener">JamieMason/ImageOptim-CLI</a> 这个 macOS 项目。一句话描述就是，它会根据你的指定，选择性调用 JPEGmini、ImageAlpha、ImageOptim 等工具，来处理图片压缩问题。开始敲一行命令，中间过程全自动。</p><p>79% TypeScript + 21% AppleScript，分发为可执行的二进制包，所以不依赖 Node.js，并且支持 Homebrew 安装。</p><p>使用也非常方便，终端输入 imageoptim –help 即可得到说明，只需注意三个事项：第一、终端要取得 Accessibility 权限；第二、调用的 App 要自己另外下载安装；第三、顾名思义，它默认调用 ImageOptim，如果不用，可通过 –no-imageoptim 参数 disable 它。</p><p>支持的调用的 3 个 App 如下，JPEGmini Lite 免费，标准和 Pro 均需付费：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Supported Apps:</span><br><span class="line"></span><br><span class="line">  ImageAlpha: https://pngmini.com</span><br><span class="line">  ImageOptim: https://imageoptim.com</span><br><span class="line">  JPEGmini Lite: https://itunes.apple.com/us/app/jpegmini-lite/id525742250</span><br><span class="line">  JPEGmini Pro: https://itunes.apple.com/us/app/jpegmini-pro/id887163276</span><br><span class="line">  JPEGmini: https://itunes.apple.com/us/app/jpegmini/id498944723</span><br></pre></td></tr></table></figure><p>如需组合其他工具，添加对应参数即可，比如添加 –jpegmini 参数调用 JPEGmini：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">Examples:</span><br><span class="line"></span><br><span class="line">  # Run ImageOptim.app over every image in current directory</span><br><span class="line">  imageoptim</span><br><span class="line"></span><br><span class="line">  # Run ImageAlpha.app and ImageOptim.app over every PNG in current directory</span><br><span class="line">  imageoptim --imagealpha &apos;**/*.png&apos;</span><br><span class="line"></span><br><span class="line">  # Run JPEGmini.app and ImageOptim.app over every JPG in current directory</span><br><span class="line">  imageoptim --jpegmini &apos;**/*.jpg&apos; &apos;**/*.jpeg&apos;</span><br><span class="line"></span><br><span class="line">  # Run JPEGmini.app over every JPG in current directory</span><br><span class="line">  imageoptim --jpegmini --no-imageoptim &apos;**/*.jpg&apos; &apos;**/*.jpeg&apos;</span><br><span class="line"></span><br><span class="line">  # Run ImageOptim.app over every image in a specific directory</span><br><span class="line">  imageoptim &apos;~/Desktop&apos;</span><br></pre></td></tr></table></figure><p>那么，这几个工具是单干还是组合好呢，哪个搭配压缩和质量最合理呢？针对这个问题，ImageOptim-CLI 贴心地给出了 GIF、JPEG、PNG 各个格式各个工具及组合的<a href="https://jamiemason.github.io/ImageOptim-CLI/comparison/all/photoshop/desc/" target="_blank" rel="noopener">压缩效果对比</a></p><p><img src="/res/weeklyshare/imageoptim-cli-comparison.png" alt="imageoptim-cli-comparison"></p><p>先看 JPEG 项目，果不其然，JPEGmini &amp; ImageOptim 组合几无敌手，只在蓝天白云和 bril 人像上输给了 Kraken，当然，这个成绩主要是 JPEGmini 的功劳。</p><p>再看 PNG 项目，悬念只在 ImageOptim 单干以及与 ImageOptim 搭配两个方案之间。考虑到 24 位色的天际线、雀斑脸、蓝天白云等图片上，ImageOptim 单干弱势明显，还是建议二者无脑搭配。ImageAlpha则又是明显。</p><p>综合以上两个项目来看，如果能接受较高的图片质量损耗（注意表格中无底色的红字），Kraken 也是非常有竞争力的单一优化工具。</p><h3 id="Squoosh-是-Google-推出的一款在线简单高效的图片压缩工具"><a href="#Squoosh-是-Google-推出的一款在线简单高效的图片压缩工具" class="headerlink" title="Squoosh 是 Google 推出的一款在线简单高效的图片压缩工具"></a><a href="https://github.com/GoogleChromeLabs/squoosh" target="_blank" rel="noopener">Squoosh 是 Google 推出的一款在线简单高效的图片压缩工具</a></h3><blockquote class="blockquote-center"><p><a href="https://squoosh.app/" target="_blank" rel="noopener">Squoosh在线链接</a><br>Squoosh 是 Google 推出的一款在线简单高效的图片压缩工具，支持 JPG、PNG 和 WebP 格式，可让您深入了解各种图像压缩器提供的高级选项</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;记录这周看到值得分享的事，本期就分享点图片压缩工具类吧~&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/res/weeklyshare/image-tool-stack.png&quot; alt=&quot;image-tool-stack&quot;&gt;&lt;/p&gt;
&lt;p&gt;上图是我的图片处理工具栈，矢量原型 Sketch，位图编辑 Pixelmator，搜集管理 Inboard，压缩优化 JPEGmini、ImageAlpha、ImageOptim。前四个是付费 App，后两个自由开源。&lt;/p&gt;
&lt;p&gt;本次主要分享&lt;strong&gt;ImageOptim&lt;/strong&gt; &lt;strong&gt;ImageAlpha&lt;/strong&gt; &lt;strong&gt;ImageOptim-CLI&lt;/strong&gt; 和 &lt;strong&gt;Squoosh&lt;/strong&gt;&lt;br&gt;
    
    </summary>
    
      <category term="每周分享" scheme="https://developerdoc.com/categories/%E6%AF%8F%E5%91%A8%E5%88%86%E4%BA%AB/"/>
    
    
  </entry>
  
  <entry>
    <title>Instruments实践-解决卡顿 使你的界面保持流畅</title>
    <link href="https://developerdoc.com/essay/Instruments/Instruments3/"/>
    <id>https://developerdoc.com/essay/Instruments/Instruments3/</id>
    <published>2019-03-06T03:36:24.000Z</published>
    <updated>2019-03-07T08:25:39.609Z</updated>
    
    <content type="html"><![CDATA[<p>结合Instrument CoreAnimation分析影响性能的因素： <strong>图层混合</strong>,<strong>光栅化</strong>,<strong>颜色格式</strong>,<strong>图片大小</strong>,<strong>离屏渲染</strong></p><p>在使用UIKit的过程中，性能优化是永恒的话题。很多人都看过分析优化滑动性能的文章，但其中不少文章只介绍了优化方法却对背后的原理避而不谈，或者是晦涩难懂而且读者缺乏实践体验的机会。不妨思考一下下面的问题自己是否有一个清晰的认识：</p><ol><li>为什么要把控件尽量设置成不透明的，如果是透明的会有什么影响，如何检测这种影响？</li><li>为什么cell中的图片，尽可能要使用正确的大小、格式，如果错误会有什么影响，如何检测这种影响？</li><li>为什么设置阴影和圆角有可能影响滑动时流畅度？</li><li>shouldRasterize和离屏渲染的关系是什么，何时应该使用？</li></ol><a id="more"></a><p><img src="/res/Instrument/9.png" width="80%"></p><p>打开CoreAnimation可以看到主体的PFS数据和右边的Debug Options。</p><p>我们知道任何屏幕总是有一个刷新率，比如iphone推荐的刷新率是60Hz，也就是说GPU每秒钟刷新屏幕60次，因此两次刷新之间的间隔为16.67ms。这段时间内屏幕内容保持不变，称为一帧(frame)，fps表示frames per second，也就是每秒钟显示多少帧画面。对于静止不变的内容，我们不需要考虑它的刷新率，但在执行动画或滑动时，fps的值直接反映出滑动的流畅程度。</p><h2 id="图层混合"><a href="#图层混合" class="headerlink" title="图层混合"></a>图层混合</h2><p>首先我们要明白像素的概念，屏幕上每一个点都是一个像素，像素有R、G、B三种颜色构成(有时候还带有alpha值)。如果某一块区域上覆盖了多个layer,最后的显示效果受到这些layer的共同影响。举个例子，上层是蓝色(RGB=0,0,1),透明度为50%，下层是红色(RGB=1,0,0)。那么最终的显示效果是紫色(RGB=0.5,0,0.5)。这种颜色的混合(blending)需要消耗一定的GPU资源，因为实际上可能不止只有两层。如果只想显示最上层的蓝色，可以把它的透明度设置为100%，这样GPU会忽略下面所有的layer，从而节约了很多不必要的运算。</p><p>第一个调试选项”Color Blended Layers”正是用于检测哪里发生了图层混合，并用红色标记出来。因此我们需要尽可能减少看到的红色区域。一旦发现应该想法设法消除它。</p><p><img src="/res/Instrument/15.png" width="40%"></p><p>解决办法如下：</p><ol><li>透明度设置为100%<ul><li>如果某一块区域上覆盖了多个layer,最后的显示效果受到这些layer的共同影响。举个例子，上层是蓝色(RGB=0,0,1),透明度为50%，下层是红色(RGB=1,0,0)。那么最终的显示效果是紫色(RGB=0.5,0,0.5)。这种颜色的混合(blending)需要消耗一定的GPU资源，因为实际上可能不止只有两层。如果只想显示最上层的蓝色，可以把它的透明度设置为100%，这样GPU会忽略下面所有的layer，从而节约了很多不必要的运算。</li></ul></li><li>UIImageView的图片也不能含有alpha通道<ul><li>图像自身的性质可能会对结果有影响</li></ul></li><li>设置 backgroundColor 属性（如果不设置背景颜色，控件依然会被认为是透明的）<ul><li>虽然在白色背景下，无法肉眼看到效果，但重新调试后我们可以发现label的红色消失了。也正是因为对背景颜色的不重视，它成了影响滑动性能的第一个杀手。</li></ul></li><li>如果label文字有中文，依然会出现图层混合，这是因为此时label多了一个sublayer</li></ol><h2 id="光栅化"><a href="#光栅化" class="headerlink" title="光栅化"></a>光栅化</h2><p>第二个调试选项是<strong>“Color Hits Green and Misses Red</strong>”，它表示如果命中缓存则显示为绿色，否则显示为红色，显然绿色越多越好，红色越少越好。勾选这个选项后我们看到如下的场景：</p><p><img src="/res/Instrument/16.png" width="40%"></p><p>光栅化是将一个layer预先渲染成位图(bitmap)，然后加入缓存中。如果对于阴影效果这样比较消耗资源的静态内容进行缓存，可以得到一定幅度的性能提升</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//这一行代码表示将label的layer光栅化：</span></span><br><span class="line">label.layer.shouldRasterize = <span class="literal">true</span></span><br></pre></td></tr></table></figure><p>因为layer进行光栅化后渲染成位图放在缓存中。缓存中的对象有效期只有100ms，即如果在0.1s内没有被使用就会自动从缓存中清理出去。光栅化的缓存机制是一把双刃剑，先写入缓存再读取有可能消耗较多的时间。因此光栅化仅适用于较复杂的、静态的效果。 光栅化会导致离屏渲染</p><p>所以我们做的第二个优化是注释掉下面这行代码：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//    label.layer.shouldRasterize = true</span></span><br></pre></td></tr></table></figure><h2 id="颜色格式"><a href="#颜色格式" class="headerlink" title="颜色格式"></a>颜色格式</h2><p>像素在内存中的布局和它在磁盘中的存储方式并不相同。考虑一种简单的情况：每个像素有R、G、B和alpha四个值，每个值占用1字节，因此每个像素占用4字节的内存空间。一张1920*1080的照片(iPhone6 Plus的分辨率)一共有2,073,600个像素，因此占用了超过8Mb的内存。但是一张同样分辨率的PNG格式或JPEG格式的图片一般情况下不会有这么大。这是因为JPEG将像素数据进行了一种非常复杂且可逆的转化。</p><p>当我们打开JPEG格式的图片时，CPU会进行一系列运算，将JPEG图片解压成像素数据。显然这个工作会消耗不少时间，所以不应该在滑动时进行，我们应该预先处理好图片。借用WWDC上的一页PPT来说明：</p><p><img src="/res/Instrument/t_UIKit性能优化22.png" alt=""></p><p>Commit Transaction和Decode在同一帧内进行，如果这两个操作的耗时超过16.67s，Draw Calls就会延迟到下一帧，从而导致fps值的降低。下面是Commit Transaction的详细流程：</p><p><img src="/res/Instrument/t_UIKit性能优化23.png" alt=""></p><p>在第三步的Prepare中，CPU主要处理两件事：</p><ol><li>把图片从PNG或JPEG等格式中解压出来，得到像素数据</li><li>如果GPU不支持这种颜色各式，CPU需要进行格式转换</li></ol><p>使用core animation 的 <strong>Color Copied Images</strong>检测图片格式,确保图片颜色格式能被GPU支持</p><h2 id="图片大小"><a href="#图片大小" class="headerlink" title="图片大小"></a>图片大小</h2><p>使用core animation 的第五个选项“<strong>Color Misaligned Images”</strong>。它表示如果图片需要缩放则标记为黄色，如果没有像素对齐则标记为紫色。勾选上这个选项并进行调试，可以看到如下场景：</p><p><img src="/res/Instrument/17.png" width="40%"></p><p>图片如果需要缩放会占用一定时间，确保图片大小与frame一致，不要在滑动的时候缩放图片</p><h2 id="离屏渲染"><a href="#离屏渲染" class="headerlink" title="离屏渲染"></a>离屏渲染</h2><p>开始调试并勾选“<strong>Color Offscreen-Rendered Yellow”</strong>，会看到这样的场景：</p><p><img src="/res/Instrument/20.png" width="40%"></p><p>这表示产生了离屏渲染。离屏渲染表示渲染发生在屏幕之外，你可能认为这是一句废话。为了真正解释清楚什么是离屏渲染，我们先来看一下正常的渲染通道(Render-Pass)：</p><p><img src="/res/Instrument/18.png"> </p><p>首先，OpenGL提交一个命令到Command Buffer，随后GPU开始渲染，渲染结果放到Render Buffer中，这是正常的渲染流程。但是有一些复杂的效果无法直接渲染出结果，它需要分步渲染最后再组合起来，比如添加一个蒙版(mask)：</p><p><img src="/res/Instrument/19.png"> </p><p>在前两个渲染通道中，GPU分别得到了纹理(texture，也就是那个相机图标)和layer(蓝色的蒙版)的渲染结果。但这两个渲染结果没有直接放入Render Buffer中，也就表示这是离屏渲染。直到第三个渲染通道，才把两者组合起来放入Render Buffer中。离屏渲染意味着把渲染结果临时保存，等用到时再取出，因此相对于普通渲染更占用资源。</p><p>以下情况可能会导致触发离屏渲染：</p><ol><li>重写drawRect方法</li><li>有mask或者是阴影(layer.masksToBounds, layer.shadow*)，模糊效果也是一种mask</li><li>layer.shouldRasterize = true</li></ol><h2 id="卡顿的解决方案"><a href="#卡顿的解决方案" class="headerlink" title="卡顿的解决方案"></a>卡顿的解决方案</h2><p>CPU 和 GPU 不论哪个阻碍了显示流程，都会造成掉帧现象。所以开发时，也需要分别对 CPU 和 GPU 压力进行评估和优化。</p><h3 id="CPU-资源消耗原因和解决方案"><a href="#CPU-资源消耗原因和解决方案" class="headerlink" title="CPU 资源消耗原因和解决方案"></a>CPU 资源消耗原因和解决方案</h3><p><strong>1. 对象创建：</strong></p><p>对象的创建会分配内存、调整属性、甚至还有读取文件等操作，比较消耗 CPU 资源。尽量用轻量的对象代替重量的对象，可以对性能有所优化。比如 CALayer 比 UIView 要轻量许多，那么不需要响应触摸事件的控件，用 CALayer 显示会更加合适。如果对象不涉及 UI 操作，则尽量放到后台线程去创建，但可惜的是包含有 CALayer 的控件，都只能在主线程创建和操作。通过 Storyboard 创建视图对象时，其资源消耗会比直接通过代码创建对象要大非常多，在性能敏感的界面里，Storyboard 并不是一个好的技术选择。</p><p>尽量推迟对象创建的时间，并把对象的创建分散到多个任务中去。尽管这实现起来比较麻烦，并且带来的优势并不多，但如果有能力做，还是要尽量尝试一下。如果对象可以复用，并且复用的代价比释放、创建新对象要小，那么这类对象应当尽量放到一个缓存池里复用。</p><p><strong>2. 对象调整: </strong></p><p>对象的调整也经常是消耗 CPU 资源的地方。这里特别说一下 CALayer：CALayer 内部并没有属性，当调用属性方法时，它内部是通过运行时 resolveInstanceMethod 为对象临时添加一个方法，并把对应属性值保存到内部的一个 Dictionary 里，同时还会通知 delegate、创建动画等等，非常消耗资源。UIView 的关于显示相关的属性（比如 frame/bounds/transform）等实际上都是 CALayer 属性映射来的，所以对 UIView 的这些属性进行调整时，消耗的资源要远大于一般的属性。对此你在应用中，应该尽量减少不必要的属性修改。</p><p>当视图层次调整时，UIView、CALayer 之间会出现很多方法调用与通知，所以在优化性能时，应该尽量避免调整视图层次、添加和移除视图。</p><p><strong>3. 对象销毁: </strong></p><p>对象的销毁虽然消耗资源不多，但累积起来也是不容忽视的。通常当容器类持有大量对象时，其销毁时的资源消耗就非常明显。同样的，如果对象可以放到后台线程去释放，那就挪到后台线程去。这里有个小 Tip：把对象捕获到 block 中，然后扔到后台队列去随便发送个消息以避免编译器警告，就可以让对象在后台线程销毁了。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">NSArray</span> *tmp = <span class="keyword">self</span>.array;</span><br><span class="line"><span class="keyword">self</span>.array = <span class="literal">nil</span>;</span><br><span class="line"><span class="built_in">dispatch_async</span>(queue, ^&#123;</span><br><span class="line">    [tmp <span class="keyword">class</span>];</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p><strong>4. 布局计算: </strong></p><p>视图布局的计算是 App 中最为常见的消耗 CPU 资源的地方。如果能在后台线程提前计算好视图布局、并且对视图布局进行缓存，那么这个地方基本就不会产生性能问题了。</p><p>不论通过何种技术对视图进行布局，其最终都会落到对 UIView.frame/bounds/center 等属性的调整上。上面也说过，对这些属性的调整非常消耗资源，所以尽量提前计算好布局，在需要时一次性调整好对应属性，而不要多次、频繁的计算和调整这些属性。</p><p><strong>5. Autolayout: </strong></p><p>Autolayout 是苹果本身提倡的技术，在大部分情况下也能很好的提升开发效率，但是 Autolayout 对于复杂视图来说常常会产生严重的性能问题。随着视图数量的增长，Autolayout 带来的 CPU 消耗会呈指数级上升。具体数据可以看这个文章：<a href="http://pilky.me/36/" target="_blank" rel="noopener">http://pilky.me/36/</a>。 如果你不想手动调整 frame 等属性，你可以用一些工具方法替代（比如常见的 left/right/top/bottom/width/height 快捷属性），或者使用 ComponentKit、AsyncDisplayKit 等框架。</p><p><strong>6. 文本计算: </strong></p><p>如果一个界面中包含大量文本（比如微博微信朋友圈等），文本的宽高计算会占用很大一部分资源，并且不可避免。如果你对文本显示没有特殊要求，可以参考下 UILabel 内部的实现方式：用 [NSAttributedString boundingRectWithSize:options:context:] 来计算文本宽高，用 -[NSAttributedString drawWithRect:options:context:] 来绘制文本。尽管这两个方法性能不错，但仍旧需要放到后台线程进行以避免阻塞主线程。</p><p>如果你用 CoreText 绘制文本，那就可以先生成 CoreText 排版对象，然后自己计算了，并且 CoreText 对象还能保留以供稍后绘制使用。</p><p><strong>7. 文本渲染:</strong></p><p>屏幕上能看到的所有文本内容控件，包括 UIWebView，在底层都是通过 CoreText 排版、绘制为 Bitmap 显示的。常见的文本控件 （UILabel、UITextView 等），其排版和绘制都是在主线程进行的，当显示大量文本时，CPU 的压力会非常大。对此解决方案只有一个，那就是自定义文本控件，用 TextKit 或最底层的 CoreText 对文本异步绘制。尽管这实现起来非常麻烦，但其带来的优势也非常大，CoreText 对象创建好后，能直接获取文本的宽高等信息，避免了多次计算（调整 UILabel 大小时算一遍、UILabel 绘制时内部再算一遍）；CoreText 对象占用内存较少，可以缓存下来以备稍后多次渲染。</p><p><strong>8. 图片的解码:</strong></p><p>当你用 UIImage 或 CGImageSource 的那几个方法创建图片时，图片数据并不会立刻解码。图片设置到 UIImageView 或者 CALayer.contents 中去，并且 CALayer 被提交到 GPU 前，CGImage 中的数据才会得到解码。这一步是发生在主线程的，并且不可避免。如果想要绕开这个机制，常见的做法是在后台线程先把图片绘制到 CGBitmapContext 中，然后从 Bitmap 直接创建图片。目前常见的网络图片库都自带这个功能。</p><p><strong>9. 图像的绘制:</strong></p><p>图像的绘制通常是指用那些以 CG 开头的方法把图像绘制到画布中，然后从画布创建图片并显示这样一个过程。这个最常见的地方就是 [UIView drawRect:] 里面了。由于 CoreGraphic 方法通常都是线程安全的，所以图像的绘制可以很容易的放到后台线程进行。一个简单异步绘制的过程大致如下（实际情况会比这个复杂得多，但原理基本一致）：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)display &#123;</span><br><span class="line">    <span class="built_in">dispatch_async</span>(backgroundQueue, ^&#123;</span><br><span class="line">        <span class="built_in">CGContextRef</span> ctx = <span class="built_in">CGBitmapContextCreate</span>(...);</span><br><span class="line">        <span class="comment">// draw in context...</span></span><br><span class="line">        <span class="built_in">CGImageRef</span> img = <span class="built_in">CGBitmapContextCreateImage</span>(ctx);</span><br><span class="line">        <span class="built_in">CFRelease</span>(ctx);</span><br><span class="line">        <span class="built_in">dispatch_async</span>(mainQueue, ^&#123;</span><br><span class="line">            layer.contents = img;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="GPU-资源消耗原因和解决方案"><a href="#GPU-资源消耗原因和解决方案" class="headerlink" title="GPU 资源消耗原因和解决方案"></a>GPU 资源消耗原因和解决方案</h3><p>相对于 CPU 来说，GPU 能干的事情比较单一：接收提交的纹理（Texture）和顶点描述（三角形），应用变换（transform）、混合并渲染，然后输出到屏幕上。通常你所能看到的内容，主要也就是纹理（图片）和形状（三角模拟的矢量图形）两类。</p><p><strong>1. 纹理的渲染</strong></p><p>所有的 Bitmap，包括图片、文本、栅格化的内容，最终都要由内存提交到显存，绑定为 GPU Texture。不论是提交到显存的过程，还是 GPU 调整和渲染 Texture 的过程，都要消耗不少 GPU 资源。当在较短时间显示大量图片时（比如 TableView 存在非常多的图片并且快速滑动时），CPU 占用率很低，GPU 占用非常高，界面仍然会掉帧。避免这种情况的方法只能是尽量减少在短时间内大量图片的显示，尽可能将多张图片合成为一张进行显示。</p><p>当图片过大，超过 GPU 的最大纹理尺寸时，图片需要先由 CPU 进行预处理，这对 CPU 和 GPU 都会带来额外的资源消耗。目前来说，iPhone 4S 以上机型，纹理尺寸上限都是 4096×4096，更详细的资料可以看这里：iosres.com。所以，尽量不要让图片和视图的大小超过这个值。</p><p><strong>2. 视图的混合 (Composing)</strong></p><p>当多个视图（或者说 CALayer）重叠在一起显示时，GPU 会首先把他们混合到一起。如果视图结构过于复杂，混合的过程也会消耗很多 GPU 资源。为了减轻这种情况的 GPU 消耗，应用应当尽量减少视图数量和层次，并在不透明的视图里标明 opaque 属性以避免无用的 Alpha 通道合成。当然，这也可以用上面的方法，把多个视图预先渲染为一张图片来显示。</p><p><strong>3. 图形的生成</strong></p><p>CALayer 的 border、圆角、阴影、遮罩（mask），CASharpLayer 的矢量图形显示，通常会触发离屏渲染（offscreen rendering），而离屏渲染通常发生在 GPU 中。当一个列表视图中出现大量圆角的 CALayer，并且快速滑动时，可以观察到 GPU 资源已经占满，而 CPU 资源消耗很少。这时界面仍然能正常滑动，但平均帧数会降到很低。为了避免这种情况，可以尝试开启 CALayer.shouldRasterize 属性，但这会把原本离屏渲染的操作转嫁到 CPU 上去。对于只需要圆角的某些场合，也可以用一张已经绘制好的圆角图片覆盖到原本视图上面来模拟相同的视觉效果。最彻底的解决办法，就是把需要显示的图形在后台线程绘制为图片，避免使用圆角、阴影、遮罩等属性。</p><!--iOS 保持界面流畅的技巧 https://blog.ibireme.com/2015/11/12/smooth_user_interfaces_for_ios/ --><!--内存恶鬼drawRect https://bihongbo.com/2016/01/03/memoryGhostdrawRect/ --><!-- UIKit性能调试 https://bestswifter.com/uikitxing-neng-diao-you-shi-zhan-jiang-jie/-->]]></content>
    
    <summary type="html">
    
      &lt;p&gt;结合Instrument CoreAnimation分析影响性能的因素： &lt;strong&gt;图层混合&lt;/strong&gt;,&lt;strong&gt;光栅化&lt;/strong&gt;,&lt;strong&gt;颜色格式&lt;/strong&gt;,&lt;strong&gt;图片大小&lt;/strong&gt;,&lt;strong&gt;离屏渲染&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;在使用UIKit的过程中，性能优化是永恒的话题。很多人都看过分析优化滑动性能的文章，但其中不少文章只介绍了优化方法却对背后的原理避而不谈，或者是晦涩难懂而且读者缺乏实践体验的机会。不妨思考一下下面的问题自己是否有一个清晰的认识：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;为什么要把控件尽量设置成不透明的，如果是透明的会有什么影响，如何检测这种影响？&lt;/li&gt;
&lt;li&gt;为什么cell中的图片，尽可能要使用正确的大小、格式，如果错误会有什么影响，如何检测这种影响？&lt;/li&gt;
&lt;li&gt;为什么设置阴影和圆角有可能影响滑动时流畅度？&lt;/li&gt;
&lt;li&gt;shouldRasterize和离屏渲染的关系是什么，何时应该使用？&lt;/li&gt;
&lt;/ol&gt;
    
    </summary>
    
      <category term="重学iOS" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/"/>
    
      <category term="Instruments" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/Instruments/"/>
    
    
      <category term="iOS" scheme="https://developerdoc.com/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>Instruments使用</title>
    <link href="https://developerdoc.com/essay/Instruments/Instruments2/"/>
    <id>https://developerdoc.com/essay/Instruments/Instruments2/</id>
    <published>2019-03-06T02:25:49.000Z</published>
    <updated>2019-03-06T13:12:17.141Z</updated>
    
    <content type="html"><![CDATA[<p>介绍一下使用<code>查看 CPU 性能</code> 、<code>图形性能</code>和<code>内存性能</code>,要执行Instruments需要先在Xcode 中对当前的项目做 Profile (Command-I ：</p><p><img src="/res/Instrument/2.png" width="80%"><br><a id="more"></a><br>这时就会打开 Profiling Template 选项对话框：<br><img src="/res/Instrument/3.png" width="80%"></p><h2 id="查看-CPU-性能"><a href="#查看-CPU-性能" class="headerlink" title="查看 CPU 性能"></a>查看 CPU 性能</h2><p><strong>1、</strong> 选择 Time Profiler 这个模板，并点击 Choose 按钮。<br><img src="/res/Instrument/4.png" width="20%"></p><p><strong>2、</strong> 进入 Instruments 后，选择正确的设备和应用程序：<br><img src="/res/Instrument/5.png" width="80%"></p><p><strong>3、</strong>点击红色按钮运行后，就能得到 CPU 性能的结果了：<br><img src="/res/Instrument/6.png" width="80%"></p><p><strong>4、</strong>我们还能在时间轴面板里面去选择一段时间来查看该时间段里更为细节的 CPU 性能<br><img src="/res/Instrument/7.png" width="80%"></p><p>从这个结果中，我们可以看到不同的线程以及方法调用占用的时间，从而可以评估出 CPU 性能的瓶颈和优化方向。</p><p><strong>5、</strong>除此之外，我们还能在 Instruments 选择使用 Counters、Activity Monitor、System Trace 等 Profiling Template 对程序做 CPU 性能考量。</p><p><strong>6、</strong>需要注意的是，有时候如果你使用 Instruments 查看调用堆栈时可能看到的都是地址而不是函数名，这样就不太好定位问题了。这时候你可以把相关项目的 Build Settings - Debug Information Format 的 Debug 和 Release 都设置为 DWARF with dSYM File，这样就能将对应的堆栈信息符号化显示了。</p><h2 id="图形性能"><a href="#图形性能" class="headerlink" title="图形性能"></a>图形性能</h2><p>关于图形性能方面，我们平时最关注的应该就是「帧率」这个概念了。在 Instruments 中，我们可以使用 <code>Core Animation</code> + <code>Time Profiler</code> 来评估图形性能。使用步骤如下：</p><p><strong>1、</strong>同「CPU 占用性能测试」一样，先在 Xcode 中对当前的项目执行 Profile (Command-I ，并在打开的对话框中选择 Core Animaiton 这个模板：<br><img src="/res/Instrument/8.png" width="20%"><br><strong>2、</strong>进入 Instruments 后，选择正确的设备和应用程序。</p><p><strong>3、</strong>点击红色按钮运行应用程序，随着我们操作 App 界面，就可以看到帧率的变化和数据了：</p><p><img src="/res/Instrument/9.png" width="80%"><br>在滑动屏幕时，帧率越高表示性能越好，帧率过低则意味着屏幕可能会出现卡顿。</p><p><strong>4、</strong>在右下角面板的 Display Settings 区域，我们可以看到多个 Debug Options：</p><ul><li><code>Color Blended Layers</code>，这个选项选项基于渲染程度对屏幕中的混合区域进行绿到红的高亮显示，<strong>越红表示性能越差</strong>，会对帧率等指标造成较大的影响。红色通常是由于多个半透明图层叠加引起。</li><li><code>Color Hits Green and Misses Red</code>，当 UIView.layer.shouldRasterize = YES 时，耗时的图片绘制会被缓存，并当做一个简单的扁平图片来呈现。这时候，如果页面的其他区块(比如 UITableViewCell 的复用’ width=’80%’/&gt;使用缓存直接命中，就显示绿色，反之，如果不命中，这时就显示红色。<strong>红色越多，性能越差</strong>。因为栅格化生成缓存的过程是有开销的，如果缓存能被大量命中和有效使用，则总体上会降低开销，反之则意味着要频繁生成新的缓存，这会让性能问题雪上加霜。</li><li><code>Color Copied Images</code>，对于 GPU 不支持的色彩格式的图片只能由 CPU 来处理，把这样的图片标为蓝色。<strong>蓝色越多，性能越差</strong>。因为，我们不希望在滚动视图的时候，由 CPU 来处理图片，这样可能会对主线程造成阻塞。</li><li><code>Color Immediately</code>，通常 Core Animation Instruments 以每毫秒 10 次的频率更新图层调试颜色。对某些效果来说，这显然太慢了。这个选项就可以用来设置每帧都更新（可能会影响到渲染性能，而且会导致帧率测量不准，所以不要一直都设置它）。</li><li><code>Color Misaligned Images</code>，这个选项检查了图片是否被缩放，以及像素是否对齐。被放缩的图片会被标记为黄色，像素不对齐则会标注为紫色。<strong>黄色、紫色越多，性能越差。</strong></li><li><code>Color Offscreen-Rendered Yellow</code>，这个选项会把那些离屏渲染的图层显示为黄色。<strong>黄色越多，性能越差</strong>。这些显示为黄色的图层很可能需要用 shadowPath 或者 shouldRasterize 来优化。</li><li><code>Color OpenGL Fast Path Blue</code>，这个选项会把任何直接使用 OpenGL 绘制的图层显示为蓝色。<strong>蓝色越多，性能越好</strong>。如果仅仅使用 UIKit 或者 Core Animation 的 API，那么不会有任何效果。如果使用 GLKView 或者 CAEAGLLayer`，那如果不显示蓝色块的话就意味着你正在强制 CPU 渲染额外的纹理，而不是绘制到屏幕。</li><li><code>Flash Updated Regions</code>，这个选项会把重绘的内容显示为黄色。不该出现的<strong>黄色越多，性能越差</strong>。通常我们希望只是更新的部分被标记完黄色。</li></ul><p>我们可以使用这些选项，来监测更加具体的图形性能。</p><p><strong>5、</strong>我们还可以选择使用 OpenGL ES Analysis、GPU Driver 等模板来监测图形相关性能。</p><h2 id="内存性能"><a href="#内存性能" class="headerlink" title="内存性能"></a>内存性能</h2><p>Instruments 可以帮我们了解到应用程序使用内存的几个方面：</p><ul><li><strong>全局内存使用情况(Overall Memory Use</strong>: 从全局的角度监测应用程序的内存使用情况，捕捉非预期的或大幅度的内存增长。</li><li><strong>内存泄露(Leaked memory </strong>: 未被你的程序引用，同时也不能被使用或释放的内存。</li><li><strong>废弃内存(Abandoned memory </strong>: 被你的程序引用，但是没什么卵用的内存。<br>僵尸对象(Zombies’ width=’80%’/&gt;: 僵尸对象指的是对应的内存已经被释放并且不再会使用到，但是你的程序却在某处依然有指向它的引用。在 iOS 中有一个 NSZombie 机制，这个是为了内存调试的目的而设计的一种机制。在这个机制下，当你 NSZombieEnabled 为 YES 时，当一个对应的引用计数减为 0 时，这个对象不会被释放，当这个对象再收到任何消息时，它会记录一条 warning，而不是直接崩溃，以方便我们进行程序调试。<br>这里我们介绍下查找内存泄露的过程：</li></ul><p><strong>1、</strong>同「CPU 占用性能测试」一样，先在 Xcode 中对当前的项目执行 Profile (Command-I ，并在打开的对话框中选择 Leaks 这个模板：<br><img src="/res/Instrument/10.png" width="20%"></p><p><strong>2、</strong>进入 Instruments 后，选择正确的设备和应用程序。</p><p><strong>3、</strong>点击红色按钮运行应用程序，我们可以看到如下界面：<br><img src="/res/Instrument/11.png" width="80%"></p><p><strong>4、</strong>在 Display Settings 中勾选 <code>Invert Call Tree</code> 和 <code>Hide System Libraries</code> 或其他选项可以过滤显示的数据。<br><img src="/res/Instrument/12.png" width="80%"></p><p><strong>5、</strong>在导航栏的筛选框中，我们可以输入关键字来筛选数据。<br><img src="/res/Instrument/13.png" width="80%"></p><p><strong>6、</strong>在实际使用中，我们进入一个页面后再退出，发现相关的内存分配没有清空，这时候就发生内存泄露了。我们查看更细节的调用信息，追踪到可能造成内存泄露的代码位置：<br><img src="/res/Instrument/14.png" width="80%"><br><strong>7、</strong>我们还可以使用 Activity Monitor、Allocations、Zombies 等模板来针对性地做内存监测。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;介绍一下使用&lt;code&gt;查看 CPU 性能&lt;/code&gt; 、&lt;code&gt;图形性能&lt;/code&gt;和&lt;code&gt;内存性能&lt;/code&gt;,要执行Instruments需要先在Xcode 中对当前的项目做 Profile (Command-I ：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/res/Instrument/2.png&quot; width=&quot;80%&quot;&gt;&lt;br&gt;
    
    </summary>
    
      <category term="重学iOS" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/"/>
    
      <category term="Instruments" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/Instruments/"/>
    
    
      <category term="iOS" scheme="https://developerdoc.com/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>CoreLocation 教程</title>
    <link href="https://developerdoc.com/essay/CoreLocation/"/>
    <id>https://developerdoc.com/essay/CoreLocation/</id>
    <published>2019-03-04T12:40:16.000Z</published>
    <updated>2019-03-14T12:55:26.238Z</updated>
    
    <content type="html"><![CDATA[<h1 id="CoreLocation-iOS定位"><a href="#CoreLocation-iOS定位" class="headerlink" title="CoreLocation - iOS定位"></a>CoreLocation - iOS定位</h1><p>使用 <a href="https://developer.apple.com/documentation/corelocation" target="_blank" rel="noopener">CoreLocation</a> 框架定位，基本分为权限获取,位置更新，区域检测，iBeacon,方向检测，地理编码几方面。</p><p>demo链接： <a href="https://github.com/sunyanyan/BlogDemos/tree/master/Location_Demo" target="_blank" rel="noopener">Location_Demo</a></p><h2 id="iOS的定位支持"><a href="#iOS的定位支持" class="headerlink" title="iOS的定位支持"></a>iOS的定位支持</h2><blockquote><p>GPS卫星定位</p></blockquote><p>其原理就是:利用天上的卫星(24颗工作卫星和数颗备份星)不断地广播信号, 地面的GPS接收设备收到信号后, 通过分析多个卫星信号，就可以计算出地球坐标, GPS保证全球任何一个地方(98%)都可以同时收到至少4个卫星的信号, 从而可以准确确定您的经纬度以及海拔位置（三颗星只能获得经纬度，四颗星还可以获得海拔高度）。GPS定位精度可达10米以内<br><a id="more"></a><br>iOS的GPS定位于单纯的GPS定位不同，它是A-GPS（所谓辅助GPS），首先通过基站定位或WiFi定位获得该设备的大概位置，然后通过将设备的大致位置发发哦远程服务器，由服务器负责进行查询和计算，从而获取当前位置的卫星信息，并通过网络将这些卫星信息反馈给iOS设备，避免了iOS设备直接通过GPS扫面、分析天上的卫星信息。A-GPS优点是定位快，缺点是需要网络，但也只是在初次定位时需要网络，因为一旦卫星信息返回，在有限时间和范围内，这些信息无须改变，之后的GPS定位就不再需要联网，都是直接用这些卫星参数接受信息了。</p><p>与基站、WIFI定位相比，GPS定位耗电量最大，速度最慢，但是精度最高。</p><blockquote><p>基站定位</p></blockquote><p>每个手机基站都是一个标识符，iOS设备可以搜集周围所有收到信号的基站和它们的标识符，通过联网发送到苹果云端服务器，再由服务器根据这些基站的位置信息查询并计算出当前位置，然后把该定位信息返回给手机。</p><blockquote><p>WiFi定位</p></blockquote><p>iOS设备通过无线网卡搜集周围所有的WiFi热点，获得它们的MAC地址，然后通过Apple的云端服务器查询该WiFi热点是否已经登记，如果已经登记，即可获取该WiFi热点的位置，最后通过对多个WiFi热点折中计算得到当前位置并返回给iOS设备。精确度大概在几十米范围内。</p><h2 id="定位相关API"><a href="#定位相关API" class="headerlink" title="定位相关API"></a>定位相关API</h2><p>首先需要获取地理信息权限。定位权限分为 <strong>仅在使用应用期间</strong> 和 <strong>始终允许</strong> ; 获取到权限后，使用 CLLocationManager 类可以开始在其代理方法 <em>didUpdateLocations</em> 中获得CLLocation位置信息。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//CLLocationManager的常用操作</span></span><br><span class="line"><span class="comment">//开始用户定位</span></span><br><span class="line">- (<span class="keyword">void</span>)startUpdatingLocation;</span><br><span class="line"><span class="comment">//停止用户定位</span></span><br><span class="line">- (<span class="keyword">void</span>) stopUpdatingLocation;</span><br><span class="line"><span class="comment">//当调用了startUpdatingLocation方法后，就开始不断地定位用户的位置，中途会频繁地调用代理的下面方法</span></span><br><span class="line">- (<span class="keyword">void</span>)locationManager:(<span class="built_in">CLLocationManager</span> *)manager didUpdateLocations:(<span class="built_in">NSArray</span> *)locations;</span><br></pre></td></tr></table></figure><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//定位设置：</span></span><br><span class="line"><span class="comment">//只有当最新的位置与上一次获取的位置之间的距离, 大于这个值时, 才会通过代理告诉外界.</span></span><br><span class="line"><span class="keyword">self</span>.locationM.distanceFilter = <span class="number">100</span>;</span><br><span class="line"> </span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">定位精确度</span></span><br><span class="line"><span class="comment">kCLLocationAccuracyBestForNavigation    最适合导航</span></span><br><span class="line"><span class="comment">kCLLocationAccuracyBest    精度最好的</span></span><br><span class="line"><span class="comment">kCLLocationAccuracyNearestTenMeters    附近10米</span></span><br><span class="line"><span class="comment">kCLLocationAccuracyHundredMeters    附近100米</span></span><br><span class="line"><span class="comment">kCLLocationAccuracyKilometer    附近1000米</span></span><br><span class="line"><span class="comment">kCLLocationAccuracyThreeKilometers    附近3000米</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">self</span>.locationM.desiredAccuracy = kCLLocationAccuracyBest;</span><br></pre></td></tr></table></figure><p>可以获得的 CLLocation 位置信息包括了：经纬度、位置的精度、海拔高度、海拔高度的精度、速度等信息。</p><p>相应的通过CLLocation 可以计算两地之间的直线距离:</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 北京:39.6 116.39</span></span><br><span class="line"><span class="comment">// 广州:23.08 113.15</span></span><br><span class="line"><span class="built_in">CLLocation</span> *BeiJing = [[<span class="built_in">CLLocation</span> alloc] initWithLatitude:<span class="number">39.6</span> longitude:<span class="number">116.39</span>];</span><br><span class="line"><span class="built_in">CLLocation</span> *GuangZhou = [[<span class="built_in">CLLocation</span> alloc] initWithLatitude:<span class="number">23.08</span> longitude:<span class="number">113.15</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 得到两地之间的距离</span></span><br><span class="line"><span class="built_in">CLLocationDistance</span> distance = [BeiJing distanceFromLocation:GuangZhou];</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"%.2f"</span>, distance);</span><br></pre></td></tr></table></figure><h2 id="后台定位"><a href="#后台定位" class="headerlink" title="后台定位"></a>后台定位</h2><blockquote><p>后台短时间持续定位</p></blockquote><p>可以在 Background Modes 中开启Location Updates选项。这样就可以实现后台定位，但是该方法只能实现后台定位20-30分钟的时间。</p><blockquote><p>后台永久持续定位</p></blockquote><p>设置代码如下：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//allowsBackgroundLocationUpdates 这个属性是 iOS 9.0 新增的属性，</span></span><br><span class="line"><span class="comment">//默认为 NO ，设置为 YES</span></span><br><span class="line"><span class="comment">//需要保证 Target -&gt; Capabilities -&gt; Background Modes 的开关打开，并勾选 Location updates ，否则会导致 crash。</span></span><br><span class="line"><span class="keyword">if</span> (@available(iOS <span class="number">9.0</span>, *)) &#123;</span><br><span class="line"><span class="keyword">self</span>.locationManager.allowsBackgroundLocationUpdates = <span class="literal">YES</span>;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// Fallback on earlier versions</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//系统是否可以自行中断程序的定位功能    </span></span><br><span class="line"><span class="keyword">self</span>.locationManager.pausesLocationUpdatesAutomatically = <span class="literal">NO</span>;</span><br></pre></td></tr></table></figure><p>该方法让系统不能够自行关闭程序的定位功能，保证程序一直处于后台定位中， </p><p><strong>定位权限为应用使用期间的时候，程序在后台运行时会在顶部有一条蓝色的信息框<br>定位权限为始终的时候，就不会有蓝色框了。</strong></p><blockquote><p>关于 pausesLocationUpdatesAutomatically 属性</p></blockquote><p>CLLocationManager.pausesLocationUpdatesAutomatically 设置是否允许自动暂停定位服务，目前Apple的官方文档描述的很模糊，导致Stackoverflow上有很多的讨论：</p><p>该选项主要用来节省电量消耗，防止app在进入后台后没有及时停止定位消耗电量，设置为YES后，当iOS判定设备不太可能发生移动的时候(这句话是Apple文档直译，然而并不知道具体怎么判定)，会自动暂停掉定位服务以及相关硬件设备<br>app在后台定位时，一旦符合了Apple的判定，定位服务以及相关硬件设备被暂停了，除非app回到前台，否则不会恢复定位服务(硬件暂停使得iOS无法判定设备移动)</p><h2 id="区域监听"><a href="#区域监听" class="headerlink" title="区域监听"></a>区域监听</h2><p>区域监听是指,我们通过CLCircularRegion指定一个区域, 然后当用户持握设备进入或者离开指定区域, 我们都能监听到.</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//初始化CLLocationManager等……</span></span><br><span class="line"> <span class="comment">// 1. 判断区域监听服务是否可用 </span></span><br><span class="line"> <span class="keyword">if</span> ([<span class="built_in">CLLocationManager</span> isMonitoringAvailableForClass:[<span class="built_in">CLCircularRegion</span> <span class="keyword">class</span>]]) </span><br><span class="line"> &#123;</span><br><span class="line"></span><br><span class="line">     <span class="comment">// 创建区域中心</span></span><br><span class="line">     <span class="built_in">CLLocationCoordinate2D</span> center = <span class="built_in">CLLocationCoordinate2DMake</span>(<span class="number">29.12345</span>, <span class="number">131.23456</span>);</span><br><span class="line">     <span class="comment">// 创建区域（指定区域中心，和区域半径）</span></span><br><span class="line">     <span class="built_in">CLLocationDistance</span> radius = <span class="number">1000</span>;</span><br><span class="line">     <span class="comment">// 判断区域半径是否大于最大监听区域半径,如果大于, 就没法监听</span></span><br><span class="line">     <span class="keyword">if</span> (radius &gt; <span class="keyword">self</span>.locationM.maximumRegionMonitoringDistance) &#123;</span><br><span class="line">         radius = <span class="keyword">self</span>.locationM.maximumRegionMonitoringDistance;</span><br><span class="line">     &#125;</span><br><span class="line">     <span class="built_in">CLCircularRegion</span> *region = [[<span class="built_in">CLCircularRegion</span> alloc] initWithCenter:center radius:radius identifier:<span class="string">@"一个identifier"</span>];</span><br><span class="line"></span><br><span class="line">     <span class="comment">// 开始监听指定区域</span></span><br><span class="line">     [<span class="keyword">self</span>.locationM startMonitoringForRegion:region];</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">else</span>&#123;<span class="built_in">NSLog</span>(<span class="string">@"区域监听不可用"</span>);&#125;</span><br></pre></td></tr></table></figure><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 进去监听区域后调用 </span></span><br><span class="line">-(<span class="keyword">void</span>)locationManager:(<span class="keyword">nonnull</span> <span class="built_in">CLLocationManager</span> *)manager didEnterRegion:(<span class="keyword">nonnull</span> <span class="built_in">CLRegion</span> *)region</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"进入区域---%@"</span>, region.identifier);</span><br><span class="line">    [manager stopMonitoringForRegion:region];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 离开监听区域后调用 </span></span><br><span class="line">-(<span class="keyword">void</span>)locationManager:(<span class="keyword">nonnull</span> <span class="built_in">CLLocationManager</span> *)manager didExitRegion:(<span class="keyword">nonnull</span> <span class="built_in">CLRegion</span> *)region</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"离开区域---%@"</span>, region.identifier);</span><br><span class="line">    [manager stopMonitoringForRegion:region];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//监听某个区域时, 只有进入或者离开这个区域时, 才能回调对应的方法, 是一个进入或者离开的动作 </span></span><br><span class="line"><span class="comment">//如果想知道某一个区域的当前状态(识别用户是在区域内部, 还是区域外部), 则需要使用以下方法</span></span><br><span class="line"></span><br><span class="line">[<span class="keyword">self</span>.locationM requestStateForRegion:region];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 请求某个区域状态时, 回调的代理方法</span></span><br><span class="line">-(<span class="keyword">void</span>)locationManager:(<span class="built_in">CLLocationManager</span> *)manager didDetermineState:(<span class="built_in">CLRegionState</span>)state forRegion:(<span class="built_in">CLRegion</span> *)region</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">switch</span> (state) &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="built_in">CLRegionStateUnknown</span>:</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"未知状态"</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="built_in">CLRegionStateInside</span>:</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"在区域内部"</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="built_in">CLRegionStateOutside</span>:</span><br><span class="line">            <span class="built_in">NSLog</span>(<span class="string">@"在区域外部"</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">         <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="ibeacon"><a href="#ibeacon" class="headerlink" title="ibeacon"></a>ibeacon</h2><p>iBeacon 是苹果公司2013年9月发布的移动设备用OS（iOS7）上配备的新功能。其工作方式是，配备有低功耗蓝牙（BLE）通信功能的设备使用BLE技术向周围发送自己特有的 ID，接收到该 ID 的应用软件会根据该 ID 采取一些行动。</p><p>iOS 中 iBeacon 是基于地理位置的微定位技术，虽然借助手机蓝牙进行接收Majro、Minor，但是他们在开发工程中没有任何关系。<br>iBeacon使用苹果提供CoreLocation库，然而在 BLE 在开发过程中使用CoreBluetooth库。从上面提供的库来看就很清楚了，特别是在 iOS8.0 之后的时候如果想使用iBeacon，必须让用户点击是否允许XXapp使用地理位置。如果在第一次使用 iOS App 扫描iBeacon的时候没有提示这句话，是不可能接收到iBeacon的信号（除非iOS 8.0之下）。如果是 BLE 则的开发过程中之需要提示用户打开蓝牙，并不要求其他的地理位置任何信息。</p><p><em>就不写代码了……</em></p><h2 id="地理编码与反地理编码"><a href="#地理编码与反地理编码" class="headerlink" title="地理编码与反地理编码"></a>地理编码与反地理编码</h2><p>需要获得网络权限</p><blockquote><p>地理编码：根据给定的地名，获得具体的位置信息（比如经纬度、地址的全称等)。</p></blockquote><p>输入上海市闵行区南华街25号的结果：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">CLGeocoder</span> *geocoder = [[<span class="built_in">CLGeocoder</span> alloc] init];</span><br><span class="line">[geocoder geocodeAddressString:address </span><br><span class="line">completionHandler:^(<span class="built_in">NSArray</span>&lt;<span class="built_in">CLPlacemark</span> *&gt; * _Nullable placemarks, <span class="built_in">NSError</span> * _Nullable error) &#123;</span><br><span class="line">        <span class="comment">//获取结果        </span></span><br><span class="line">    &#125;];</span><br></pre></td></tr></table></figure><p><img src="/res/CoreLocation/1.jpeg" alt=""></p><blockquote><p>反地理编码：根据给定的经纬度，获得具体的位置信息,省市区街道等信息;</p></blockquote><p>解析定位当前的位置的结果：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">CLGeocoder</span> *geocoder = [[<span class="built_in">CLGeocoder</span> alloc] init];</span><br><span class="line">[geocoder reverseGeocodeLocation:location </span><br><span class="line">completionHandler:^(<span class="built_in">NSArray</span>&lt;<span class="built_in">CLPlacemark</span> *&gt; * _Nullable placemarks, <span class="built_in">NSError</span> * _Nullable error) &#123;</span><br><span class="line"><span class="comment">//获取结果</span></span><br><span class="line">    &#125;];</span><br></pre></td></tr></table></figure><p><img src="/res/CoreLocation/2.png" alt=""></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">CLPlacemark 地标对象详解</span><br><span class="line">    location            : CLLocation 类型, 位置对象信息, 里面包含经纬度, 海拔等等</span><br><span class="line">    region              : CLRegion 类型, 地标对象对应的区域</span><br><span class="line">    addressDictionary  : NSDictionary 类型, 存放街道,省市等信息</span><br><span class="line">    name                : NSString 类型, 地址全称</span><br><span class="line">    thoroughfare        : NSString 类型, 街道名称</span><br><span class="line">    locality            : NSString 类型, 城市名称</span><br><span class="line">    administrativeArea : NSString 类型, 省名称</span><br><span class="line">    country             : NSString 类型, 国家名称</span><br></pre></td></tr></table></figure><h2 id="方向监测-指南针"><a href="#方向监测-指南针" class="headerlink" title="方向监测 / 指南针"></a>方向监测 / 指南针</h2><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//获取指南针信息</span></span><br><span class="line"><span class="comment">//①、CLLocationDegrees headingFilter：设置只有当设置方向的改变值超过该属性值时才激发delegate方法；</span></span><br><span class="line"><span class="comment">//②、CLDeviceOrientation headingOrientation：设置设备当前方向；</span></span><br><span class="line">[<span class="keyword">self</span>.locationManager startUpdatingHeading];</span><br><span class="line"><span class="comment">//当设备的方向改变时，iOS系统将会自动激发CLLocationManager的delegate对象的</span></span><br><span class="line">- (<span class="keyword">void</span>)locationManager:(<span class="built_in">CLLocationManager</span> *)manager didUpdateHeading:(<span class="built_in">CLHeading</span> *)newHeading &#123;</span><br><span class="line"> <span class="comment">//CLHeading方法的主要参数：</span></span><br><span class="line"><span class="comment">//与磁北方向的偏角@property(readonly, nonatomic) CLLocationDirection magneticHeading; </span></span><br><span class="line"><span class="comment">//与正北方向的偏角@property(readonly, nonatomic) CLLocationDirection trueHeading;</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>方向补充：</p></blockquote><p>Core Location支持两种不同的获取方向相关信息的方式：</p><p>当设备处于某个点时(CLHeading)，带有磁力计的设备能能够报告其位置信息；此类被称为设备的朝向。<br>当设备处于移动过程中时(CLLocation.course)，通过带有GPS硬件的报告的位置位置信息；此类被称为设备的行动方向。</p><p>朝向和行动方向表现出设备不同的信息。设备的朝向真实的反映出了设备在一个点时相对于磁北或者真北的朝向。而设备的行动方向则表现了设备在运动过程中的运动方向其并没有考虑设备的方向。你使用一种还是这两种相结合，完全取决于你的APP；例如：一个导航APP是否要在两者之间切换完全取决于用户的速度，在步行速度时，设备的朝向对于在当前环境下的确认用户方向是用户最为关心的事情；而在一辆车里运动时，行动方向则变得尤为重要。</p><hr><blockquote><p>后台定位补充：</p></blockquote><p><em>关于 pausesLocationUpdatesAutomatically 属性</em>:</p><p>CLLocationManager.pausesLocationUpdatesAutomatically 设置是否允许自动暂停定位服务，目前Apple的官方文档描述的很模糊，导致<a href="https://stackoverflow.com/questions/17484352/iphone-gps-in-background-never-resumes-after-pause" target="_blank" rel="noopener">Stackoverflow上有很多的讨论</a>：</p><p>该选项主要用来节省电量消耗，防止app在进入后台后没有及时停止定位消耗电量，设置为YES后，当iOS判定  <strong>设备不太可能发生移动的时候</strong>  (这句话是Apple文档直译，然而并不知道具体怎么判定)，会自动暂停掉定位服务以及相关硬件设备</p><p><em>唤醒</em>:</p><p>首先这个方法需要 始终 权限，然后在开启定位调用 startUpdatingLocation 的同时调用 startMonitoringSignificantLocationChanges 方法。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">[_locationManager startUpdatingLocation];</span><br><span class="line"><span class="comment">// 开启位置明显变化监控</span></span><br><span class="line">[_locationManager startMonitoringSignificantLocationChanges];</span><br></pre></td></tr></table></figure><p>开启了这个方法之后，当位置发生较明显的变化时同样会触发一个位置更新事件，这个变化距离不依据 distanceFilter 属性值变化，而是固定的 500 米（指与上一次触发此事件时的距离），且每 5 分钟最多触发一次，触发的时候如果 APP 被杀死了，这个事件还会唤醒 APP ，APP 会在用户无感知的情况下重新走正常的启动流程，只是界面上没有任何显示，此时会在 application:didFinishLaunchingWithOptions: 方法中携带 UIApplicationLaunchOptionsLocationKey 这个 key 来表明当前启动是被位置事件唤醒的，请注意判断这个 key 来绕过一些不需要在后台启动时执行的逻辑，或者执行一些你想要在后台启动后执行的逻辑，比如启动位置上报服务。在 APP 重新启动之后需要重新调用 startMonitoringSignificantLocationChanges 以保证 APP 再次被杀之后能被重新唤醒。</p><p>由此方法触发的位置更新事件同样会调用正常位置更新服务 startUpdatingLocation 的locationManager:didUpdateLocations: 回调方法，这个更新事件可能是一个最近缓存的位置，也可能是最新位置，而更精确的位置会在几秒钟之后再次回调，所以如果对细微位置差别比较敏感的话，可以通过回调的 CLLocation 对象中的时间戳来过滤。</p>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;CoreLocation-iOS定位&quot;&gt;&lt;a href=&quot;#CoreLocation-iOS定位&quot; class=&quot;headerlink&quot; title=&quot;CoreLocation - iOS定位&quot;&gt;&lt;/a&gt;CoreLocation - iOS定位&lt;/h1&gt;&lt;p&gt;使用 &lt;a href=&quot;https://developer.apple.com/documentation/corelocation&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;CoreLocation&lt;/a&gt; 框架定位，基本分为权限获取,位置更新，区域检测，iBeacon,方向检测，地理编码几方面。&lt;/p&gt;
&lt;p&gt;demo链接： &lt;a href=&quot;https://github.com/sunyanyan/BlogDemos/tree/master/Location_Demo&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Location_Demo&lt;/a&gt;&lt;/p&gt;
&lt;h2 id=&quot;iOS的定位支持&quot;&gt;&lt;a href=&quot;#iOS的定位支持&quot; class=&quot;headerlink&quot; title=&quot;iOS的定位支持&quot;&gt;&lt;/a&gt;iOS的定位支持&lt;/h2&gt;&lt;blockquote&gt;
&lt;p&gt;GPS卫星定位&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;其原理就是:利用天上的卫星(24颗工作卫星和数颗备份星)不断地广播信号, 地面的GPS接收设备收到信号后, 通过分析多个卫星信号，就可以计算出地球坐标, GPS保证全球任何一个地方(98%)都可以同时收到至少4个卫星的信号, 从而可以准确确定您的经纬度以及海拔位置（三颗星只能获得经纬度，四颗星还可以获得海拔高度）。GPS定位精度可达10米以内&lt;br&gt;
    
    </summary>
    
      <category term="重学iOS" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/"/>
    
    
      <category term="iOS" scheme="https://developerdoc.com/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>Instruments介绍</title>
    <link href="https://developerdoc.com/essay/Instruments/Instruments1/"/>
    <id>https://developerdoc.com/essay/Instruments/Instruments1/</id>
    <published>2019-02-25T12:27:44.000Z</published>
    <updated>2019-03-06T13:11:44.268Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/res/Instrument/instrument1.png" width="80%"></p><h2 id="Instruments-是什么"><a href="#Instruments-是什么" class="headerlink" title="Instruments 是什么"></a>Instruments 是什么</h2><p><a href="https://help.apple.com/instruments/mac/current/" target="_blank" rel="noopener">Instruments</a> 是 Xcode 的一个工具集，为我们提供了强大的程序性能分析及测试能力。使用 Instruments 你可以做下面这些事：<br><a id="more"></a></p><ol><li>检查一个或多个应用或进程的行为。</li><li>检查设备相关的功能，比如：Wi-Fi、蓝牙等。</li><li>在真机或模拟器上进行性能测试。</li><li>创建自定义的 DTrace 来分析系统以及应用的各种行为。</li><li>跟踪源码中的问题。</li><li>对 App 进行性能分析。</li><li>查找 App 中的内存问题，比如：内存泄露(Leaked memory)、废弃内存(Abandoned memory)、僵尸(zombies)等。</li><li>给出程序优化的建议。</li><li>进行系统级别的问题定位。</li><li>通过脚本记录一个用户行为序列，从而可以通过运行脚本对你的 iOS 应用进行可重复多次的自动化测试。</li><li>保存测试配置模板以供复用。</li></ol><h2 id="我们看一下开始模板。"><a href="#我们看一下开始模板。" class="headerlink" title="我们看一下开始模板。"></a>我们看一下开始模板。</h2><table><thead><tr><th>项目           模板</th><th>详情</th></tr></thead><tbody><tr><td>1.Blank</td><td>创建一个空的模板，你可以自定义的添加各种工具。</td></tr><tr><td>2.Activity Monitor</td><td>可以只用这个模板，研究系统工作负载和虚拟内存大小的关系</td></tr><tr><td>3.Allocations</td><td>将Allocations和VM跟踪器加到跟踪文档中，使用该工具可以监视内存和对象的内存分配方式和情况。</td></tr><tr><td>4.CocoaLayout</td><td>是一种Cocoa布局工具 ，可以应用于iOS模拟器和Cocoa桌面应用，但是不能和连接的iOS设备一起使用。观察NSLayoutConstraint对象的改变，帮助我们判断什么时间什么地点的constraint是否合理</td></tr><tr><td>5.Core Animation</td><td>将CoreAnimation加入到跟踪文档中，可以测量ios设备上每秒的CoreAnimation帧数，这可以帮助你理解内容是如何渲染到屏幕上的，（图形性能）这个模块显示程序显卡性能以及CPU使用情况。</td></tr><tr><td>6.CoreData</td><td>将CoreData数据提取、缓存缺失和存储加入到跟踪文档中，使用这个工具可以检测应用程序中数据的存储交互。</td></tr><tr><td>7.Counters</td><td>收集使用时间或基于事件的抽样方法的性能监控计数器（PMC）事件。</td></tr><tr><td>8.Energy Log</td><td>耗电量监控，将Energy Diagnostics, CPU Activity, Display Brightness, Sleep/Wake, Bluetooth, WiFi, and GPS instruments加入到跟踪文档中进行检测。</td></tr><tr><td>9.File Activity</td><td>将File Activity, Reads/Writes, File Attributes, and Directory I/O instruments 加入到跟踪文档中，只用这个模板可以让你检查系统文件的使用情况，可以检查文件的打开、关闭、读和写操作，同时也可以检测文件系统本身的改变，包括权限和所有权发生的改变。</td></tr><tr><td>10.Leaks</td><td>将the Allocations and Leaks instruments加入到模板中，使用这个模板可以帮助你检测内存的泄漏。</td></tr><tr><td>11.Metal System Trace</td><td>它是是apple 2014年在ios平台上推出的高效底层的3D图形API，它通过减少驱动层的API调用CPU的消耗提高渲染效率。</td></tr><tr><td>12.Network</td><td>用链接工具分析你的程序如何使用TCP/IP和UDP/IP链接。</td></tr><tr><td>13.OpenGL ES Analysis</td><td>将OpenGL ES Analyzer and OpenGL ES Driver加入到模板中，这个模块测量分析OpenGL ES活动正确性检测以及表现问题，提供解决建议。</td></tr><tr><td>14.System Trace</td><td>系统跟踪，通过显示当前被调度线程提供综合的系统表现，显示从用户到系统的转换代码通过两个系统调用或内存操作。</td></tr><tr><td>15.System Usage</td><td>这个模板记录关于文件读写，sockets，I/O系统活动， 输入输出。</td></tr><tr><td>16.Time Profile</td><td>执行对系统的CPU上运行的进程低负载时间为基础采样。</td></tr><tr><td>17. Zombies</td><td>测量一般的内存使用，专注于检测过度释放的野指针对象，也提供对象分配统计，以及主动分配的内存地址历史。</td></tr></tbody></table><h2 id="下面看一下这些工具是怎么使用的。"><a href="#下面看一下这些工具是怎么使用的。" class="headerlink" title="下面看一下这些工具是怎么使用的。"></a>下面看一下这些工具是怎么使用的。</h2><p><strong><em>定位内存问题</em></strong></p><ul><li>内存不合理引用，检测重复操作内存是否持续增长 Allocations每次操作后，点击mark generations button，会设置一个flag，然后查看每个迭代的详细数据。</li><li>内存泄漏Leaks，内存泄漏使用Leaks检测，如果对象发生内存泄漏，detail panel 中会看到对象的retain release历史记录，如果非对象发生内存泄漏，就会看到malloc和free的调用历史。</li><li>野指针Zombies，Zombie问题，引用retaincount为0的对象，使用Debug模式，将环境变量NSZombieEnabled设为true。</li></ul><p><strong><em>检测I/O活动</em></strong></p><p>检测iOS app如何使用TCP/IP和UDP/IP，与connections Instruments一起使用，检测app发送和接收的包数目。</p><p><strong><em>图像性能检测</em></strong></p><ul><li>core animation graphics 用来检测帧频率</li><li>OpenGL activity</li><li>GPU Driver</li></ul><p><strong><em>CPU使用</em></strong></p><ul><li>Performance Monitor Counters。</li><li>电量：Energry 跟踪电量、CPU、网络、显示亮度、睡眠/唤醒、蓝牙、wifi、GPS。可以全天开启Energry Diagnostics Log模式，在开发手机设备中，（重启或关机会丢失该数据），数据收集结束后，将log数据传到PC上。</li><li>线程使用Multicore Trace Template，分析多核性能，线程状态、调度队列、块使用情况。Multicore Trace Template包含Thread states和dispatch Instruments。</li><li>Timer Profiler Trace Template检测内核使用情况。</li></ul><p><strong><em>自动化测试</em></strong></p><p>UI automation Automation instrument 工具允许你让 iOS 应用的用户界面测试自动化。自动化界面测试可以让你：</p><ul><li>省去关键人员和释放其他工作资源</li><li>执行更多综合测试</li><li>开发可重复的回归测试</li><li>减少程序错误</li><li>提高开发周期，产品更新</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;img src=&quot;/res/Instrument/instrument1.png&quot; width=&quot;80%&quot;&gt;&lt;/p&gt;
&lt;h2 id=&quot;Instruments-是什么&quot;&gt;&lt;a href=&quot;#Instruments-是什么&quot; class=&quot;headerlink&quot; title=&quot;Instruments 是什么&quot;&gt;&lt;/a&gt;Instruments 是什么&lt;/h2&gt;&lt;p&gt;&lt;a href=&quot;https://help.apple.com/instruments/mac/current/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Instruments&lt;/a&gt; 是 Xcode 的一个工具集，为我们提供了强大的程序性能分析及测试能力。使用 Instruments 你可以做下面这些事：&lt;br&gt;
    
    </summary>
    
      <category term="重学iOS" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/"/>
    
      <category term="Instruments" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/Instruments/"/>
    
    
      <category term="iOS" scheme="https://developerdoc.com/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>重学iOS-知识树大纲</title>
    <link href="https://developerdoc.com/quick-start/ReLearning-iOS/"/>
    <id>https://developerdoc.com/quick-start/ReLearning-iOS/</id>
    <published>2019-02-12T02:12:31.000Z</published>
    <updated>2019-03-06T03:34:29.386Z</updated>
    
    <content type="html"><![CDATA[<p>来京东一年多了，从预想的ipad业务到RN业务，再到现在的iphone业务加RN业务。学习了很多RN、前端相关的知识也遗忘了部分iOS的相关逻辑。<br>现在打算重新回顾学习一下iOS,做一个系列帮助自己重学iOS,带自己还有你重新认识iOS。</p><p>在开始画知识树大纲之前，我想先来为这个系列定下一个小规范。本身我们可以网上找到很多参考资料，比如官方开发者文档这样的参考手册。苹果的参考手册做的是很好好全面，但这并不是我们所期望学习的，我想尽量和你一起打造一个iOS知识框架，再把主要的知识点做个遍历，其中主要探讨原理和背景相关的知识点。这样就算遇到了不认识的API知识点，也可以很容易地查阅开发者文档来解决。<br><a id="more"></a><br>我简单做个了一个划分，将iOS知识在总体上分为三个部分：<br><strong>一是基础部分，主要探讨初学者在业务中常遇见知识点和疑惑；</strong><br><img src="/res/reLearningiOS/重学iOS-基础.png" width="80%"></p><p><strong>二是深入部分，主要探讨iOS机制原理和比较具体业务倾向的框架应用；</strong><br><img src="/res/reLearningiOS/重学iOS-深入.png" width="80%"><br><strong>三是进阶实践部分，主要探讨业务开发中的难点疑点。</strong><br><img src="/res/reLearningiOS/重学iOS-进阶.png" width="80%"></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;来京东一年多了，从预想的ipad业务到RN业务，再到现在的iphone业务加RN业务。学习了很多RN、前端相关的知识也遗忘了部分iOS的相关逻辑。&lt;br&gt;现在打算重新回顾学习一下iOS,做一个系列帮助自己重学iOS,带自己还有你重新认识iOS。&lt;/p&gt;
&lt;p&gt;在开始画知识树大纲之前，我想先来为这个系列定下一个小规范。本身我们可以网上找到很多参考资料，比如官方开发者文档这样的参考手册。苹果的参考手册做的是很好好全面，但这并不是我们所期望学习的，我想尽量和你一起打造一个iOS知识框架，再把主要的知识点做个遍历，其中主要探讨原理和背景相关的知识点。这样就算遇到了不认识的API知识点，也可以很容易地查阅开发者文档来解决。&lt;br&gt;
    
    </summary>
    
      <category term="重学iOS" scheme="https://developerdoc.com/categories/%E9%87%8D%E5%AD%A6iOS/"/>
    
    
      <category term="iOS" scheme="https://developerdoc.com/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>可视化D3教程-快速开始</title>
    <link href="https://developerdoc.com/quick-start/d3-tutorials-quickstart/"/>
    <id>https://developerdoc.com/quick-start/d3-tutorials-quickstart/</id>
    <published>2018-12-12T02:12:31.000Z</published>
    <updated>2019-03-06T03:18:46.966Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>最近想开个可视化的坑，就看到了 <a href="https://github.com/d3/d3" target="_blank" rel="noopener">D3</a> , 别问为什么不选择echarts、Highcharts之类的，老夫写代码就看star数最多的……<br>D3 的全称是（Data-Driven Documents）, D3 将生成可视化的复杂步骤精简到了几个简单的函数，你只需要输入几个简单的数据，就能够转换为各种绚丽的图形。<br>咋一看d3首页 被其凌乱的文档折服了 差点就完成了就入门到放弃，于是做个学习过程记录，帮助一起入门d3的大家^_^</p></blockquote><a id="more"></a><p>本文快速上手先来画出这样一个图形：</p><p><img src="/res/d3/quick-start-2.png" width="33%"></p><p>迫于只会 React  之后的代码里可能会出现 react相关知识，但是只会vue的同学不用着急 重点相关的都是 d3 ，都可以看得懂得啦~ 废话不多说，让我们快速上手d3吧。</p><h3 id="引入d3"><a href="#引入d3" class="headerlink" title="引入d3"></a>引入d3</h3><p>引入d3的方式很多，我是用create-react-app创建示例工程，所以这里用npm引入</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">npm install d3</span><br><span class="line"><span class="comment">//安装后得到的d3版本 之后都会依据这个版本做示例： "d3": "^5.7.0",</span></span><br><span class="line"><span class="comment">//d3 4/5 版本之间的API会略有不同</span></span><br><span class="line"><span class="comment">//然后在js文件中</span></span><br><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> d3 <span class="keyword">from</span> <span class="string">'d3'</span></span><br></pre></td></tr></table></figure><h3 id="准备根视图和数据"><a href="#准备根视图和数据" class="headerlink" title="准备根视图和数据"></a>准备根视图和数据</h3><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//之后会在这里加入画布</span></span><br><span class="line">&lt;div id=<span class="string">"graph"</span> /&gt;</span><br><span class="line"><span class="comment">//本次最后会画出一个“柱状图”，简单准备下这样的数据源</span></span><br><span class="line"><span class="keyword">let</span> data = [<span class="number">2500</span>, <span class="number">2100</span>, <span class="number">1700</span>, <span class="number">1300</span>, <span class="number">900</span>]</span><br></pre></td></tr></table></figure><h3 id="设置画布"><a href="#设置画布" class="headerlink" title="设置画布"></a>设置画布</h3><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//在之前准备的div下插入svg</span></span><br><span class="line"><span class="keyword">let</span> svg = d3.select(<span class="string">"#graph"</span>) <span class="comment">// d3.select()：是选择所有指定元素的第一个</span></span><br><span class="line">    .append(<span class="string">"svg"</span>)<span class="comment">//获取到画布</span></span><br><span class="line">    .attr(<span class="string">"width"</span>, <span class="number">500</span>)<span class="comment">//设置宽度</span></span><br><span class="line">    .attr(<span class="string">"height"</span>, <span class="number">500</span>);<span class="comment">//设置高度</span></span><br></pre></td></tr></table></figure><p>D3 虽然没有明文规定一定要在 SVG 中绘图，但是 D3 提供了众多的 SVG 图形的生成器，它们都是只支持 SVG 的。因此，建议使用 SVG 画布。        </p><p>SVG，指可缩放矢量图形（Scalable Vector Graphics），是用于描述二维矢量图形的一种图形格式，是由万维网联盟制定的开放标准。SVG 使用 XML 格式来定义图形，可将 SVG 文本直接嵌入 HTML 中显示。</p><p>SVG 有如下特点：</p><ul><li>SVG 绘制的是矢量图，因此对图像进行放大不会失真。</li><li>基于 XML，可以为每个元素添加 JavaScript 事件处理器。</li><li>每个图形均视为对象，更改对象的属性，图形也会改变。</li><li>不适合游戏应用。</li></ul><h3 id="设置比例尺"><a href="#设置比例尺" class="headerlink" title="设置比例尺"></a>设置比例尺</h3><p>为什么要设置比例尺？直接使用输入的数值当成矩形的长度 如果数值过大或过小 可能画布没有这么大，或者根本看不见。</p><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> linear = d3.scaleLinear()<span class="comment">//返回线性比例尺</span></span><br><span class="line">    .domain([<span class="number">0</span>, <span class="number">2500</span>])<span class="comment">// 0-2500 映射到 0-100</span></span><br><span class="line">    .range([<span class="number">0</span>, <span class="number">250</span>]);<span class="comment">//这里0 2500对应输入数据的最小最大值</span></span><br></pre></td></tr></table></figure><h3 id="画出矩形"><a href="#画出矩形" class="headerlink" title="画出矩形"></a>画出矩形</h3><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 添加了与 data 数组的长度相同数量的矩形</span></span><br><span class="line">svg.selectAll(<span class="string">"rect"</span>)   <span class="comment">//选择svg内所有的矩形</span></span><br><span class="line">    .data(<span class="keyword">this</span>.props.data)  <span class="comment">//绑定数组</span></span><br><span class="line">    .enter()        <span class="comment">//指定选择集的enter部分</span></span><br><span class="line">    .append(<span class="string">"rect"</span>) <span class="comment">//添加足够数量的矩形元素 rect表示矩形</span></span><br><span class="line">    <span class="comment">//设置各个矩形的坐标和长宽</span></span><br><span class="line">    .attr(<span class="string">"x"</span>, <span class="number">25</span>) <span class="comment">//每个矩形的x坐标</span></span><br><span class="line">    .attr(<span class="string">"y"</span>, (d, i) =&gt; i * <span class="number">40</span>) <span class="comment">//每个矩形的y坐标</span></span><br><span class="line">    <span class="comment">//将比例尺应用到width</span></span><br><span class="line">    .attr(<span class="string">"width"</span>, (d, i) =&gt; &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">"&#123;d,i&#125;"</span>, &#123; d, i &#125;)</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">"linear(d)"</span>, linear(d))</span><br><span class="line">        <span class="keyword">return</span> linear(d);</span><br><span class="line">        &#125;)</span><br><span class="line">    .attr(<span class="string">"height"</span>, <span class="number">25</span>)</span><br><span class="line">    <span class="comment">//设置颜色</span></span><br><span class="line">    .attr(<span class="string">"fill"</span>, <span class="string">"#66ccff"</span>);</span><br></pre></td></tr></table></figure><h3 id="画出坐标轴"><a href="#画出坐标轴" class="headerlink" title="画出坐标轴"></a>画出坐标轴</h3><figure class="highlight jsx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> g = svg.append(<span class="string">"g"</span>);</span><br><span class="line">    <span class="comment">//设置x轴</span></span><br><span class="line">    g.append(<span class="string">"g"</span>)<span class="comment">//定义了坐标轴之后，只需要在 SVG 中添加一个分组元素 &lt;g&gt;，再将坐标轴的其他元素添加到这个 &lt;g&gt; 里即可</span></span><br><span class="line">        .attr(<span class="string">"transform"</span>, <span class="string">"translate(25,0)"</span>)<span class="comment">//坐标轴的位置，可以通过 transform 属性来设定。</span></span><br><span class="line">        .call(</span><br><span class="line">            d3.axisBottom(linear)<span class="comment">//axisBottom表示刻度向下的坐标轴</span></span><br><span class="line">            .ticks(<span class="number">5</span>)<span class="comment">//刻度的数量</span></span><br><span class="line">        );</span><br><span class="line">    <span class="comment">//设置y轴</span></span><br><span class="line">    <span class="comment">// svg.append("g").call(axis); 等价于  axis(svg.append(g));</span></span><br><span class="line">    g.append(<span class="string">"g"</span>)</span><br><span class="line">        .attr(<span class="string">"transform"</span>, <span class="string">"translate(25,0)"</span>)</span><br><span class="line">        .call(d3.axisLeft(linear).ticks(<span class="number">0</span>))</span><br></pre></td></tr></table></figure><h3 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h3><p>看到这里或许你会对 react/g 、 x/y/width/height/fill 以及 svg坐标系产生好奇,这里就推荐 MDN 的文档了 :</p><ul><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/SVG/Element" target="_blank" rel="noopener">SVG元素参考</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/SVG/Attribute" target="_blank" rel="noopener">SVG属性参考</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/SVG/Tutorial" target="_blank" rel="noopener">SVG教程</a></li></ul><p>完整代码 ： <a href="https://github.com/sunyanyan/d3-start/blob/master/src/View/D3/QuickStart.js" target="_blank" rel="noopener">QuickStart.js</a></p><p>在线演示 ： <a href="/d3start/#/quick-start">可视化学习-quick-start</a></p>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;最近想开个可视化的坑，就看到了 &lt;a href=&quot;https://github.com/d3/d3&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;D3&lt;/a&gt; , 别问为什么不选择echarts、Highcharts之类的，老夫写代码就看star数最多的……&lt;br&gt;D3 的全称是（Data-Driven Documents）, D3 将生成可视化的复杂步骤精简到了几个简单的函数，你只需要输入几个简单的数据，就能够转换为各种绚丽的图形。&lt;br&gt;咋一看d3首页 被其凌乱的文档折服了 差点就完成了就入门到放弃，于是做个学习过程记录，帮助一起入门d3的大家^_^&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="quick-start" scheme="https://developerdoc.com/categories/quick-start/"/>
    
      <category term="可视化D3教程" scheme="https://developerdoc.com/categories/quick-start/%E5%8F%AF%E8%A7%86%E5%8C%96D3%E6%95%99%E7%A8%8B/"/>
    
    
      <category term="可视化" scheme="https://developerdoc.com/tags/%E5%8F%AF%E8%A7%86%E5%8C%96/"/>
    
      <category term="D3" scheme="https://developerdoc.com/tags/D3/"/>
    
  </entry>
  
  <entry>
    <title>GitHub精选分享第三期</title>
    <link href="https://developerdoc.com/weekly-share/GitHub%E7%B2%BE%E9%80%89%E5%88%86%E4%BA%AB%E7%AC%AC%E4%B8%89%E6%9C%9F/"/>
    <id>https://developerdoc.com/weekly-share/GitHub精选分享第三期/</id>
    <published>2018-12-07T15:06:57.000Z</published>
    <updated>2019-03-06T03:19:10.618Z</updated>
    
    <content type="html"><![CDATA[<p>记录这周看到值得分享的事，本期就分享点前端可视化相关吧~</p><h3 id="lianjia-scrawler-链家二手房租房在线数据，存量房交易服务平台数据"><a href="#lianjia-scrawler-链家二手房租房在线数据，存量房交易服务平台数据" class="headerlink" title="lianjia-scrawler 链家二手房租房在线数据，存量房交易服务平台数据"></a><a href="https://github.com/XuefengHuang/lianjia-scrawler" target="_blank" rel="noopener">lianjia-scrawler 链家二手房租房在线数据，存量房交易服务平台数据</a></h3><a id="more"></a><p><a href="https://github.com/XuefengHuang/lianjia-scrawler" target="_blank" rel="noopener"><img src="https://github.com/XuefengHuang/lianjia-scrawler/raw/master/screenshots/homepage_1.png" width="50%"></a></p><blockquote class="blockquote-center"><p>该项目提供一个链家网全国房源爬虫工具，数据存储目前支持Mysql,Sqlite和Postgres。非常方便转化成csv等格式文件。<br>详细数据分析教程 <a href="http://www.ershoufangdata.com" target="_blank" rel="noopener">http://www.ershoufangdata.com</a></p></blockquote><h3 id="D3-一个基于web标准的JavaScript可视化库"><a href="#D3-一个基于web标准的JavaScript可视化库" class="headerlink" title="D3 一个基于web标准的JavaScript可视化库"></a><a href="https://github.com/d3/d3" target="_blank" rel="noopener">D3 一个基于web标准的JavaScript可视化库</a></h3><p><a href="https://github.com/d3/d3" target="_blank" rel="noopener"><img src="https://camo.githubusercontent.com/722a5cc12c7d40231ebeb8ca6facdc8547e2abf7/68747470733a2f2f64336a732e6f72672f6c6f676f2e737667" alt=""></a></p><blockquote class="blockquote-center"><p>D3 (或者叫 D3.js )是一个基于 web 标准的 JavaScript 可视化库.<br>D3 可以借助 SVG, Canvas 以及 HTML 将你的数据生动的展现出来.<br>D3 结合了强大的可视化交互技术以及数据驱动 DOM 的技术结合起来,<br>让你可以借助于现代浏览器的强大功能自由的对数据进行可视化。</p></blockquote><h3 id="G2-一套基于可视化编码的图形语法"><a href="#G2-一套基于可视化编码的图形语法" class="headerlink" title="G2 一套基于可视化编码的图形语法"></a><a href="https://github.com/antvis/g2" target="_blank" rel="noopener">G2 一套基于可视化编码的图形语法</a></h3><p><a href="https://github.com/antvis/g2" target="_blank" rel="noopener"><img src="https://antv.alipay.com/assets/image/home/g2/case3.png" alt=""></a></p><blockquote class="blockquote-center"><p>G2 是一套基于可视化编码的图形语法，<br>以数据驱动，具有高度的易用性和扩展性，<br>用户无需关注各种繁琐的实现细节，一条语句即可构建出各种各样的可交互的统计图表</p></blockquote><h3 id="echarts-一个使用JavaScript实现的开源可视化库"><a href="#echarts-一个使用JavaScript实现的开源可视化库" class="headerlink" title="echarts 一个使用JavaScript实现的开源可视化库"></a><a href="https://github.com/apache/incubator-echarts" target="_blank" rel="noopener">echarts 一个使用JavaScript实现的开源可视化库</a></h3><p><a href="https://github.com/apache/incubator-echarts" target="_blank" rel="noopener"><img src="https://github.com/apache/incubator-echarts/raw/master/asset/logo.png?raw=true" alt=" "></a></p><blockquote class="blockquote-center"><p>ECharts，一个使用 JavaScript 实现的开源可视化库，<br>可以流畅的运行在 PC 和移动设备上，<br>兼容当前绝大部分浏览器（IE8/9/10/11，Chrome，Firefox，Safari等），<br>底层依赖轻量级的矢量图形库 ZRender，提供直观，交互丰富，可高度个性化定制的数据可视化图表。</p></blockquote><h3 id="three-js-JavaScript-3D-library"><a href="#three-js-JavaScript-3D-library" class="headerlink" title="three.js JavaScript 3D library"></a><a href="https://github.com/mrdoob/three.js" target="_blank" rel="noopener">three.js JavaScript 3D library</a></h3><p><a href="https://github.com/mrdoob/three.js" target="_blank" rel="noopener"><img src="https://threejs.org/files/projects/infinitown.png" alt=" "></a></p><blockquote class="blockquote-center"><p>three.js是JavaScript编写的WebGL第三方库。提供了非常多的3D显示功能。T<br>hree.js 是一款运行在浏览器中的 3D 引擎，<br>你可以用它创建各种三维场景，包括了摄影机、光影、材质等各种对象。<br>你可以在它的主页上看到许多精采的演示。</p></blockquote><h3 id="highcharts-HTML5交互性图表库"><a href="#highcharts-HTML5交互性图表库" class="headerlink" title="highcharts HTML5交互性图表库"></a><a href="https://github.com/highcharts/highcharts" target="_blank" rel="noopener">highcharts HTML5交互性图表库</a></h3><p><a href="https://github.com/highcharts/highcharts" target="_blank" rel="noopener"><img src="https://shop.highsoft.com/media/highsoft/highchart-in-laptop-960x552-optimized.png" alt=" "></a></p><blockquote class="blockquote-center"><p>兼容 IE6+、完美支持移动端、图表类型丰富、方便快捷的 HTML5 交互性图表库</p></blockquote><h3 id="Leaflet-互动地图"><a href="#Leaflet-互动地图" class="headerlink" title="Leaflet 互动地图"></a><a href="https://github.com/Leaflet/Leaflet" target="_blank" rel="noopener">Leaflet 互动地图</a></h3><p><a href="https://github.com/Leaflet/Leaflet" target="_blank" rel="noopener"><img src="https://leafletjs.com/docs/images/logo.png" alt=" "></a></p><blockquote class="blockquote-center"><p>Leaflet 是一个为建设移动设备友好的互动地图,而开发的现代的、开源的 JavaScript 库</p></blockquote><h3 id="mxgraph"><a href="#mxgraph" class="headerlink" title="mxgraph "></a><a href="https://github.com/jgraph/mxgraph" target="_blank" rel="noopener">mxgraph </a></h3><p><a href="https://github.com/jgraph/mxgraph" target="_blank" rel="noopener"><img src="https://jgraph.github.io/mxgraph/docs/images/mxgraph_logo.gif" alt=" "></a></p><blockquote class="blockquote-center"><p>mxGraph客户端功能是 一个JavaScript库，<br>通过浏览器与您的图表显示和交互，提供通常所需的所有应用程序 功能以及方便添加自定义功能的高扩展性。<br>mxGraph是专为使用客户端 - 服务器架构而设计，<br>其中的JavaScript库负责与服务器通信交流以及更新图表的状态信息</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;记录这周看到值得分享的事，本期就分享点前端可视化相关吧~&lt;/p&gt;
&lt;h3 id=&quot;lianjia-scrawler-链家二手房租房在线数据，存量房交易服务平台数据&quot;&gt;&lt;a href=&quot;#lianjia-scrawler-链家二手房租房在线数据，存量房交易服务平台数据&quot; class=&quot;headerlink&quot; title=&quot;lianjia-scrawler 链家二手房租房在线数据，存量房交易服务平台数据&quot;&gt;&lt;/a&gt;&lt;a href=&quot;https://github.com/XuefengHuang/lianjia-scrawler&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;lianjia-scrawler 链家二手房租房在线数据，存量房交易服务平台数据&lt;/a&gt;&lt;/h3&gt;
    
    </summary>
    
      <category term="每周分享" scheme="https://developerdoc.com/categories/%E6%AF%8F%E5%91%A8%E5%88%86%E4%BA%AB/"/>
    
    
  </entry>
  
  <entry>
    <title>(译)React是如何区分Class和Function？</title>
    <link href="https://developerdoc.com/essay/(%E8%AF%91)how-does-react-tell-a-class-from-a-function/"/>
    <id>https://developerdoc.com/essay/(译)how-does-react-tell-a-class-from-a-function/</id>
    <published>2018-12-04T14:33:48.000Z</published>
    <updated>2019-03-06T03:19:45.753Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>原文地址: <a href="https://overreacted.io/how-does-react-tell-a-class-from-a-function/" target="_blank" rel="noopener">how-does-react-tell-a-class-from-a-function</a><br>本文地址： <a href="https://developerdoc.com/essay/(译)how-does-react-tell-a-class-from-a-function/">React是如何区分Class和Function？</a><br>边看边翻译 花了2h+… 如果你觉得读起来还算通顺不费事 那也算我为大家做了一点小贡献吧</p></blockquote><a id="more"></a><h2 id="React调用两者的不同之处"><a href="#React调用两者的不同之处" class="headerlink" title="React调用两者的不同之处"></a>React调用两者的不同之处</h2><p>一起来看下这个 function 类型的 <code>Greeting</code>组件：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Greeting</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> &lt;p&gt;Hello&lt;/p&gt;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>React 同样支持将它定义为 class 类型:<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Greeting</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> &lt;p&gt;Hello&lt;/p&gt;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>(直到<a href="https://reactjs.org/docs/hooks-intro.html" target="_blank" rel="noopener">最近 hooks-intro</a>，这是使用state等特性的唯一方法。)</p><p>当你想渲染<code>&lt;Greeting /&gt;</code>组件时，你不必关心它是如何定义的：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//类或者函数，都可以</span></span><br><span class="line">&lt;Greeting /&gt;</span><br></pre></td></tr></table></figure><p>但是，作为 <em>React本身</em> 是会认为这两个是有不同之处的。</p><p>如果<code>Greeting</code>是一个函数，React 只需要直接调用它:</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 你的代码</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Greeting</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> &lt;p&gt;Hello&lt;/p&gt;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// React内</span></span><br><span class="line"><span class="keyword">const</span> result = Greeting(props); <span class="comment">// &lt;p&gt;Hello&lt;/p&gt;</span></span><br></pre></td></tr></table></figure><p>但是如果<code>Greeting</code>是一个类，那么 React 就需要使用<code>new</code>来实例化它，然后在实例上调用<code>render</code>方法：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 你的代码</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Greeting</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> &lt;p&gt;Hello&lt;/p&gt;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// React内</span></span><br><span class="line"><span class="keyword">const</span> instance = <span class="keyword">new</span> Greeting(props); <span class="comment">// Greeting &#123;&#125;</span></span><br><span class="line"><span class="keyword">const</span> result = instance.render(); <span class="comment">// &lt;p&gt;Hello&lt;/p&gt;</span></span><br></pre></td></tr></table></figure></p><p>在这两种情况下，<code>React</code>的目标是获取渲染节点（本例中，是<code>&lt;p&gt; Hello &lt;/ p&gt;</code>），但确切的步骤取决于<code>Greeting</code>的类型。</p><p><strong>那么React如何知道某个东西是class类型还是function类型呢？</strong></p><p>事实上，这篇文章更多的是关于JavaScript而不是关于React。<br>如何你好奇React为何以某种方式运作，让我们一起挖掘其中的原理。</p><p><strong>这是一段漫长的探求之旅。这篇文章没有太多关于React本身的信息，我们将讨论<code>new</code>，<code>this</code>，<code>class</code>，<code>arrow function</code>，<code>prototype</code>，<code>__ proto__</code>，<code>instanceof</code>这些概念，以及这些东西如何在JavaScript中运作的机制。幸运的是，当你仅仅是使用React时，你不需要考虑这么多。但你如果要深究React……</strong></p><p>(如果你真的只想知道答案，请拉动到文章最后。)</p><h2 id="为什么要用不同的调用方式？"><a href="#为什么要用不同的调用方式？" class="headerlink" title="为什么要用不同的调用方式？"></a>为什么要用不同的调用方式？</h2><p>首先，我们需要理解以不同方式处理class和function的重要性。注意我们在调用类时如何使用new运算符：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// If Greeting is a function</span></span><br><span class="line"><span class="keyword">const</span> result = Greeting(props); <span class="comment">// &lt;p&gt;Hello&lt;/p&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// If Greeting is a class</span></span><br><span class="line"><span class="keyword">const</span> instance = <span class="keyword">new</span> Greeting(props); <span class="comment">// Greeting &#123;&#125;</span></span><br><span class="line"><span class="keyword">const</span> result = instance.render(); <span class="comment">// &lt;p&gt;Hello&lt;/p&gt;</span></span><br></pre></td></tr></table></figure></p><p>让我们大致的了解下<code>new</code>在Javascript中做了什么：</p><hr><p>在ES6之前，Javascript没有class这个概念。但是，可以使用纯函数表现出和class相似的模式。 <strong>具体来说，你可以使用new来调用类似类构造方法的函数，来表现出和class相似的模式</strong><br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 只是一个function</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = name;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> fred = <span class="keyword">new</span> Person(<span class="string">'Fred'</span>); <span class="comment">// ✅ Person &#123;name: 'Fred'&#125;</span></span><br><span class="line"><span class="keyword">var</span> george = Person(<span class="string">'George'</span>); <span class="comment">// 🔴 不会如期工作</span></span><br><span class="line"><span class="comment">//你今天仍然可以写这样的代码！在 `DevTools` 中尝试一下。</span></span><br></pre></td></tr></table></figure></p><p>如果不用<code>new</code>修饰<code>Person(&#39;Fred&#39;)</code>,Person内部的<code>this</code>在里面会指向 <code>window</code> 或者 <code>undefined</code> 。结果就是代码会崩溃或者像给<code>window.name</code>赋值一样愚蠢。</p><p>在调用之前添加<code>new</code>，等于说：“嘿 JavaScript，我知道<code>Person</code>只是一个函数，但让我们假装它是个类构造函数。 <strong>创建一个{}对象 并在<code>Person</code>函数内将<code>this</code>指向该对象， 这样我就可以赋值像<code>this.name</code>这样的东西。然后把那个对象返回给我。”</strong></p><p>上面这些就是<code>new</code>操作符做的事情。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> fred = <span class="keyword">new</span> Person(<span class="string">'Fred'</span>); <span class="comment">// `Person`内，相同的对象作为`this`</span></span><br></pre></td></tr></table></figure><p>同时<code>new</code>操作符使上面的<code>fred</code>对象可以使用<code>Person.prototype</code>上的任何内容。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = name;</span><br><span class="line">&#125;</span><br><span class="line">Person.prototype.sayHi = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  alert(<span class="string">'Hi, I am '</span> + <span class="keyword">this</span>.name);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> fred = <span class="keyword">new</span> Person(<span class="string">'Fred'</span>);</span><br><span class="line">fred.sayHi();</span><br></pre></td></tr></table></figure><p>这是之前人们在JavaScript模拟类的方式。</p><p>可以看到的是在JavaScript早已有<code>new</code>。但是，<code>class</code>却是后来加入的特性。为了更明确我们的意图，重写一下代码：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(name) &#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name;</span><br><span class="line">  &#125;</span><br><span class="line">  sayHi() &#123;</span><br><span class="line">    alert(<span class="string">'Hi, I am '</span> + <span class="keyword">this</span>.name);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> fred = <span class="keyword">new</span> Person(<span class="string">'Fred'</span>);</span><br><span class="line">fred.sayHi();</span><br></pre></td></tr></table></figure></p><p><em>在语言和API设计中把握开发人员的意图非常重要。</em></p><p>如果你编写一个函数，JavaScript就无法猜测你的意思是像<code>alert()</code>一样被直接调用，还是像<code>new Person()</code>那样充当构造函数。忘记添加<code>new</code>会导致令人困惑的执行结果。</p><p><strong>class语法让我们明确的告诉Javascript：“这不仅仅是一个函数 - 它是一个类，它有一个构造函数”。</strong> 如果在调用class时忘记使用<code>new</code>，JavaScript抛出异常：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> fred = <span class="keyword">new</span> Person(<span class="string">'Fred'</span>);</span><br><span class="line"><span class="comment">// ✅  If Person is a function: works fine</span></span><br><span class="line"><span class="comment">// ✅  If Person is a class: works fine too</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> george = Person(<span class="string">'George'</span>); <span class="comment">// We forgot `new`</span></span><br><span class="line"><span class="comment">// 😳 If Person is a constructor-like function: confusing behavior</span></span><br><span class="line"><span class="comment">// 🔴 If Person is a class: fails immediately</span></span><br></pre></td></tr></table></figure></p><p>这有助于我们尽早发现错误，而不是出现一些不符合预期的结果 比如<code>this.name</code>被视为<code>window.name</code>而不是<code>george.name</code>。</p><p>但是，这意味着<code>React</code>需要在调用任何class之前使用<code>new</code>。它不能只是将其作为普通函数直接调用，因为JavaScript会将其视为错误！<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Counter</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> &lt;p&gt;Hello&lt;/p&gt;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 🔴 React can't just do this:</span></span><br><span class="line"><span class="keyword">const</span> instance = Counter(props);</span><br></pre></td></tr></table></figure></p><p>这意味着麻烦(麻烦就是在于React需要区分Class和Function……)。</p><h2 id="探究React式如何解决的"><a href="#探究React式如何解决的" class="headerlink" title="探究React式如何解决的"></a>探究React式如何解决的</h2><blockquote><p>babel之类编译工具给解决问题带来的麻烦</p></blockquote><p>在我们探究React式如何解决这个问题时，需要考虑到大多数人都使用Babel之类的编译器来兼容浏览器（例如转义class等），所以我们需要在设计中考虑编译器这种情况。</p><p>在Babel的早期版本中，可以在没有<code>new</code>的情况下调用类。但是通过生成一些额外的代码，这个情况已经被修复了：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// A bit simplified from Babel output:</span></span><br><span class="line">  <span class="keyword">if</span> (!(<span class="keyword">this</span> <span class="keyword">instanceof</span> Person)) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">TypeError</span>(<span class="string">"Cannot call a class as a function"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// Our code:</span></span><br><span class="line">  <span class="keyword">this</span>.name = name;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> Person(<span class="string">'Fred'</span>); <span class="comment">// ✅ Okay</span></span><br><span class="line">Person(<span class="string">'George'</span>);   <span class="comment">// 🔴 Can’t call class as a function</span></span><br></pre></td></tr></table></figure><p>你或许在打包文件中看到类似的代码，这就是<code>_classCallCheck</code>函数所做的功能。 （您可以通过设置“loose mode”不进行检查来减小捆绑包大小，但这可能会使代码最终转换为真正的原生类变得复杂。）</p><p>到现在为止，你应该大致了解使用<code>new</code>与不使用<code>new</code>来调用某些内容之间的区别：</p><table><thead><tr><th></th><th><code>new Person()</code></th><th><code>Person()</code></th></tr></thead><tbody><tr><td><code>class</code></td><td>✅ <code>this</code> is a <code>Person</code> instance</td><td>🔴 <code>TypeError</code></td></tr><tr><td><code>function</code></td><td>✅ <code>this</code> is a <code>Person</code> instance</td><td>😳 <code>this</code> is <code>window</code> or <code>undefined</code></td></tr></tbody></table><p>这之中的区别就是React为什么需要正确调用组件的重要原因。 <strong>如果您的组件被定义为类，React在调用它时需要使用<code>new</code>。</strong></p><p><strong>那么问题来了 React是否可以判断某个东西是不是一个class？</strong></p><p>没有那么容易！即使我们可以<a href="https://stackoverflow.com/questions/29093396/how-do-you-check-the-difference-between-an-ecmascript-6-class-and-function" target="_blank" rel="noopener">在JavaScript es6 中区别class 和 function</a>，这仍然不适用于像Babel这样的工具处理之后的class。因为对于浏览器来说，它们只是单纯的function而已（class被babel处理后）。 </p><hr><p>Okay，也许React可以在每次调用时使用<code>new</code>？不幸的是，这也并不总是奏效。</p><h3 id="异常情况一："><a href="#异常情况一：" class="headerlink" title="异常情况一："></a>异常情况一：</h3><p>作为一般function，使用<code>new</code>调用它们会为它们提供一个对象实例作为<code>this</code>。对于作为构造函数编写的函数（如上面的<code>Person</code>），它是理想的，但它会给函数组件带来混乱：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Greeting</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 我们不希望“this”在这里成为任何一种情况下的实例</span></span><br><span class="line">  <span class="keyword">return</span> &lt;p&gt;Hello&lt;/p&gt;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>虽然这种情况也是可以容忍的，但还有另外两个原因可以扼杀一直使用<code>new</code>的想法。</p><h3 id="异常情况二："><a href="#异常情况二：" class="headerlink" title="异常情况二："></a>异常情况二：</h3><p>第一个是箭头函数（未被babel编译时）会使<code>new</code>调用失效，使用<code>new</code>调用箭头函数会抛出一个异常<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> Greeting = <span class="function"><span class="params">()</span> =&gt;</span> &lt;p&gt;Hello&lt;<span class="regexp">/p&gt;;</span></span><br><span class="line"><span class="regexp">new Greeting(); /</span><span class="regexp">/ 🔴 Greeting is not a constructor</span></span><br></pre></td></tr></table></figure></p><p>这种情况时是有意的，并且遵循箭头函数的设计。箭头函数的主要优点之一是它们没有自己的<code>this</code>绑定 - 取而代之的是 <code>this</code>被绑定到最近的函数体中。<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Friends</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">const</span> friends = <span class="keyword">this</span>.props.friends;</span><br><span class="line">    <span class="keyword">return</span> friends.map(<span class="function"><span class="params">friend</span> =&gt;</span></span><br><span class="line">      &lt;Friend</span><br><span class="line">        <span class="comment">// `this` is resolved from the `render` method</span></span><br><span class="line">        size=&#123;<span class="keyword">this</span>.props.size&#125;</span><br><span class="line">        name=&#123;friend.name&#125;</span><br><span class="line">        key=&#123;friend.id&#125;</span><br><span class="line">      /&gt;</span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>Okay，所以<strong>箭头功能没有自己的<code>this</code>。</strong> 这意味着箭头函数无法成为构造者！</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> Person = <span class="function">(<span class="params">name</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 🔴 This wouldn’t make sense!</span></span><br><span class="line">  <span class="keyword">this</span>.name = name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>因此，JavaScript不允许使用<code>new</code>调用箭头函数。如果你这样做，只会产生错误。这类似于JavaScript不允许在没有<code>new</code>的情况下调用类的方式。</p><p>这很不错，但它也使我们在全部函数调用前添加new的计划失败。 React不可以在所有情况下调用new，因为它会破坏箭头函数！我们可以尝试通过缺少<code>prototype</code>来判断出箭头函数：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="function"><span class="params">()</span> =&gt;</span> &#123;&#125;).prototype <span class="comment">// undefined</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;&#125;).prototype <span class="comment">// &#123;constructor: f&#125;</span></span><br></pre></td></tr></table></figure></p><p>但是这个<a href="https://github.com/facebook/react/issues/4599#issuecomment-136562930" target="_blank" rel="noopener">不适用</a>于使用<code>babel</code>编译的函数。这可能不是什么大问题，但还有另一个原因让这种方法失败。</p><h3 id="异常情况三："><a href="#异常情况三：" class="headerlink" title="异常情况三："></a>异常情况三：</h3><p>我们不能总是使用<code>new</code>的另一个原因是，这样做不支持返回字符串或其他原始类型。<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Greeting</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">'Hello'</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Greeting(); <span class="comment">// ✅ 'Hello'</span></span><br><span class="line"><span class="keyword">new</span> Greeting(); <span class="comment">// 😳 Greeting &#123;&#125;</span></span><br></pre></td></tr></table></figure></p><p>这再次与<a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/new" target="_blank" rel="noopener"><code>new</code>的设计</a>奇怪表现有关。正如我们之前看到的那样，<code>new</code>告诉JavaScript引擎创建一个对象，在函数内部创建该对象，然后将该对象作为<code>new</code>的结果。<br>但是，JavaScript还允许使用<code>new</code>调用的函数通过返回一些其他对象来覆盖<code>new</code>的返回值。这可能对类似对象池这样的模式很有用：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Created lazily</span></span><br><span class="line"><span class="keyword">var</span> zeroVector = <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Vector</span>(<span class="params">x, y</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (x === <span class="number">0</span> &amp;&amp; y === <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (zeroVector !== <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="comment">// Reuse the same instance</span></span><br><span class="line">      <span class="keyword">return</span> zeroVector;</span><br><span class="line">    &#125;</span><br><span class="line">    zeroVector = <span class="keyword">this</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">this</span>.x = x;</span><br><span class="line">  <span class="keyword">this</span>.y = y;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> a = <span class="keyword">new</span> Vector(<span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line"><span class="keyword">var</span> b = <span class="keyword">new</span> Vector(<span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line"><span class="keyword">var</span> c = <span class="keyword">new</span> Vector(<span class="number">0</span>, <span class="number">0</span>); <span class="comment">// 😲 b === c</span></span><br></pre></td></tr></table></figure><p>但是，如果函数的返回值不是对象，<code>new</code>会完全忽略函数的返回值。如果你返回一个字符串或一个数字，就好像没有返回一样。<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Answer</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">42</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Answer(); <span class="comment">// ✅ 42</span></span><br><span class="line"><span class="keyword">new</span> Answer(); <span class="comment">// 😳 Answer &#123;&#125;</span></span><br></pre></td></tr></table></figure></p><p>当使用<code>new</code>调用函数时，无法从函数中读取原始返回值（如数字或字符串）。因此，如果React总是使用new，它将无法支持返回字符串类型的函数（组件）</p><p>这是不可接受的，所以我们得另寻他法。</p><h3 id="解决方式"><a href="#解决方式" class="headerlink" title="解决方式"></a>解决方式</h3><p><strong>到目前为止我们了解到了什么？</strong>  React需要用<code>new</code>调用类（兼容<code>Babel</code>情况），但它需要调用常规函数或箭头函数（兼容Babel）时不能使用<code>new</code>。同时并没有可靠的方法来区分它们。<br><strong>如果我们无法解决一个普遍问题，那么我们能解决一个更具体的问题吗？</strong></p><p>将Component定义为class时，你可能希望继承<code>React.Component</code>使用其内置方法（比如<code>this.setState()</code>）。<strong>那么我们可以只检测React.Component子类，而不是尝试检测所有class吗？</strong></p><p><em>剧透：这正是React所做的。</em></p><h4 id="prototype-与-proto"><a href="#prototype-与-proto" class="headerlink" title="prototype 与 __proto__"></a><code>prototype</code> 与 <code>__proto__</code></h4><p>也许，判断<code>Greeting</code>是否是React component class的一般方法是测试<code>Greeting.prototype instanceof React.Component</code>：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(B.prototype <span class="keyword">instanceof</span> A); <span class="comment">// true</span></span><br></pre></td></tr></table></figure></p><p>我知道你在想什么。刚刚发生了什么？！要回答这个问题，我们需要了解JavaScript原型。</p><p>你可能熟悉原型链。JavaScript中的每个对象都可能有一个“原型”。当我们编写<code>fred.sayHi（）</code>但<code>fred</code>对象没有<code>sayHi</code>属性时，我们在<code>fred</code>的原型上查找<code>sayHi</code>属性。如果我们在那里找不到它，我们会看看链中的下一个原型–<code>fred</code>的原型的原型。并以此类推。</p><p><strong>令人困惑的是，类或函数的prototype属性并不指向该值的原型。</strong> 我不是在开玩笑。<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(Person.prototype); <span class="comment">// 🤪 Not Person's prototype</span></span><br><span class="line"><span class="built_in">console</span>.log(Person.__proto__); <span class="comment">// 😳 Person's prototype</span></span><br></pre></td></tr></table></figure></p><p>所以“原型链”更像是<code>__proto __.__ proto __.__ proto__</code>而不是<code>prototype.prototype.prototype</code>。我花了许多年才了解到这一点。</p><p><strong>所有对象的 <code>__proto__</code> 都指向其构造器的prototype</strong> 函数或类的<code>prototype</code>属性就是这样一个东西<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = name;</span><br><span class="line">&#125;</span><br><span class="line">Person.prototype.sayHi = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  alert(<span class="string">'Hi, I am '</span> + <span class="keyword">this</span>.name);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> fred = <span class="keyword">new</span> Person(<span class="string">'Fred'</span>); <span class="comment">// Sets `fred.__proto__` to `Person.prototype`</span></span><br></pre></td></tr></table></figure></p><p>而<code>__proto__</code>链是JavaScript查找属性的方式：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">fred.sayHi();</span><br><span class="line"><span class="comment">// 1. Does fred have a sayHi property? No.</span></span><br><span class="line"><span class="comment">// 2. Does fred.__proto__ have a sayHi property? Yes. Call it!</span></span><br><span class="line"></span><br><span class="line">fred.toString();</span><br><span class="line"><span class="comment">// 1. Does fred have a toString property? No.</span></span><br><span class="line"><span class="comment">// 2. Does fred.__proto__ have a toString property? No.</span></span><br><span class="line"><span class="comment">// 3. Does fred.__proto__.__proto__ have a toString property? Yes. Call it!</span></span><br></pre></td></tr></table></figure></p><p>事实上，除非您正在调试与原型链相关的内容，否则您几乎不需要直接在代码中修改<code>__proto__</code>。如果你想在<code>fred .__ proto__</code>上添加东西，你应该把它放在<code>Person.prototype</code>上。它最初就是这么设计的。</p><p>甚至浏览器都不应该暴露<code>__proto__</code>属性，因为原型链被设计为一个内部概念。但是有些浏览器添加了<code>__proto__</code>，最终它被勉强标准化。</p><p>至今我仍然觉得“<code>prototype</code>的属性没有给你一个值的原型“非常令人困惑（例如，<code>fred.prototype</code>未定义，因为<code>fred</code>不是一个函数）。就个人而言，我认为这是导致经验丰富的开发人员也会误解JavaScript原型的最大原因。</p><h4 id="extends-与-原型链"><a href="#extends-与-原型链" class="headerlink" title="extends 与 原型链"></a>extends 与 原型链</h4><p>这帖子有点长 不是吗？别放弃！现在已经讲了80%的内容了，让我们继续吧</p><p>我们知道，当说调用<code>obj.foo</code>时，JavaScript实际上在<code>obj</code>，<code>obj .__ proto__</code>，<code>obj .__ proto __.__ proto__</code>中寻找<code>foo</code>，依此类推。</p><p>对于类来说原型链机制会更加复杂，但<code>extends</code>会使类完美适用原型链机制。这也是React类的实例访问<code>setState</code>之类的方法的原理：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Greeting</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> &lt;p&gt;Hello&lt;/p&gt;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> c = <span class="keyword">new</span> Greeting();</span><br><span class="line"><span class="built_in">console</span>.log(c.__proto__); <span class="comment">// Greeting.prototype</span></span><br><span class="line"><span class="built_in">console</span>.log(c.__proto__.__proto__); <span class="comment">// React.Component.prototype</span></span><br><span class="line"><span class="built_in">console</span>.log(c.__proto__.__proto__.__proto__); <span class="comment">// Object.prototype</span></span><br><span class="line"></span><br><span class="line">c.render();      <span class="comment">// Found on c.__proto__ (Greeting.prototype)</span></span><br><span class="line">c.setState();    <span class="comment">// Found on c.__proto__.__proto__ (React.Component.prototype)</span></span><br><span class="line">c.toString();    <span class="comment">// Found on c.__proto__.__proto__.__proto__ (Object.prototype)</span></span><br></pre></td></tr></table></figure></p><p>换句话说，类实例的<code>__protp__</code>链会镜像拷贝类的继承关系：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// `extends` chain</span></span><br><span class="line">Greeting</span><br><span class="line">  → React.Component</span><br><span class="line">    → <span class="built_in">Object</span> (implicitly)</span><br><span class="line"></span><br><span class="line"><span class="comment">// `__proto__` chain</span></span><br><span class="line"><span class="keyword">new</span> Greeting()</span><br><span class="line">  → Greeting.prototype</span><br><span class="line">    → React.Component.prototype</span><br><span class="line">      → <span class="built_in">Object</span>.prototype</span><br></pre></td></tr></table></figure></p><p>如此两个链（继承链 原型链）</p><h4 id="instanceof-判断方式"><a href="#instanceof-判断方式" class="headerlink" title="instanceof 判断方式"></a>instanceof 判断方式</h4><p>由于<code>__proto__</code>链镜像拷贝类的继承关系，因此我们可以通过<code>Greeting</code>的原型链来判断<code>Greeting</code>是否继承了<code>React.Component</code>：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// `__proto__` chain</span></span><br><span class="line"><span class="keyword">new</span> Greeting()</span><br><span class="line">  → Greeting.prototype <span class="comment">// 🕵️ We start here</span></span><br><span class="line">    → React.Component.prototype <span class="comment">// ✅ Found it!</span></span><br><span class="line">      → <span class="built_in">Object</span>.prototype</span><br></pre></td></tr></table></figure></p><p>方便的是，<code>x instanceof Y</code>就是相同的搜索原理。它在<code>x .__ proto__</code>链中寻找是否有<code>Y.prototype</code>存在。</p><p>通常，它用于确定某些东西是否是类的实例：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> greeting = <span class="keyword">new</span> Greeting();</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(greeting <span class="keyword">instanceof</span> Greeting); <span class="comment">// true</span></span><br><span class="line"><span class="comment">// greeting (🕵️‍ We start here)</span></span><br><span class="line"><span class="comment">//   .__proto__ → Greeting.prototype (✅ Found it!)</span></span><br><span class="line"><span class="comment">//     .__proto__ → React.Component.prototype </span></span><br><span class="line"><span class="comment">//       .__proto__ → Object.prototype</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(greeting <span class="keyword">instanceof</span> React.Component); <span class="comment">// true</span></span><br><span class="line"><span class="comment">// greeting (🕵️‍ We start here)</span></span><br><span class="line"><span class="comment">//   .__proto__ → Greeting.prototype</span></span><br><span class="line"><span class="comment">//     .__proto__ → React.Component.prototype (✅ Found it!)</span></span><br><span class="line"><span class="comment">//       .__proto__ → Object.prototype</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(greeting <span class="keyword">instanceof</span> <span class="built_in">Object</span>); <span class="comment">// true</span></span><br><span class="line"><span class="comment">// greeting (🕵️‍ We start here)</span></span><br><span class="line"><span class="comment">//   .__proto__ → Greeting.prototype</span></span><br><span class="line"><span class="comment">//     .__proto__ → React.Component.prototype</span></span><br><span class="line"><span class="comment">//       .__proto__ → Object.prototype (✅ Found it!)</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(greeting <span class="keyword">instanceof</span> Banana); <span class="comment">// false</span></span><br><span class="line"><span class="comment">// greeting (🕵️‍ We start here)</span></span><br><span class="line"><span class="comment">//   .__proto__ → Greeting.prototype</span></span><br><span class="line"><span class="comment">//     .__proto__ → React.Component.prototype </span></span><br><span class="line"><span class="comment">//       .__proto__ → Object.prototype (🙅‍ Did not find it!)</span></span><br></pre></td></tr></table></figure></p><p>但它也可以用于确定一个类是否继承另一个类：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(Greeting.prototype <span class="keyword">instanceof</span> React.Component);</span><br><span class="line"><span class="comment">// greeting</span></span><br><span class="line"><span class="comment">//   .__proto__ → Greeting.prototype (🕵️‍ We start here)</span></span><br><span class="line"><span class="comment">//     .__proto__ → React.Component.prototype (✅ Found it!)</span></span><br><span class="line"><span class="comment">//       .__proto__ → Object.prototype</span></span><br></pre></td></tr></table></figure></p><p>这种判断方式就是是我们如何确定某些东西是React组件类还是一般函数。</p><h4 id="React-判断方式"><a href="#React-判断方式" class="headerlink" title="React 判断方式"></a>React 判断方式</h4><p>但这并不是React所做的。 😳</p><p><code>instanceof</code>解决方案的一个隐患是：当页面上有多个React副本时，我们正在检查的组件可能继承自另一个React副本的React.Component，这种instanceof方式就会失效。<br>在一个项目中混合使用React的多个副本是不好的方式，但我们应该尽可能避免出现由于历史遗留所产生的这种问题。 （使用Hooks，我们<a href="https://github.com/facebook/react/issues/13991" target="_blank" rel="noopener">可能需要</a>强制删除重复数据。）</p><p>另一种可能的骚操作是检查原型上是否存在<code>render</code>方法。但是，当时还<a href="https://github.com/facebook/react/issues/4599#issuecomment-129714112" target="_blank" rel="noopener">不清楚</a>组件API将如何变换。每个判断操作都有成本我们不想添加多于一次的操作。如果将render定义为实例方法（例如使用类属性语法），这也不起作用。</p><p>因此，React为基本组件<a href="https://github.com/facebook/react/pull/4663" target="_blank" rel="noopener">添加</a>了一个特殊标志。React通过检查是该标志来判断一个东西是否是React组件类。</p><p>最初该标志在React.Component基础类本身上：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Inside React</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Component</span> </span>&#123;&#125;</span><br><span class="line">Component.isReactClass = &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// We can check it like this</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Greeting</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;&#125;</span><br><span class="line"><span class="built_in">console</span>.log(Greeting.isReactClass); <span class="comment">// ✅ Yes</span></span><br></pre></td></tr></table></figure></p><p>但是，我们想要判断的一些类实现没有<a href="https://github.com/scala-js/scala-js/issues/1900" target="_blank" rel="noopener">复制</a>静态属性（或设置<code>__proto__</code>），因此标志丢失了。</p><p>这就是React将此标志移动到<code>React.Component.prototype</code>的原因：<br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Inside React</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Component</span> </span>&#123;&#125;</span><br><span class="line">Component.prototype.isReactComponent = &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// We can check it like this</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Greeting</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;&#125;</span><br><span class="line"><span class="built_in">console</span>.log(Greeting.prototype.isReactComponent); <span class="comment">// ✅ Yes</span></span><br></pre></td></tr></table></figure></p><p><strong>这就是React如何判断class的全部内容。</strong></p><hr><p>如今在React中使用就是<code>isReactComponent</code>标志检查。</p><p>如果不扩展React.Component，React将不会在原型上找到<code>isReactComponent</code>，也不会将组件视为类。现在你知道为什么<code>Cannot call a class as a function</code>问题<a href="https://stackoverflow.com/questions/38481857/getting-cannot-call-a-class-as-a-function-in-my-react-project/42680526#42680526" target="_blank" rel="noopener">最受欢迎的回答</a>是添加<code>extends React.Component</code>。最后，添加了一个<code>prototype.render</code>存在时，但<code>prototype.isReactComponent</code>不存在的<a href="https://github.com/facebook/react/pull/11168" target="_blank" rel="noopener">警告</a>。</p><p><strong>实际的解决方案非常简单，但我用大量的时间解释了为什么React最终采取这个解决方案，以及替代方案是什么。</strong> 你可能觉得博文这个解释过程有点啰嗦，</p><p>根据我的经验，开发库API经常会遇到这种情况。为了使API易于使用，开发者需要考虑语言语义（可能，对于多种语言，包括未来的方向）、运行时性能、是否编译情况的兼容、完整体系和打包解决方案的状态、 早期预警和许多其他事情。 最终结果可能并不优雅，但必须实用。</p><p><strong>如果最终API是成功的，则用户永远不必考虑此过程。</strong> 取而代之的是他们只需要专注于创建应用程序。</p><p>但如果你也好奇……去探究其中的原因还是十分有趣的。</p>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;原文地址: &lt;a href=&quot;https://overreacted.io/how-does-react-tell-a-class-from-a-function/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;how-does-react-tell-a-class-from-a-function&lt;/a&gt;&lt;br&gt;本文地址： &lt;a href=&quot;https://developerdoc.com/essay/(译)how-does-react-tell-a-class-from-a-function/&quot;&gt;React是如何区分Class和Function？&lt;/a&gt;&lt;br&gt;边看边翻译 花了2h+… 如果你觉得读起来还算通顺不费事 那也算我为大家做了一点小贡献吧&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="essay" scheme="https://developerdoc.com/categories/essay/"/>
    
      <category term="译文" scheme="https://developerdoc.com/categories/essay/%E8%AF%91%E6%96%87/"/>
    
    
      <category term="前端" scheme="https://developerdoc.com/tags/%E5%89%8D%E7%AB%AF/"/>
    
  </entry>
  
  <entry>
    <title>GitHub精选分享第二期</title>
    <link href="https://developerdoc.com/weekly-share/GitHub%E7%B2%BE%E9%80%89%E5%88%86%E4%BA%AB%E7%AC%AC%E4%BA%8C%E6%9C%9F/"/>
    <id>https://developerdoc.com/weekly-share/GitHub精选分享第二期/</id>
    <published>2018-11-30T03:00:00.000Z</published>
    <updated>2018-12-04T14:38:03.777Z</updated>
    
    <content type="html"><![CDATA[<p>记录这周值得分享的事，主要是GitHub精选 前端相关~</p><h3 id="edex-ui，一个到随处运行的科幻桌面"><a href="#edex-ui，一个到随处运行的科幻桌面" class="headerlink" title="edex-ui，一个到随处运行的科幻桌面"></a><a href="https://github.com/GitSquared/edex-ui" target="_blank" rel="noopener">edex-ui，一个到随处运行的科幻桌面</a></h3><p><a href="https://github.com/GitSquared/edex-ui" target="_blank" rel="noopener"><img src="https://github.com/GitSquared/edex-ui/raw/master/media/screenshot_default.png"></a></p><blockquote class="blockquote-center"><p>eDEX-UI 是一个全屏桌面应用程序，类似于科幻电脑界面，<br>其灵感来自于DEX-UI和TRON Legacy电影特效。<br>它在真实终端中运行您选择的shell，并显示有关系统的实时信息。</p></blockquote><a id="more"></a><h3 id="Howler-js-JavaScript-开发跨平台音频相关的开发"><a href="#Howler-js-JavaScript-开发跨平台音频相关的开发" class="headerlink" title="Howler.js - JavaScript 开发跨平台音频相关的开发 "></a><a href="https://howlerjs.com/" target="_blank" rel="noopener">Howler.js - JavaScript 开发跨平台音频相关的开发 </a></h3><p><a href="https://howlerjs.com/" target="_blank" rel="noopener"><img src="https://howlerjs.com/assets/images/logo-big.svg" width="50%"></a></p><blockquote class="blockquote-center"><p>Howler.js提供了一个现代音频库，支持Web Audio API和HTML5 Audio的回退机制。<br>该项目致力于简化与使用 JavaScript 开发跨平台音频相关的开发工作。<br>几家大型技术和媒体公司正在使用 Howler.js，包括谷歌、迪士尼、Mozilla 和乐高。</p></blockquote><h3 id="NES-css-一款像素风格css库"><a href="#NES-css-一款像素风格css库" class="headerlink" title="NES.css - 一款像素风格css库"></a><a href="https://github.com/BcRikko/NES.css" target="_blank" rel="noopener">NES.css - 一款像素风格css库</a></h3><p><a href="https://github.com/BcRikko/NES.css" target="_blank" rel="noopener"><img src="https://user-images.githubusercontent.com/5305599/49061716-da649680-f254-11e8-9a89-d95a7407ec6a.png" alt=""></a></p><h3 id="33-js-concepts-每个-JavaScript-工程师都应懂的33个概念"><a href="#33-js-concepts-每个-JavaScript-工程师都应懂的33个概念" class="headerlink" title="33-js-concepts  每个 JavaScript 工程师都应懂的33个概念"></a><a href="https://github.com/stephentian/33-js-concepts" target="_blank" rel="noopener">33-js-concepts  每个 JavaScript 工程师都应懂的33个概念</a></h3><p><a href="https://github.com/stephentian/33-js-concepts" target="_blank" rel="noopener"><img src="https://github.com/stephentian/33-js-concepts/raw/master/33_js_concepts.jpg" alt=""></a></p><blockquote class="blockquote-center"><p>这个项目是为了帮助开发者掌握 JavaScript 概念而创立的。<br>在学习（JavaScript）中，可以作为一篇指南。<br>从 调用堆栈 / 原始类型 到 设计模式/代码整洁之道</p></blockquote><h3 id="30-seconds-of-code-有趣的JS代码片段集合"><a href="#30-seconds-of-code-有趣的JS代码片段集合" class="headerlink" title="30-seconds-of-code 有趣的JS代码片段集合"></a><a href="https://github.com/30-seconds/30-seconds-of-code" target="_blank" rel="noopener">30-seconds-of-code 有趣的JS代码片段集合</a></h3><p><a href="https://github.com/30-seconds/30-seconds-of-code" target="_blank" rel="noopener"><img src="https://github.com/30-seconds/30-seconds-of-code/raw/master/logo.png" alt=""></a></p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//摘取一段 ： Adapter-ary</span></span><br><span class="line"><span class="comment">//Creates a function that accepts up to n arguments, ignoring any additional arguments.</span></span><br><span class="line"><span class="comment">//Call the provided function, fn, with up to n arguments, </span></span><br><span class="line"><span class="comment">//using Array.prototype.slice(0,n) and the spread operator (...).</span></span><br><span class="line"><span class="keyword">const</span> ary = <span class="function">(<span class="params">fn, n</span>) =&gt;</span> (...args) =&gt; fn(...args.slice(<span class="number">0</span>, n));</span><br></pre></td></tr></table></figure><h3 id="scrcpy，-Android屏幕分享与控制"><a href="#scrcpy，-Android屏幕分享与控制" class="headerlink" title="scrcpy， Android屏幕分享与控制"></a><a href="https://github.com/Genymobile/scrcpy" target="_blank" rel="noopener">scrcpy， Android屏幕分享与控制</a></h3><p><a href="https://github.com/Genymobile/scrcpy" target="_blank" rel="noopener"><img src="https://github.com/Genymobile/scrcpy/raw/master/assets/screenshot-debian-600.jpg"></a></p><blockquote class="blockquote-center"><p>由 Genymobile 推出的可跨平台的、可自定义码率的、开源的屏幕共享工具。<br>提供了在 USB (或 TCP/IP )上连接的 Android 设备的显示和控制，<br>适用于 GNU/Linux、Windows 和 MacOS 。</p></blockquote><h3 id="css-animation-101，-css动画示例"><a href="#css-animation-101，-css动画示例" class="headerlink" title="css-animation-101， css动画示例"></a><a href="https://github.com/cssanimation/css-animation-101" target="_blank" rel="noopener">css-animation-101， css动画示例</a></h3><p><a href="https://github.com/cssanimation/css-animation-101" target="_blank" rel="noopener"><img src="https://github.com/cssanimation/css-animation-101/raw/master/images/cover-small.png" alt=""></a></p><blockquote class="blockquote-center"><p>从零开始介绍 CSS 动画开源电子书<br><a href="https://cssanimation.rocks/" target="_blank" rel="noopener">https://cssanimation.rocks/</a></p></blockquote><h3 id="Dan-Abramov-个人博客"><a href="#Dan-Abramov-个人博客" class="headerlink" title="Dan Abramov 个人博客"></a><a href="https://overreacted.io/" target="_blank" rel="noopener">Dan Abramov 个人博客</a></h3><blockquote class="blockquote-center"><p>Dan Abramov 参与开发过react, redux create-react-app的作者之一</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;记录这周值得分享的事，主要是GitHub精选 前端相关~&lt;/p&gt;
&lt;h3 id=&quot;edex-ui，一个到随处运行的科幻桌面&quot;&gt;&lt;a href=&quot;#edex-ui，一个到随处运行的科幻桌面&quot; class=&quot;headerlink&quot; title=&quot;edex-ui，一个到随处运行的科幻桌面&quot;&gt;&lt;/a&gt;&lt;a href=&quot;https://github.com/GitSquared/edex-ui&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;edex-ui，一个到随处运行的科幻桌面&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;&lt;a href=&quot;https://github.com/GitSquared/edex-ui&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;&lt;img src=&quot;https://github.com/GitSquared/edex-ui/raw/master/media/screenshot_default.png&quot;&gt;&lt;/a&gt;&lt;/p&gt;
&lt;blockquote class=&quot;blockquote-center&quot;&gt;&lt;p&gt;eDEX-UI 是一个全屏桌面应用程序，类似于科幻电脑界面，&lt;br&gt;其灵感来自于DEX-UI和TRON Legacy电影特效。&lt;br&gt;它在真实终端中运行您选择的shell，并显示有关系统的实时信息。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="每周分享" scheme="https://developerdoc.com/categories/%E6%AF%8F%E5%91%A8%E5%88%86%E4%BA%AB/"/>
    
    
  </entry>
  
  <entry>
    <title>create-react-app教程-源码篇</title>
    <link href="https://developerdoc.com/quick-start/create-react-app%E6%95%99%E7%A8%8B-%E6%BA%90%E7%A0%81%E7%AF%87/"/>
    <id>https://developerdoc.com/quick-start/create-react-app教程-源码篇/</id>
    <published>2018-11-29T14:34:15.000Z</published>
    <updated>2019-03-06T03:20:09.935Z</updated>
    
    <content type="html"><![CDATA[<blockquote class="blockquote-center"><p>之前在 <a href="../create-react-app教程-使用篇/">create-react-app教程-使用篇</a> 中介绍了create-react-app的基本使用，<br>为了便于理解一个脚手架脚本是如何运作的，现在来看一下 create-react-app v1.5.2 的源码</p></blockquote><h2 id="入口index-js"><a href="#入口index-js" class="headerlink" title="入口index.js"></a>入口index.js</h2><p>create-react-app 一般会作为全局命令，因为便于更新等原因，create-react-app 只会做初始化仓库 执行当前版本命令等操作。</p><p>找到 <code>create-react-app</code> 入口index文件：<br><a id="more"></a><br><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">'use strict'</span>;</span><br><span class="line"><span class="keyword">var</span> chalk = <span class="built_in">require</span>(<span class="string">'chalk'</span>);</span><br><span class="line"><span class="comment">// 返回Node版本信息，如果有多个版本返回多个版本</span></span><br><span class="line"><span class="keyword">var</span> currentNodeVersion = process.versions.node; </span><br><span class="line"><span class="keyword">var</span> semver = currentNodeVersion.split(<span class="string">'.'</span>);</span><br><span class="line"><span class="keyword">var</span> major = semver[<span class="number">0</span>];<span class="comment">// 取出第一个Node版本信息</span></span><br><span class="line"><span class="comment">//小于 4.x的提示并终止程序</span></span><br><span class="line"><span class="keyword">if</span> (major &lt; <span class="number">4</span>) &#123;</span><br><span class="line">  <span class="built_in">console</span>.error(</span><br><span class="line">    chalk.red(</span><br><span class="line">      <span class="string">'You are running Node '</span> +</span><br><span class="line">        currentNodeVersion +</span><br><span class="line">        <span class="string">'.\n'</span> +</span><br><span class="line">        <span class="string">'Create React App requires Node 4 or higher. \n'</span> +</span><br><span class="line">        <span class="string">'Please update your version of Node.'</span></span><br><span class="line">    )</span><br><span class="line">  );</span><br><span class="line">  process.exit(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 没有小于4就引入以下文件继续执行</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">'./createReactApp'</span>);</span><br></pre></td></tr></table></figure></p><p>可以看到 index 文件没有做什么，只是做为一个入口文件判断一下 node版本，小于 4.x的提示并终止程序， 如果正常则加载 ./createReactApp 这个文件，主要的逻辑在该文件实现。</p><h2 id="createReactApp-js"><a href="#createReactApp-js" class="headerlink" title="createReactApp.js"></a>createReactApp.js</h2><p>虽然 createReactApp.js 有751行，但是里面有一大半是注释和错误友好信息。</p><p>除了声明的依赖。跟着执行顺序先看到的是第56行 <code>program</code>  ，</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> program = <span class="keyword">new</span> commander.Command(packageJson.name)</span><br><span class="line">  .version(packageJson.version)<span class="comment">// create-react-app -v 时输出 $&#123;packageJson.version&#125;</span></span><br><span class="line">  .arguments(<span class="string">'&lt;project-directory&gt;'</span>)<span class="comment">// 这里用&lt;&gt; 包着project-directory 表示 project-directory为必填项</span></span><br><span class="line">  .usage(<span class="string">`<span class="subst">$&#123;chalk.green(<span class="string">'&lt;project-directory&gt;'</span>)&#125;</span> [options]`</span>)<span class="comment">// 用绿色字体输出 &lt;project-directory&gt;</span></span><br><span class="line">  .action(<span class="function"><span class="params">name</span> =&gt;</span> &#123;</span><br><span class="line">    projectName = name;</span><br><span class="line">  &#125;)<span class="comment">// 获取用户传入的第一个参数作为 projectName</span></span><br><span class="line">  .option(<span class="string">'--verbose'</span>, <span class="string">'print additional logs'</span>)</span><br><span class="line">  <span class="comment">// option用于配置`create-react-app -[option]`的选项，</span></span><br><span class="line">  <span class="comment">//比如这里如果用户参数带了 --verbose， 会自动设置program.verbose = true;</span></span><br><span class="line">  .option(<span class="string">'--info'</span>, <span class="string">'print environment debug info'</span>)</span><br><span class="line">  <span class="comment">// info，用于打印出环境调试的版本信息</span></span><br><span class="line">  .option(</span><br><span class="line">    <span class="string">'--scripts-version &lt;alternative-package&gt;'</span>,</span><br><span class="line">    <span class="string">'use a non-standard version of react-scripts'</span></span><br><span class="line">  )</span><br><span class="line">  .option(<span class="string">'--use-npm'</span>)<span class="comment">// 默认使用`yarn`，指定使用`npm`</span></span><br><span class="line">  .allowUnknownOption()</span><br><span class="line">  .on(<span class="string">'--help'</span>, () =&gt; &#123;</span><br><span class="line">    <span class="comment">//help 信息</span></span><br><span class="line">  &#125;)</span><br><span class="line">  .parse(process.argv);<span class="comment">// 解析传入的参数</span></span><br></pre></td></tr></table></figure><p>这里用到 <code>commander</code> 的依赖，这是 node.js 命令行接口的解决方案，正如我们所看到的 处理用户输入的参数，输出友好的提示信息等。</p><p>接着到了第109行：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//没有输入projectName的话,输出一些提示信息就终止程序</span></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">typeof</span> projectName === <span class="string">'undefined'</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (program.info) &#123;<span class="comment">// 如果参数输入了 --info,就会进入这里</span></span><br><span class="line">    envinfo.print(&#123;<span class="comment">// envinfo 是一个用来输出当前环境系统的而一些系统信息</span></span><br><span class="line">      packages: [<span class="string">'react'</span>, <span class="string">'react-dom'</span>, <span class="string">'react-scripts'</span>],</span><br><span class="line">      noNativeIDE: <span class="literal">true</span>,</span><br><span class="line">      duplicates: <span class="literal">true</span>,</span><br><span class="line">    &#125;);</span><br><span class="line">    process.exit(<span class="number">0</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//略去部分log...</span></span><br><span class="line">  process.exit(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里的 projectName 就是我们要创建的web应用名称，如果没有输入的话,输出一些提示信息就终止程序。</p><h3 id="createApp-检测判断"><a href="#createApp-检测判断" class="headerlink" title="createApp 检测判断"></a>createApp 检测判断</h3><p>然后到了第148行 执行<code>createApp</code>：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br></pre></td><td class="code"><pre><span class="line">createApp(</span><br><span class="line">  projectName,<span class="comment">//项目名称 </span></span><br><span class="line">  program.verbose, <span class="comment">//是否暑促额外信息</span></span><br><span class="line">  program.scriptsVersion, <span class="comment">//传入的脚本版本</span></span><br><span class="line">  program.useNpm, <span class="comment">//是否使用npm</span></span><br><span class="line">  hiddenProgram.internalTestingTemplate <span class="comment">//调试的模板路径，这个不管它，给开发人员调试用的……</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">createApp</span>(<span class="params">name, verbose, version, useNpm, template</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> root = path.resolve(name);<span class="comment">// 获取当前进程运行的位置，也就是文件目录的绝对路径</span></span><br><span class="line">  <span class="keyword">const</span> appName = path.basename(root);<span class="comment">// 返回root路径下最后一部分</span></span><br><span class="line"></span><br><span class="line">  checkAppName(appName);<span class="comment">// 检查传入的项目名合法性</span></span><br><span class="line">  fs.ensureDirSync(name);<span class="comment">//这里的 fs = require('fs-extra');</span></span><br><span class="line">  <span class="keyword">if</span> (!isSafeToCreateProjectIn(root, name)) &#123;</span><br><span class="line">    process.exit(<span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 写入 package.json 文件</span></span><br><span class="line">  <span class="keyword">const</span> packageJson = &#123;</span><br><span class="line">    name: appName,</span><br><span class="line">    version: <span class="string">'0.1.0'</span>,</span><br><span class="line">    private: <span class="literal">true</span>,</span><br><span class="line">  &#125;;</span><br><span class="line">  fs.writeFileSync(</span><br><span class="line">    path.join(root, <span class="string">'package.json'</span>),</span><br><span class="line">    <span class="built_in">JSON</span>.stringify(packageJson, <span class="literal">null</span>, <span class="number">2</span>)</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> useYarn = useNpm ? <span class="literal">false</span> : shouldUseYarn();</span><br><span class="line">  <span class="keyword">const</span> originalDirectory = process.cwd();</span><br><span class="line">  process.chdir(root);<span class="comment">// 在这里就把进程目录修改为了我们创建的目录</span></span><br><span class="line">  <span class="comment">// 如果是使用npm，检查npm是否能正常执行</span></span><br><span class="line">  <span class="keyword">if</span> (!useYarn &amp;&amp; !checkThatNpmCanReadCwd()) &#123;</span><br><span class="line">    process.exit(<span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//这里的 semver = require('semver'); 做版本处理的</span></span><br><span class="line">  <span class="comment">//如果node版本不符合要求就使用旧版本的 react-scripts</span></span><br><span class="line">  <span class="keyword">if</span> (!semver.satisfies(process.version, <span class="string">'&gt;=6.0.0'</span>)) &#123;</span><br><span class="line">    <span class="comment">//略去log信息...</span></span><br><span class="line">    <span class="comment">// Fall back to latest supported react-scripts on Node 4</span></span><br><span class="line">    version = <span class="string">'react-scripts@0.9.x'</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 如果npm版本小于3.x，使用旧版的 react-scripts</span></span><br><span class="line">  <span class="keyword">if</span> (!useYarn) &#123;</span><br><span class="line">    <span class="keyword">const</span> npmInfo = checkNpmVersion();</span><br><span class="line">    <span class="keyword">if</span> (!npmInfo.hasMinNpm) &#123;</span><br><span class="line">      <span class="comment">//略去log信息...</span></span><br><span class="line">      <span class="comment">// Fall back to latest supported react-scripts for npm 3</span></span><br><span class="line">      version = <span class="string">'react-scripts@0.9.x'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 判断结束之后，执行 run 方法</span></span><br><span class="line">  run(root, appName, version, verbose, originalDirectory, template, useYarn);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以了解到 createApp 主要做的事情就是做一些安全判断比如：检查项目名是否合法，检查新建的话是否安全，检查npm版本，处理react-script的版本兼容。然后看下在<code>createApp</code>中用到的 <code>checkAppName</code></p><h4 id="checkAppName-检查项目名"><a href="#checkAppName-检查项目名" class="headerlink" title="checkAppName 检查项目名"></a>checkAppName 检查项目名</h4><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">checkAppName</span>(<span class="params">appName</span>) </span>&#123;</span><br><span class="line">  <span class="comment">//这里 validateProjectName = require('validate-npm-package-name');</span></span><br><span class="line">  <span class="comment">//可以用来判断当前的项目名是否符合npm规范 比如不能大写等</span></span><br><span class="line">  <span class="keyword">const</span> validationResult = validateProjectName(appName);</span><br><span class="line">  <span class="comment">// 判断是否符合npm规范如果不符合，输出提示并结束任务</span></span><br><span class="line">  <span class="keyword">if</span> (!validationResult.validForNewPackages) &#123;</span><br><span class="line">    <span class="comment">//略去log信息...</span></span><br><span class="line">    process.exit(<span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> dependencies = [<span class="string">'react'</span>, <span class="string">'react-dom'</span>, <span class="string">'react-scripts'</span>].sort();</span><br><span class="line">  <span class="comment">// 判断是否重名，如果重名则输出提示并结束任务</span></span><br><span class="line">  <span class="keyword">if</span> (dependencies.indexOf(appName) &gt;= <span class="number">0</span>) &#123;    </span><br><span class="line">    <span class="comment">//略去log信息...</span></span><br><span class="line">    process.exit(<span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="run-安装依赖拷贝模版"><a href="#run-安装依赖拷贝模版" class="headerlink" title="run 安装依赖拷贝模版"></a>run 安装依赖拷贝模版</h3><p>在 createApp 方法体内调用了run方法，run方法体内完成主要的安装依赖 拷贝模板等功能。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">run</span>(<span class="params">root,appName,version,verbose,originalDirectory,template,useYarn</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 这里获取要安装的package，</span></span><br><span class="line">  <span class="comment">//    getInstallPackage 默认情况下packageToInstall是 `react-scripts`。</span></span><br><span class="line">  <span class="comment">//    也可能是根据去本地拿到对应的package</span></span><br><span class="line">  <span class="comment">//    react-scripts是一系列的webpack配置与模版</span></span><br><span class="line">  <span class="keyword">const</span> packageToInstall = getInstallPackage(version, originalDirectory);</span><br><span class="line">  <span class="comment">// 需要安装所有的依赖</span></span><br><span class="line">  <span class="keyword">const</span> allDependencies = [<span class="string">'react'</span>, <span class="string">'react-dom'</span>, packageToInstall];</span><br><span class="line"></span><br><span class="line">  <span class="comment">// getPackageName 获取依赖包原始名称并返回</span></span><br><span class="line">  getPackageName(packageToInstall)</span><br><span class="line">    .then(<span class="function"><span class="params">packageName</span> =&gt;</span></span><br><span class="line">      <span class="comment">// 如果是yarn,判断是否在线模式（对应的就是离线模式），处理完判断就返回给下一个then处理</span></span><br><span class="line">      checkIfOnline(useYarn).then(<span class="function"><span class="params">isOnline</span> =&gt;</span> (&#123;</span><br><span class="line">        isOnline: isOnline,</span><br><span class="line">        packageName: packageName,</span><br><span class="line">      &#125;))</span><br><span class="line">    )</span><br><span class="line">    .then(<span class="function"><span class="params">info</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">const</span> isOnline = info.isOnline;</span><br><span class="line">      <span class="keyword">const</span> packageName = info.packageName;</span><br><span class="line">      <span class="comment">//略去log信息...      </span></span><br><span class="line">      <span class="comment">//传参数给install 负责安装 allDependencies</span></span><br><span class="line">      <span class="keyword">return</span> install(root, useYarn, allDependencies, verbose, isOnline).then(</span><br><span class="line">        () =&gt; packageName</span><br><span class="line">      );</span><br><span class="line">    &#125;)</span><br><span class="line">    .then(<span class="function"><span class="params">packageName</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="comment">//检查当前环境运行的node版本是否符合要求</span></span><br><span class="line">      checkNodeVersion(packageName);</span><br><span class="line">      <span class="comment">//修改react, react-dom的版本信息，将准确版本信息改为高于等于版本</span></span><br><span class="line">      <span class="comment">//    例如 15.0.0 =&gt; ^15.0.0</span></span><br><span class="line">      setCaretRangeForRuntimeDeps(packageName);</span><br><span class="line">      <span class="comment">// `react-scripts`脚本的目录</span></span><br><span class="line">      <span class="keyword">const</span> scriptsPath = path.resolve(</span><br><span class="line">        process.cwd(),</span><br><span class="line">        <span class="string">'node_modules'</span>,</span><br><span class="line">        packageName,</span><br><span class="line">        <span class="string">'scripts'</span>,</span><br><span class="line">        <span class="string">'init.js'</span></span><br><span class="line">      );</span><br><span class="line">      <span class="keyword">const</span> init = <span class="built_in">require</span>(scriptsPath);</span><br><span class="line">      <span class="comment">//调用安装了的 react-scripts/script/init 去拷贝模版</span></span><br><span class="line">      init(root, appName, verbose, originalDirectory, template);</span><br><span class="line">      <span class="comment">//略去log信息...  </span></span><br><span class="line">    &#125;)</span><br><span class="line">    </span><br><span class="line">    .catch(<span class="function"><span class="params">reason</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="comment">// 出错的话，把安装了的文件全删了 并输出一些日志信息等</span></span><br><span class="line">      <span class="comment">// 错误处理 略</span></span><br><span class="line">      process.exit(<span class="number">1</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以猜到其中最重要的逻辑是 <code>install</code> 安装依赖和 <code>init</code> 拷贝模板。</p><h3 id="install-安装依赖"><a href="#install-安装依赖" class="headerlink" title="install  安装依赖"></a>install  安装依赖</h3><p>install 方法体中是根据参数拼装命令行，然后用node去跑安装脚本 ，执行完成后返回一个 Promise</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">install</span>(<span class="params">root, useYarn, dependencies, verbose, isOnline</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> command;</span><br><span class="line">    <span class="keyword">let</span> args;</span><br><span class="line">    <span class="comment">// 参数拼装命令行，</span></span><br><span class="line">    <span class="comment">//    例如 使用yarn ： `yarn add react react-dom`</span></span><br><span class="line">    <span class="comment">//    或 使用npm ： `npm install react react-dom --save` </span></span><br><span class="line">    <span class="keyword">if</span> (useYarn) &#123;</span><br><span class="line">      command = <span class="string">'yarnpkg'</span>;</span><br><span class="line">      args = [<span class="string">'add'</span>, <span class="string">'--exact'</span>];</span><br><span class="line">      <span class="keyword">if</span> (!isOnline) &#123;</span><br><span class="line">        args.push(<span class="string">'--offline'</span>);</span><br><span class="line">      &#125;</span><br><span class="line">      [].push.apply(args, dependencies);</span><br><span class="line">      args.push(<span class="string">'--cwd'</span>);</span><br><span class="line">      args.push(root);</span><br><span class="line">      <span class="comment">//略去log信息... </span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      command = <span class="string">'npm'</span>;</span><br><span class="line">      args = [</span><br><span class="line">        <span class="string">'install'</span>,</span><br><span class="line">        <span class="string">'--save'</span>,</span><br><span class="line">        <span class="string">'--save-exact'</span>,</span><br><span class="line">        <span class="string">'--loglevel'</span>,</span><br><span class="line">        <span class="string">'error'</span>,</span><br><span class="line">      ].concat(dependencies);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (verbose) &#123;</span><br><span class="line">      args.push(<span class="string">'--verbose'</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//然后用node去跑安装脚本 </span></span><br><span class="line">    <span class="comment">//这里 spawn = require('cross-spawn'); 出来处理平台差异</span></span><br><span class="line">    <span class="keyword">const</span> child = spawn(command, args, &#123; <span class="attr">stdio</span>: <span class="string">'inherit'</span> &#125;);</span><br><span class="line">    child.on(<span class="string">'close'</span>, code =&gt; &#123;</span><br><span class="line">      <span class="keyword">if</span> (code !== <span class="number">0</span>) &#123;</span><br><span class="line">        reject(&#123;</span><br><span class="line">          command: <span class="string">`<span class="subst">$&#123;command&#125;</span> <span class="subst">$&#123;args.join(<span class="string">' '</span>)&#125;</span>`</span>,</span><br><span class="line">        &#125;);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      resolve();</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="init-拷贝模板"><a href="#init-拷贝模板" class="headerlink" title="init 拷贝模板"></a><code>init</code> 拷贝模板</h3><p>init 方法默认是在 【当前web项目路径】/node_modules/react-scripts/script/init.js 中 ：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="keyword">function</span>(<span class="params"></span></span></span><br><span class="line"><span class="function"><span class="params">  appPath,</span></span></span><br><span class="line"><span class="function"><span class="params">  appName,</span></span></span><br><span class="line"><span class="function"><span class="params">  verbose,</span></span></span><br><span class="line"><span class="function"><span class="params">  originalDirectory,</span></span></span><br><span class="line"><span class="function"><span class="params">  template</span></span></span><br><span class="line"><span class="function"><span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> ownPath = path.dirname(</span><br><span class="line">    <span class="built_in">require</span>.resolve(path.join(__dirname, <span class="string">'..'</span>, <span class="string">'package.json'</span>))</span><br><span class="line">  );</span><br><span class="line">  <span class="keyword">const</span> appPackage = <span class="built_in">require</span>(path.join(appPath, <span class="string">'package.json'</span>));</span><br><span class="line">  <span class="keyword">const</span> useYarn = fs.existsSync(path.join(appPath, <span class="string">'yarn.lock'</span>));</span><br><span class="line">  appPackage.dependencies = appPackage.dependencies || &#123;&#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> useTypeScript = appPackage.dependencies[<span class="string">'typescript'</span>] != <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 设置package.json 中 scripts/eslint/browserslist 信息</span></span><br><span class="line">  appPackage.scripts = &#123;</span><br><span class="line">    start: <span class="string">'react-scripts start'</span>,</span><br><span class="line">    build: <span class="string">'react-scripts build'</span>,</span><br><span class="line">    test: <span class="string">'react-scripts test'</span>,</span><br><span class="line">    eject: <span class="string">'react-scripts eject'</span>,</span><br><span class="line">  &#125;;</span><br><span class="line">  appPackage.eslintConfig = &#123;</span><br><span class="line">    extends: <span class="string">'react-app'</span>,</span><br><span class="line">  &#125;;</span><br><span class="line">  appPackage.browserslist = defaultBrowsers;</span><br><span class="line"></span><br><span class="line">  fs.writeFileSync(</span><br><span class="line">    path.join(appPath, <span class="string">'package.json'</span>),</span><br><span class="line">    <span class="built_in">JSON</span>.stringify(appPackage, <span class="literal">null</span>, <span class="number">2</span>) + os.EOL</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 如果已有 README.md 则重命名</span></span><br><span class="line">  <span class="keyword">const</span> readmeExists = fs.existsSync(path.join(appPath, <span class="string">'README.md'</span>));</span><br><span class="line">  <span class="keyword">if</span> (readmeExists) &#123;</span><br><span class="line">    fs.renameSync(</span><br><span class="line">      path.join(appPath, <span class="string">'README.md'</span>),</span><br><span class="line">      path.join(appPath, <span class="string">'README.old.md'</span>)</span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">//把预设的模版拷贝到项目下</span></span><br><span class="line">  <span class="comment">//  可以在 react-scripts/template 看到这些文件 public目录 src目录 gitignore README.md</span></span><br><span class="line">  <span class="keyword">const</span> templatePath = template</span><br><span class="line">    ? path.resolve(originalDirectory, template)</span><br><span class="line">    : path.join(ownPath, useTypeScript ? <span class="string">'template-typescript'</span> : <span class="string">'template'</span>);</span><br><span class="line">  <span class="keyword">if</span> (fs.existsSync(templatePath)) &#123;</span><br><span class="line">    fs.copySync(templatePath, appPath);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.error(</span><br><span class="line">      <span class="string">`Could not locate supplied template: <span class="subst">$&#123;chalk.green(templatePath)&#125;</span>`</span></span><br><span class="line">    );</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 如果发现没有安装react和react-dom,重新安装一次 代码略</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// Install additional template dependencies, if present</span></span><br><span class="line">  <span class="comment">//略去log信息... </span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>简化一下逻辑这里的主要内容就是 修改package.json信息和拷贝模板文件</p><p>~END~</p>]]></content>
    
    <summary type="html">
    
      &lt;blockquote class=&quot;blockquote-center&quot;&gt;&lt;p&gt;之前在 &lt;a href=&quot;../create-react-app教程-使用篇/&quot;&gt;create-react-app教程-使用篇&lt;/a&gt; 中介绍了create-react-app的基本使用，&lt;br&gt;为了便于理解一个脚手架脚本是如何运作的，现在来看一下 create-react-app v1.5.2 的源码&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;入口index-js&quot;&gt;&lt;a href=&quot;#入口index-js&quot; class=&quot;headerlink&quot; title=&quot;入口index.js&quot;&gt;&lt;/a&gt;入口index.js&lt;/h2&gt;&lt;p&gt;create-react-app 一般会作为全局命令，因为便于更新等原因，create-react-app 只会做初始化仓库 执行当前版本命令等操作。&lt;/p&gt;
&lt;p&gt;找到 &lt;code&gt;create-react-app&lt;/code&gt; 入口index文件：&lt;br&gt;
    
    </summary>
    
      <category term="quick-start" scheme="https://developerdoc.com/categories/quick-start/"/>
    
      <category term="create-react-app教程" scheme="https://developerdoc.com/categories/quick-start/create-react-app%E6%95%99%E7%A8%8B/"/>
    
    
      <category term="前端工程化" scheme="https://developerdoc.com/tags/%E5%89%8D%E7%AB%AF%E5%B7%A5%E7%A8%8B%E5%8C%96/"/>
    
      <category term="react" scheme="https://developerdoc.com/tags/react/"/>
    
  </entry>
  
  <entry>
    <title>create-react-app教程-使用篇</title>
    <link href="https://developerdoc.com/quick-start/create-react-app%E6%95%99%E7%A8%8B-%E4%BD%BF%E7%94%A8%E7%AF%87/"/>
    <id>https://developerdoc.com/quick-start/create-react-app教程-使用篇/</id>
    <published>2018-11-28T14:27:04.000Z</published>
    <updated>2019-03-06T03:20:48.632Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>是不是厌倦了每次新建web项目繁琐的配置，想不想只用10秒搭建就搭建一个完整的react项目？ </p></blockquote><p><img src="/res/create-react-app/1.svg" alt=""><br><a id="more"></a><br>create-react-app 分为两个部分</p><ul><li>其中 <code>create-react-app</code>是创建单页面的react脚手架项目脚本 ；</li><li>而<code>react-scripts</code>则管理着工程的依赖。</li></ul><p>如果我们手动创建react web应用程序的时候,要自己通过 npm 或者 yarn 安装项目的全部依赖，再写webpack.config.js,一系列复杂的配置,搭建好开发环境后写src源代码。使用 create-react-app 去自动构建你的app程序, 你的项目无需配置，也没有复杂的目录结构，只有你构建应用所需的文件。省去了很多精力，最适合快速上手一个demo了。</p><h2 id="create-react-app-使用"><a href="#create-react-app-使用" class="headerlink" title="create-react-app 使用"></a>create-react-app 使用</h2><p>先来看看如何十秒上手create-react-app!</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//my-app 是你要创建的web单页面项目名字</span></span><br><span class="line">npx create-react-app my-app</span><br><span class="line"><span class="comment">//npx 需要 npm 5.2+ </span></span><br><span class="line">cd my-app</span><br><span class="line">npm start</span><br></pre></td></tr></table></figure><p>输入上述命令后，打开 <a href="http://localhost:3000/" target="_blank" rel="noopener">http://localhost:3000/</a> 就可以看到初始页面了~</p><h3 id="文件结构"><a href="#文件结构" class="headerlink" title="文件结构"></a>文件结构</h3><p>接着上面的命令，可以看到生成了如下文件：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">my-app</span><br><span class="line">├── README.md</span><br><span class="line">├── node_modules</span><br><span class="line">├── package.json</span><br><span class="line">├── .gitignore</span><br><span class="line">├── public</span><br><span class="line">│   ├── favicon.ico</span><br><span class="line">│   ├── index.html</span><br><span class="line">│   └── manifest.json</span><br><span class="line">└── src</span><br><span class="line">    ├── App.css</span><br><span class="line">    ├── App.js</span><br><span class="line">    ├── App.test.js</span><br><span class="line">    ├── index.css</span><br><span class="line">    ├── index.js</span><br><span class="line">    ├── logo.svg</span><br><span class="line">    └── serviceWorker.js</span><br></pre></td></tr></table></figure><p>public/index.html 是页面模板,src/index.js 则是入口JS文件</p><p>其中<code>src</code>目录和 <code>public</code>目录需要注意下。只有在<code>src</code>目录下的文件才会被webpack编译，js和css文件都建议放在此处；而<code>public</code>文件夹则存放着被 public/index.html使用的资源。</p><h3 id="webpack配置"><a href="#webpack配置" class="headerlink" title="webpack配置"></a>webpack配置</h3><p>对于喜欢手动创建web项目的人来说，没有webpack的配置很是不习惯啊！我就是要手动配置webpack怎么办？</p><p>那就是使用 <a href="https://github.com/timarney/react-app-rewired" target="_blank" rel="noopener">https://github.com/timarney/react-app-rewired</a> 使用<code>react-app-rewired</code> 需要三步：</p><p><strong>1)</strong> 安装 react-app-rewired</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm install react-app-rewired --save-dev</span><br></pre></td></tr></table></figure><p><strong>2)</strong> 在项目根目录创建 <code>config-overrides.js</code>文件</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"> <span class="comment">/* config-overrides.js */</span></span><br><span class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="keyword">function</span> <span class="title">override</span>(<span class="params">config, env</span>) </span>&#123;</span><br><span class="line">  <span class="comment">//console.log('原webpack配置 config:',config)</span></span><br><span class="line">  <span class="comment">//在这里修改config就行</span></span><br><span class="line">  <span class="comment">//console.log('修改后webpack配置 config:',config)</span></span><br><span class="line">  <span class="keyword">return</span> config;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>3）</strong> 修改package.json中script:</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">  <span class="comment">/* package.json */</span></span><br><span class="line">  <span class="string">"scripts"</span>: &#123;</span><br><span class="line"><span class="comment">//   "start": "react-scripts start",</span></span><br><span class="line">   <span class="string">"start"</span>: <span class="string">"react-app-rewired start"</span>,</span><br><span class="line"><span class="comment">//    "build": "react-scripts build",</span></span><br><span class="line">   <span class="string">"build"</span>: <span class="string">"react-app-rewired build"</span>,</span><br><span class="line"><span class="comment">//   "test": "react-scripts test --env=jsdom",</span></span><br><span class="line">   <span class="string">"test"</span>: <span class="string">"react-app-rewired test --env=jsdom"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="那么如何查看create-react-app自带的依赖配置呢？"><a href="#那么如何查看create-react-app自带的依赖配置呢？" class="headerlink" title="那么如何查看create-react-app自带的依赖配置呢？"></a>那么如何查看<code>create-react-app</code>自带的依赖配置呢？</h4><p>答案就是使用<code>npm run eject</code> 。<code>npm run eject</code>的作用就是将原有隐藏的依赖（比如Webpack, Babel, ESLint等）暴露出来， 请注意这是不可逆的行为。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">//npm run eject执行后 项目下多了这样的文件</span><br><span class="line">//我的create-react-app版本为1.5.2</span><br><span class="line">|-config</span><br><span class="line">    |-jest</span><br><span class="line">        |-cssTransform.js</span><br><span class="line">        |-fileTransform.js</span><br><span class="line">    |-env.js</span><br><span class="line">    |-paths.js</span><br><span class="line">    |-webpack.config.dev.js</span><br><span class="line">    |-webpack.config.prod.js</span><br><span class="line">    |-webpackDevServer.config.js</span><br></pre></td></tr></table></figure><p>查看 <code>webpack.config.dev.js</code> 和 <code>webpack.config.prod.js</code> 支持的支持的依赖大致有：</p><ul><li>React, JSX, ES6, TypeScript 和 Flow 语法支持<ul><li><a href="https://facebook.github.io/react/docs/introducing-jsx.html" target="_blank" rel="noopener">JSX</a>, </li><li><a href="https://facebook.github.io/create-react-app/docs/adding-flow" target="_blank" rel="noopener">Flow</a></li><li><a href="https://facebook.github.io/create-react-app/docs/adding-typescript" target="_blank" rel="noopener">TypeScript</a>.</li></ul></li><li>ES6以外的特性<ul><li><a href="https://github.com/rwaldron/exponentiation-operator" target="_blank" rel="noopener">Exponentiation Operator</a> (ES2016).</li><li><a href="https://github.com/tc39/ecmascript-asyncawait" target="_blank" rel="noopener">Async/await</a> (ES2017).</li><li><a href="https://github.com/tc39/proposal-object-rest-spread" target="_blank" rel="noopener">Object Rest/Spread Properties</a> (ES2018).</li><li><a href="">Dynamic import() </a>(stage 3 proposal)</li><li><a href="https://github.com/tc39/proposal-class-public-fields" target="_blank" rel="noopener">Class Fields and Static Properties</a> (part of stage 3 proposal).</li></ul></li><li>通过Autoprefixer 帮你的css文件自动添加浏览器兼容前缀</li><li>快速交互式单元测试运行程序，内置对覆盖率报告的支持。</li><li>一个能检查常见错误的实时服务器</li><li>打包脚本 能输出带 hashes 和 sourcemaps信息.</li><li>使用单个react-script脚本对上面依赖无侵入更新</li></ul><h3 id="使用sass样式"><a href="#使用sass样式" class="headerlink" title="使用sass样式"></a>使用sass样式</h3><p>在create-react-app中使用 Sass需要以下步骤：</p><p><strong>1)</strong> 安装 <code>node-sass</code></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">npm install node-sass --save</span><br><span class="line"><span class="comment"># or</span></span><br><span class="line">yarn add node-sass</span><br></pre></td></tr></table></figure><p>现在你可以重命名 <code>src/App.css</code> 为 <code>src/App.scss</code>然后在 <code>src/App.js</code>中更新为 <code>import src/App.scss</code> ,.scss 或 .sass 后缀的文件会自动被编译。</p><h3 id="使用-Bootstrap"><a href="#使用-Bootstrap" class="headerlink" title="使用 Bootstrap"></a>使用 Bootstrap</h3><p>你可以尝试使用下<a href="https://reactstrap.github.io/" target="_blank" rel="noopener">reactstrap</a>, 这是一个流行的整合bootstrap和react的库。</p><p><strong>1）</strong> 安装</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">npm install --save reactstrap bootstrap@4</span><br><span class="line"><span class="comment">#or</span></span><br><span class="line">yarn add bootstrap@4 reactstrap</span><br></pre></td></tr></table></figure><p><strong>2）</strong> 在你的src/index.js 文件内容的顶部，导入 Bootstrap CSS 和可选的 Bootstrap theme CSS</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">'bootstrap/dist/css/bootstrap.css'</span>;  <span class="comment">// 必须的 </span></span><br><span class="line"><span class="keyword">import</span> <span class="string">'bootstrap/dist/css/bootstrap-theme.css'</span>;  <span class="comment">//  可选的</span></span><br></pre></td></tr></table></figure><p><strong>3）</strong> 使用 reactstrap 组件 </p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; Grid, Navbar, Jumbotron, Button &#125; <span class="keyword">from</span> <span class="string">'react-bootstrap'</span>;</span><br></pre></td></tr></table></figure><blockquote class="blockquote-center"><p>会在之后的 <a href="../create-react-app教程-源码篇/">create-react-app教程-源码篇</a> 中介绍create-react-app的源码</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;是不是厌倦了每次新建web项目繁琐的配置，想不想只用10秒搭建就搭建一个完整的react项目？ &lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;/res/create-react-app/1.svg&quot; alt=&quot;&quot;&gt;&lt;br&gt;
    
    </summary>
    
      <category term="quick-start" scheme="https://developerdoc.com/categories/quick-start/"/>
    
      <category term="create-react-app教程" scheme="https://developerdoc.com/categories/quick-start/create-react-app%E6%95%99%E7%A8%8B/"/>
    
    
      <category term="前端工程化" scheme="https://developerdoc.com/tags/%E5%89%8D%E7%AB%AF%E5%B7%A5%E7%A8%8B%E5%8C%96/"/>
    
      <category term="react" scheme="https://developerdoc.com/tags/react/"/>
    
  </entry>
  
  <entry>
    <title>(转)浏览器的工作原理-现代浏览器幕后</title>
    <link href="https://developerdoc.com/essay/(%E8%BD%AC)%E6%B5%8F%E8%A7%88%E5%99%A8%E7%9A%84%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86-%E7%8E%B0%E4%BB%A3%E6%B5%8F%E8%A7%88%E5%99%A8%E5%B9%95%E5%90%8E/"/>
    <id>https://developerdoc.com/essay/(转)浏览器的工作原理-现代浏览器幕后/</id>
    <published>2018-11-27T02:34:13.000Z</published>
    <updated>2018-11-27T15:08:22.384Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>原文地址： <a href="https://www.html5rocks.com/en/tutorials/internals/howbrowserswork/" target="_blank" rel="noopener">How Browsers Work: Behind the scenes of modern web browsers</a></p></blockquote><center><strong>序言</strong></center><p>这是一篇全面介绍 WebKit 和 Gecko 内部操作的入门文章，是以色列开发人员塔利·加希尔大量研究的成果。在过去的几年中，她查阅了所有公开发布的关于浏览器内部机制的数据（请参见资源），并花了很多时间来研读网络浏览器的源代码。她写道：</p><p>在 IE 占据 90% 市场份额的年代，我们除了把浏览器当成一个“黑箱”，什么也做不了。但是现在，开放源代码的浏览器拥有了过半的市场份额，因此，是时候来揭开神秘的面纱，一探网络浏览器的内幕了。呃，里面只有数以百万行计的 C++ 代码…</p><p>塔利在她的网站上公布了自己的研究成果，但是我们觉得它值得让更多的人来了解，所以我们在此重新整理并公布。</p><p>作为一名网络开发人员，学习浏览器的内部工作原理将有助于您作出更明智的决策，并理解那些最佳开发实践的个中缘由。尽管这是一篇相当长的文档，但是我们建议您花些时间来仔细阅读；读完之后，您肯定会觉得所费不虚。</p><p align="right">保罗·爱丽诗 (Paul Irish)，Chrome 浏览器开发人员事务部</p><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>网络浏览器很可能是使用最广的软件。在这篇入门文章中，我将会介绍它们的幕后工作原理。我们会了解到，从您在地址栏输入 google.com 直到您在浏览器屏幕上看到 Google 首页的整个过程中都发生了些什么。</p><a id="more"></a><h3 id="我们要讨论的浏览器"><a href="#我们要讨论的浏览器" class="headerlink" title="我们要讨论的浏览器"></a>我们要讨论的浏览器</h3><p>目前使用的主流浏览器有五个：Internet Explorer、Firefox、Safari、Chrome 浏览器和 Opera。本文中以开放源代码浏览器为例，即 Firefox、Chrome 浏览器和 Safari（部分开源）。根据 StatCounter 浏览器统计数据，目前（2011 年 8 月）Firefox、Safari 和 Chrome 浏览器的总市场占有率将近 60%。由此可见，如今开放源代码浏览器在浏览器市场中占据了非常坚实的部分。</p><h3 id="浏览器的主要功能"><a href="#浏览器的主要功能" class="headerlink" title="浏览器的主要功能"></a>浏览器的主要功能</h3><p>浏览器的主要功能就是向服务器发出请求，在浏览器窗口中展示您选择的网络资源。这里所说的资源一般是指 HTML 文档，也可以是 PDF、图片或其他的类型。资源的位置由用户使用 URI（统一资源标示符）指定。</p><p>浏览器解释并显示 HTML 文件的方式是在 HTML 和 CSS 规范中指定的。这些规范由网络标准化组织 W3C（万维网联盟）进行维护。<br>多年以来，各浏览器都没有完全遵从这些规范，同时还在开发自己独有的扩展程序，这给网络开发人员带来了严重的兼容性问题。如今，大多数的浏览器都是或多或少地遵从规范。</p><p>浏览器的用户界面有很多彼此相同的元素，其中包括：</p><ul><li>用来输入 URI 的地址栏</li><li>前进和后退按钮</li><li>书签设置选项</li><li>用于刷新和停止加载当前文档的刷新和停止按钮</li><li>用于返回主页的主页按钮</li></ul><p>奇怪的是，浏览器的用户界面并没有任何正式的规范，这是多年来的最佳实践自然发展以及彼此之间相互模仿的结果。HTML5 也没有定义浏览器必须具有的用户界面元素，但列出了一些通用的元素，例如地址栏、状态栏和工具栏等。当然，各浏览器也可以有自己独特的功能，比如 Firefox 的下载管理器。</p><h3 id="浏览器的高层结构"><a href="#浏览器的高层结构" class="headerlink" title="浏览器的高层结构"></a>浏览器的高层结构</h3><p>浏览器的主要组件为 (1.1)：</p><ol><li>用户界面 - 包括地址栏、前进/后退按钮、书签菜单等。除了浏览器主窗口显示的您请求的页面外，其他显示的各个部分都属于用户界面。</li></ol><ul><li>浏览器引擎 - 在用户界面和呈现引擎之间传送指令。</li><li>呈现引擎 - 负责显示请求的内容。如果请求的内容是 HTML，它就负责解析 HTML 和 CSS 内容，并将解析后的内容显示在屏幕上。</li><li>网络 - 用于网络调用，比如 HTTP 请求。其接口与平台无关，并为所有平台提供底层实现。</li><li>用户界面后端 - 用于绘制基本的窗口小部件，比如组合框和窗口。其公开了与平台无关的通用接口，而在底层使用操作系统的用户界面方法。</li><li>JavaScript 解释器。用于解析和执行 JavaScript 代码。</li><li>数据存储。这是持久层。浏览器需要在硬盘上保存各种数据，例如 Cookie。新的 HTML 规范 (HTML5) 定义了“网络数据库”，这是一个完整（但是轻便）的浏览器内数据库。</li></ul><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/layers.png" alt=""></p><center><strong>图：浏览器的主要组件</strong></center><p>值得注意的是，和大多数浏览器不同，Chrome 浏览器的每个标签页都分别对应一个呈现引擎实例。每个标签页都是一个独立的进程。</p><h2 id="呈现引擎"><a href="#呈现引擎" class="headerlink" title="呈现引擎"></a>呈现引擎</h2><p>呈现引擎的作用嘛…当然就是“呈现”了，也就是在浏览器的屏幕上显示请求的内容。</p><p>默认情况下，呈现引擎可显示 HTML 和 XML 文档与图片。通过插件（或浏览器扩展程序），还可以显示其他类型的内容；例如，使用 PDF 查看器插件就能显示 PDF 文档。但是在本章中，我们将集中介绍其主要用途：显示使用 CSS 格式化的 HTML 内容和图片。</p><h3 id="呈现引擎-1"><a href="#呈现引擎-1" class="headerlink" title="呈现引擎"></a>呈现引擎</h3><p>本文所讨论的浏览器（Firefox、Chrome 浏览器和 Safari）是基于两种呈现引擎构建的。Firefox 使用的是 Gecko，这是 Mozilla 公司“自制”的呈现引擎。而 Safari 和 Chrome 浏览器使用的都是 WebKit。</p><p>WebKit 是一种开放源代码呈现引擎，起初用于 Linux 平台，随后由 Apple 公司进行修改，从而支持苹果机和 Windows。有关详情，请参阅 webkit.org。</p><h3 id="主流程"><a href="#主流程" class="headerlink" title="主流程"></a>主流程</h3><p>呈现引擎一开始会从网络层获取请求文档的内容，内容的大小一般限制在 8000 个块以内。</p><p>然后进行如下所示的基本流程：<br><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/flow.png" alt=""></p><center><strong>图：呈现引擎的基本流程。</strong></center><p>呈现引擎将开始解析 HTML 文档，并将各标记逐个转化成“内容树”上的 DOM 节点。同时也会解析外部 CSS 文件以及样式元素中的样式数据。HTML 中这些带有视觉指令的样式信息将用于创建另一个树结构：呈现树。</p><p>呈现树包含多个带有视觉属性（如颜色和尺寸）的矩形。这些矩形的排列顺序就是它们将在屏幕上显示的顺序。</p><p>呈现树构建完毕之后，进入“布局”处理阶段，也就是为每个节点分配一个应出现在屏幕上的确切坐标。下一个阶段是绘制 - 呈现引擎会遍历呈现树，由用户界面后端层将每个节点绘制出来。</p><p>需要着重指出的是，这是一个渐进的过程。为达到更好的用户体验，呈现引擎会力求尽快将内容显示在屏幕上。它不必等到整个 HTML 文档解析完毕之后，就会开始构建呈现树和设置布局。在不断接收和处理来自网络的其余内容的同时，呈现引擎会将部分内容解析并显示出来。</p><h3 id="主流程示例"><a href="#主流程示例" class="headerlink" title="主流程示例"></a>主流程示例</h3><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/webkitflow.png" alt=""></p><center><strong>图：WebKit 主流程</strong></center><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image008.jpg" alt=""></p><center><strong>图：Mozilla 的 Gecko 呈现引擎主流程 (3.6)</strong></center><p>从图 3 和图 4 可以看出，虽然 WebKit 和 Gecko 使用的术语略有不同，但整体流程是基本相同的。</p><p>Gecko 将视觉格式化元素组成的树称为“框架树”。每个元素都是一个框架。WebKit 使用的术语是“呈现树”，它由“呈现对象”组成。对于元素的放置，WebKit 使用的术语是“布局”，而 Gecko 称之为“重排”。对于连接 DOM 节点和可视化信息从而创建呈现树的过程，WebKit 使用的术语是“附加”。有一个细微的非语义差别，就是 Gecko 在 HTML 与 DOM 树之间还有一个称为“内容槽”的层，用于生成 DOM 元素。我们会逐一论述流程中的每一部分：</p><h2 id="解析和-DOM-树构建"><a href="#解析和-DOM-树构建" class="headerlink" title="解析和 DOM 树构建"></a>解析和 DOM 树构建</h2><h3 id="解析-综述"><a href="#解析-综述" class="headerlink" title="解析 - 综述"></a>解析 - 综述</h3><p>解析是呈现引擎中非常重要的一个环节，因此我们要更深入地讲解。首先，来介绍一下解析。</p><p>解析文档是指将文档转化成为有意义的结构，也就是可让代码理解和使用的结构。解析得到的结果通常是代表了文档结构的节点树，它称作解析树或者语法树。</p><p>示例 - 解析 <code>2 + 3 - 1</code> 这个表达式，会返回下面的树：<br><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image009.png" alt=""></p><center><strong>图：数学表达式树节点</strong></center><h4 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h4><p>解析是以文档所遵循的语法规则（编写文档所用的语言或格式）为基础的。所有可以解析的格式都必须对应确定的语法（由词汇和语法规则构成）。这称为与上下文无关的语法。人类语言并不属于这样的语言，因此无法用常规的解析技术进行解析。</p><h4 id="解析器和词法分析器的组合"><a href="#解析器和词法分析器的组合" class="headerlink" title="解析器和词法分析器的组合"></a>解析器和词法分析器的组合</h4><p>解析的过程可以分成两个子过程：词法分析和语法分析。</p><p>词法分析是将输入内容分割成大量标记的过程。标记是语言中的词汇，即构成内容的单位。在人类语言中，它相当于语言字典中的单词。</p><p>语法分析是应用语言的语法规则的过程。</p><p>解析器通常将解析工作分给以下两个组件来处理：词法分析器（有时也称为标记生成器），负责将输入内容分解成一个个有效标记；而解析器负责根据语言的语法规则分析文档的结构，从而构建解析树。词法分析器知道如何将无关的字符（比如空格和换行符）分离出来。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image011.png" alt=""></p><center><strong>图：从源文档到解析树</strong></center><p>解析是一个迭代的过程。通常，解析器会向词法分析器请求一个新标记，并尝试将其与某条语法规则进行匹配。如果发现了匹配规则，解析器会将一个对应于该标记的节点添加到解析树中，然后继续请求下一个标记。</p><p>如果没有规则可以匹配，解析器就会将标记存储到内部，并继续请求标记，直至找到可与所有内部存储的标记匹配的规则。如果找不到任何匹配规则，解析器就会引发一个异常。这意味着文档无效，包含语法错误。</p><h4 id="翻译"><a href="#翻译" class="headerlink" title="翻译"></a>翻译</h4><p>很多时候，解析树还不是最终产品。解析通常是在翻译过程中使用的，而翻译是指将输入文档转换成另一种格式。编译就是这样一个例子。编译器可将源代码编译成机器代码，具体过程是首先将源代码解析成解析树，然后将解析树翻译成机器代码文档。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image013.png" alt=""></p><center><strong>图：编译流程</strong></center><h4 id="解析示例"><a href="#解析示例" class="headerlink" title="解析示例"></a>解析示例</h4><p>在图 5 中，我们通过一个数学表达式建立了解析树。现在，让我们试着定义一个简单的数学语言，用来演示解析的过程。</p><p>词汇：我们用的语言可包含整数、加号和减号。</p><p>语法：</p><ol><li>构成语言的语法单位是表达式、项和运算符。</li></ol><ul><li>我们用的语言可以包含任意数量的表达式。</li><li>表达式的定义是：一个“项”接一个“运算符”，然后再接一个“项”。</li><li>运算符是加号或减号。</li><li>项是一个整数或一个表达式。</li></ul><p>让我们分析一下 <code>2 + 3 - 1</code>。 </p><p>匹配语法规则的第一个子串是 2，而根据第 5 条语法规则，这是一个项。匹配语法规则的第二个子串是 2 + 3，而根据第 3 条规则（一个项接一个运算符，然后再接一个项），这是一个表达式。下一个匹配项已经到了输入的结束。2 + 3 - 1 是一个表达式，因为我们已经知道 2 + 3 是一个项，这样就符合“一个项接一个运算符，然后再接一个项”的规则。2 + + 不与任何规则匹配，因此是无效的输入。</p><h4 id="词汇和语法的正式定义"><a href="#词汇和语法的正式定义" class="headerlink" title="词汇和语法的正式定义"></a>词汇和语法的正式定义</h4><p>词汇通常用正则表达式表示。</p><p>例如，我们的示例语言可以定义如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">INTEGER :0|[1-9][0-9]*</span><br><span class="line">PLUS : +</span><br><span class="line">MINUS: -</span><br></pre></td></tr></table></figure><p>正如您所看到的，这里用正则表达式给出了整数的定义。<br>语法通常使用一种称为 BNF 的格式来定义。我们的示例语言可以定义如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">expression :=  term  operation  term</span><br><span class="line">operation :=  PLUS | MINUS</span><br><span class="line">term := INTEGER | expression</span><br></pre></td></tr></table></figure><p>之前我们说过，如果语言的语法是与上下文无关的语法，就可以由常规解析器进行解析。与上下文无关的语法的直观定义就是可以完全用 BNF 格式表达的语法。有关正式定义，请参阅关于与上下文无关的语法的维基百科文章。</p><h4 id="解析器类型"><a href="#解析器类型" class="headerlink" title="解析器类型"></a>解析器类型</h4><p>有两种基本类型的解析器：自上而下解析器和自下而上解析器。直观地来说，自上而下的解析器从语法的高层结构出发，尝试从中找到匹配的结构。而自下而上的解析器从低层规则出发，将输入内容逐步转化为语法规则，直至满足高层规则。</p><p>让我们来看看这两种解析器会如何解析我们的示例：</p><p>自上而下的解析器会从高层的规则开始：首先将 2 + 3 标识为一个表达式，然后将 2 + 3 - 1 标识为一个表达式（标识表达式的过程涉及到匹配其他规则，但是起点是最高级别的规则）。</p><p>自下而上的解析器将扫描输入内容，找到匹配的规则后，将匹配的输入内容替换成规则。如此继续替换，直到输入内容的结尾。部分匹配的表达式保存在解析器的堆栈中。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">堆栈         输入</span><br><span class="line">               2 + 3 - 1</span><br><span class="line">项            + 3 - 1</span><br><span class="line">项运算        3 - 1</span><br><span class="line">表达式        - 1</span><br><span class="line">表达式运算符    1</span><br><span class="line">表达式</span><br></pre></td></tr></table></figure><p>这种自下而上的解析器称为移位归约解析器，因为输入在向右移位（设想有一个指针从输入内容的开头移动到结尾），并且逐渐归约到语法规则上。</p><h4 id="自动生成解析器"><a href="#自动生成解析器" class="headerlink" title="自动生成解析器"></a>自动生成解析器</h4><p>有一些工具可以帮助您生成解析器，它们称为解析器生成器。您只要向其提供您所用语言的语法（词汇和语法规则），它就会生成相应的解析器。创建解析器需要对解析有深刻理解，而人工创建并优化解析器并不是一件容易的事情，所以解析器生成器是非常实用的。</p><p>WebKit 使用了两种非常有名的解析器生成器：用于创建词法分析器的 Flex 以及用于创建解析器的 Bison（您也可能遇到 Lex 和 Yacc 这样的别名）。Flex 的输入是包含标记的正则表达式定义的文件。Bison 的输入是采用 BNF 格式的语言语法规则。</p><h3 id="HTML-解析器"><a href="#HTML-解析器" class="headerlink" title="HTML 解析器"></a>HTML 解析器</h3><p>HTML 解析器的任务是将 HTML 标记解析成解析树。</p><h4 id="HTML-语法定义"><a href="#HTML-语法定义" class="headerlink" title="HTML 语法定义"></a>HTML 语法定义</h4><p>HTML 的词汇和语法在 W3C 组织创建的规范中进行了定义。当前的版本是 HTML4，HTML5 正在处理过程中。</p><h4 id="非与上下文无关的语法"><a href="#非与上下文无关的语法" class="headerlink" title="非与上下文无关的语法"></a>非与上下文无关的语法</h4><p>正如我们在解析过程的简介中已经了解到的，语法可以用 BNF 等格式进行正式定义。</p><p>很遗憾，所有的常规解析器都不适用于 HTML（我并不是开玩笑，它们可以用于解析 CSS 和 JavaScript）。HTML 并不能很容易地用解析器所需的与上下文无关的语法来定义。</p><p>有一种可以定义 HTML 的正规格式：DTD（Document Type Definition，文档类型定义），但它不是与上下文无关的语法。</p><p>这初看起来很奇怪：HTML 和 XML 非常相似。有很多 XML 解析器可以使用。HTML 存在一个 XML 变体 (XHTML)，那么有什么大的区别呢？</p><p>区别在于 HTML 的处理更为“宽容”，它允许您省略某些隐式添加的标记，有时还能省略一些起始或者结束标记等等。和 XML 严格的语法不同，HTML 整体来看是一种“软性”的语法。</p><p>显然，这种看上去细微的差别实际上却带来了巨大的影响。一方面，这是 HTML 如此流行的原因：它能包容您的错误，简化网络开发。另一方面，这使得它很难编写正式的语法。概括地说，HTML 无法很容易地通过常规解析器解析（因为它的语法不是与上下文无关的语法），也无法通过 XML 解析器来解析。</p><h4 id="HTML-DTD"><a href="#HTML-DTD" class="headerlink" title="HTML DTD"></a>HTML DTD</h4><p>HTML 的定义采用了 DTD 格式。此格式可用于定义 SGML 族的语言。它包括所有允许使用的元素及其属性和层次结构的定义。如上文所述，HTML DTD 无法构成与上下文无关的语法。</p><p>DTD 存在一些变体。严格模式完全遵守 HTML 规范，而其他模式可支持以前的浏览器所使用的标记。这样做的目的是确保向下兼容一些早期版本的内容。最新的严格模式 DTD 可以在这里找到：<a href="http://www.w3.org/TR/html4/strict.dtd" target="_blank" rel="noopener">www.w3.org/TR/html4/strict.dtd</a></p><h4 id="DOM"><a href="#DOM" class="headerlink" title="DOM"></a>DOM</h4><p>解析器的输出“解析树”是由 DOM 元素和属性节点构成的树结构。DOM 是文档对象模型 (Document Object Model) 的缩写。它是 HTML 文档的对象表示，同时也是外部内容（例如 JavaScript）与 HTML 元素之间的接口。<br>解析树的根节点是“Document”对象。</p><p>DOM 与标记之间几乎是一一对应的关系。比如下面这段标记：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">p</span>&gt;</span></span><br><span class="line">      Hello World</span><br><span class="line">    <span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span>&gt;</span> <span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">"example.png"</span>/&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><p>可翻译成如下的 DOM 树：<br><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image015.png" alt=""></p><center><strong>图：示例标记的 DOM 树</strong></center><p>和 HTML 一样，DOM 也是由 W3C 组织指定的。请参见 <a href="http://www.w3.org/DOM/DOMTR" target="_blank" rel="noopener">www.w3.org/DOM/DOMTR</a>。这是关于文档操作的通用规范。其中一个特定模块描述针对 HTML 的元素。HTML 的定义可以在这里找到：<a href="http://www.w3.org/TR/2003/REC-DOM-Level-2-HTML-20030109/idl-definitions.html" target="_blank" rel="noopener">www.w3.org/TR/2003/REC-DOM-Level-2-HTML-20030109/idl-definitions.html</a>。</p><p>我所说的树包含 DOM 节点，指的是树是由实现了某个 DOM 接口的元素构成的。浏览器在具体的实现中会有一些供内部使用的其他属性。</p><h4 id="解析算法"><a href="#解析算法" class="headerlink" title="解析算法"></a>解析算法</h4><p>我们在之前章节已经说过，HTML 无法用常规的自上而下或自下而上的解析器进行解析。</p><p>原因在于：</p><ol><li>语言的宽容本质。</li></ol><ul><li>浏览器历来对一些常见的无效 HTML 用法采取包容态度。</li><li>解析过程需要不断地反复。源内容在解析过程中通常不会改变，但是在 HTML 中，脚本标记如果包含 document.write，就会添加额外的标记，这样解析过程实际上就更改了输入内容。</li></ul><p>由于不能使用常规的解析技术，浏览器就创建了自定义的解析器来解析 HTML。</p><p>HTML5 规范详细地描述了解析算法。此算法由两个阶段组成：标记化和树构建。</p><p>标记化是词法分析过程，将输入内容解析成多个标记。HTML 标记包括起始标记、结束标记、属性名称和属性值。</p><p>标记生成器识别标记，传递给树构造器，然后接受下一个字符以识别下一个标记；如此反复直到输入的结束。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image017.png" alt=""></p><center><strong>图：HTML 解析流程（摘自 HTML5 规范）</strong></center><h4 id="标记化算法"><a href="#标记化算法" class="headerlink" title="标记化算法"></a>标记化算法</h4><p>该算法的输出结果是 HTML 标记。该算法使用状态机来表示。每一个状态接收来自输入信息流的一个或多个字符，并根据这些字符更新下一个状态。当前的标记化状态和树结构状态会影响进入下一状态的决定。这意味着，即使接收的字符相同，对于下一个正确的状态也会产生不同的结果，具体取决于当前的状态。该算法相当复杂，无法在此详述，所以我们通过一个简单的示例来帮助大家理解其原理。</p><p>基本示例 - 将下面的 HTML 代码标记化：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    Hello world</span><br><span class="line">  <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><p>初始状态是数据状态。遇到字符 &lt; 时，状态更改为“标记打开状态”。接收一个 a-z 字符会创建“起始标记”，状态更改为“标记名称状态”。这个状态会一直保持到接收 &gt; 字符。在此期间接收的每个字符都会附加到新的标记名称上。在本例中，我们创建的标记是 html 标记。</p><p>遇到 &gt; 标记时，会发送当前的标记，状态改回“数据状态”。<body> 标记也会进行同样的处理。目前 html 和 body 标记均已发出。现在我们回到“数据状态”。接收到 Hello world 中的 H 字符时，将创建并发送字符标记，直到接收 <script src="/live2dw/lib/L2Dwidget.min.js?0c58a1486de42ac6cc1c59c7d98ae887"></script><script>L2Dwidget.init({"pluginRootPath":"live2dw/","pluginJsPath":"lib/","pluginModelPath":"assets/","tagMode":false,"debug":false,"model":{"hHeadPos":0.5,"vHeadPos":0.618,"jsonPath":"/live2dw/assets/hijiki.model.json"},"display":{"superSample":2,"width":100,"height":200,"position":"right","hOffset":0,"vOffset":-20},"mobile":{"show":false,"scale":0.5},"react":{"opacityDefault":0.7,"opacityOnHover":0.2},"log":false});</script></body> 中的 &lt;。我们将为 Hello world 中的每个字符都发送一个字符标记。</p><p>现在我们回到“标记打开状态”。接收下一个输入字符 / 时，会创建 end tag token 并改为“标记名称状态”。我们会再次保持这个状态，直到接收 &gt;。然后将发送新的标记，并回到“数据状态”。 输入也会进行同样的处理。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image019.png" alt=""></p><center><strong>图：对示例输入进行标记化</strong></center><h4 id="树构建算法"><a href="#树构建算法" class="headerlink" title="树构建算法"></a>树构建算法</h4><p>在创建解析器的同时，也会创建 Document 对象。在树构建阶段，以 Document 为根节点的 DOM 树也会不断进行修改，向其中添加各种元素。标记生成器发送的每个节点都会由树构建器进行处理。规范中定义了每个标记所对应的 DOM 元素，这些元素会在接收到相应的标记时创建。这些元素不仅会添加到 DOM 树中，还会添加到开放元素的堆栈中。此堆栈用于纠正嵌套错误和处理未关闭的标记。其算法也可以用状态机来描述。这些状态称为“插入模式”。</p><p>让我们来看看示例输入的树构建过程：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    Hello world</span><br><span class="line">  <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><p>树构建阶段的输入是一个来自标记化阶段的标记序列。第一个模式是“initial mode”。接收 HTML 标记后转为“before html”模式，并在这个模式下重新处理此标记。这样会创建一个 HTMLHtmlElement 元素，并将其附加到 Document 根对象上。</p><p>然后状态将改为“before head”。此时我们接收“body”标记。即使我们的示例中没有“head”标记，系统也会隐式创建一个 HTMLHeadElement，并将其添加到树中。</p><p>现在我们进入了“in head”模式，然后转入“after head”模式。系统对 body 标记进行重新处理，创建并插入 HTMLBodyElement，同时模式转变为“in body”。</p><p>现在，接收由“Hello world”字符串生成的一系列字符标记。接收第一个字符时会创建并插入“Text”节点，而其他字符也将附加到该节点。</p><p>接收 body 结束标记会触发“after body”模式。现在我们将接收 HTML 结束标记，然后进入“after after body”模式。接收到文件结束标记后，解析过程就此结束。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image022.gif" alt=""></p><center><strong>图：示例 HTML 的树构建</strong></center><h4 id="解析结束后的操作"><a href="#解析结束后的操作" class="headerlink" title="解析结束后的操作"></a>解析结束后的操作</h4><p>在此阶段，浏览器会将文档标注为交互状态，并开始解析那些处于“deferred”模式的脚本，也就是那些应在文档解析完成后才执行的脚本。然后，文档状态将设置为“完成”，一个“加载”事件将随之触发。</p><p>您可以在 HTML5 规范中查看标记化和树构建的完整算法</p><h4 id="浏览器的容错机制"><a href="#浏览器的容错机制" class="headerlink" title="浏览器的容错机制"></a>浏览器的容错机制</h4><p>您在浏览 HTML 网页时从来不会看到“语法无效”的错误。这是因为浏览器会纠正任何无效内容，然后继续工作。</p><p>以下面的 HTML 代码为例：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">mytag</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">mytag</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">p</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    Really lousy HTML</span><br><span class="line">  <span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><p>在这里，我已经违反了很多语法规则（“mytag”不是标准的标记，“p”和“div”元素之间的嵌套有误等等），但是浏览器仍然会正确地显示这些内容，并且毫无怨言。因为有大量的解析器代码会纠正 HTML 网页作者的错误。</p><p>不同浏览器的错误处理机制相当一致，但令人称奇的是，这种机制并不是 HTML 当前规范的一部分。和书签管理以及前进/后退按钮一样，它也是浏览器在多年发展中的产物。很多网站都普遍存在着一些已知的无效 HTML 结构，每一种浏览器都会尝试通过和其他浏览器一样的方式来修复这些无效结构。</p><p>HTML5 规范定义了一部分这样的要求。WebKit 在 HTML 解析器类的开头注释中对此做了很好的概括。</p><blockquote><p>解析器对标记化输入内容进行解析，以构建文档树。如果文档的格式正确，就直接进行解析。<br>遗憾的是，我们不得不处理很多格式错误的 HTML 文档，所以解析器必须具备一定的容错性。<br>我们至少要能够处理以下错误情况：</p><ol><li>明显不能在某些外部标记中添加的元素。在此情况下，我们应该关闭所有标记，直到出现禁止添加的元素，然后再加入该元素。</li><li>我们不能直接添加的元素。这很可能是网页作者忘记添加了其中的一些标记（或者其中的标记是可选的）。这些标签可能包括：HTML HEAD BODY TBODY TR TD LI（还有遗漏的吗？）。</li><li>向 inline 元素内添加 block 元素。关闭所有 inline 元素，直到出现下一个较高级的 block 元素。</li><li>如果这样仍然无效，可关闭所有元素，直到可以添加元素为止，或者忽略该标记。</li></ol></blockquote><p>让我们看一些 WebKit 容错的示例：</p><h5 id="使用了-lt-br-gt-而不是-lt-br-gt"><a href="#使用了-lt-br-gt-而不是-lt-br-gt" class="headerlink" title="使用了 &lt;/br&gt; 而不是 &lt;br&gt;"></a><code>使用了 &lt;/br&gt; 而不是 &lt;br&gt;</code></h5><p>有些网站使用了 <code>&lt;/br&gt;</code> 而不是 <code>&lt;br&gt;</code>。为了与 IE 和 Firefox 兼容，WebKit 将其与 <code>&lt;br&gt;</code> 做同样的处理。<br>代码如下：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">if (t-&gt;isCloseTag(brTag) &amp;&amp; m_document-&gt;inCompatMode()) &#123;</span><br><span class="line">     reportError(MalformedBRError);</span><br><span class="line">     t-&gt;beginTag = true;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>请注意，错误处理是在内部进行的，用户并不会看到这个过程。</p><h5 id="离散表格"><a href="#离散表格" class="headerlink" title="离散表格"></a><code>离散表格</code></h5><p>离散表格是指位于其他表格内容中，但又不在任何一个单元格内的表格。<br>比如以下的示例：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">table</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">table</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">tr</span>&gt;</span><span class="tag">&lt;<span class="name">td</span>&gt;</span>inner table<span class="tag">&lt;/<span class="name">td</span>&gt;</span><span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tr</span>&gt;</span><span class="tag">&lt;<span class="name">td</span>&gt;</span>outer table<span class="tag">&lt;/<span class="name">td</span>&gt;</span><span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br></pre></td></tr></table></figure><p>WebKit 会将其层次结构更改为两个同级表格：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">table</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tr</span>&gt;</span><span class="tag">&lt;<span class="name">td</span>&gt;</span>outer table<span class="tag">&lt;/<span class="name">td</span>&gt;</span><span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">table</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tr</span>&gt;</span><span class="tag">&lt;<span class="name">td</span>&gt;</span>inner table<span class="tag">&lt;/<span class="name">td</span>&gt;</span><span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br></pre></td></tr></table></figure><p>代码如下：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (m_inStrayTableContent &amp;&amp; localName == tableTag)</span><br><span class="line">        popBlock(tableTag);</span><br></pre></td></tr></table></figure><p>WebKit 使用一个堆栈来保存当前的元素内容，它会从外部表格的堆栈中弹出内部表格。现在，这两个表格就变成了同级关系。</p><h5 id="嵌套的表单元素"><a href="#嵌套的表单元素" class="headerlink" title="嵌套的表单元素"></a><code>嵌套的表单元素</code></h5><p>如果用户在一个表单元素中又放入了另一个表单，那么第二个表单将被忽略。<br>代码如下：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!m_currentFormElement) &#123;</span><br><span class="line">        m_currentFormElement = <span class="keyword">new</span> HTMLFormElement(formTag,    m_document);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="过于复杂的标记层次结构"><a href="#过于复杂的标记层次结构" class="headerlink" title="过于复杂的标记层次结构"></a><code>过于复杂的标记层次结构</code></h5><p>代码的注释已经说得很清楚了。 </p><blockquote><p>示例网站 <a href="http://www.liceo.edu.mx" target="_blank" rel="noopener">www.liceo.edu.mx</a> 嵌套了约 1500 个标记，全都来自一堆 <b> 标记。我们只允许最多 20 层同类型标记的嵌套，如果再嵌套更多，就会全部忽略。</b></p></blockquote><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">bool HTMLParser::allowNestedRedundantTag(<span class="keyword">const</span> AtomicString&amp; tagName)</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">unsigned i = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (HTMLStackElem* curr = m_blockStack;</span><br><span class="line">         i &lt; cMaxRedundantTagDepth &amp;&amp; curr &amp;&amp; curr-&gt;tagName == tagName;</span><br><span class="line">     curr = curr-&gt;next, i++) &#123; &#125;</span><br><span class="line"><span class="keyword">return</span> i != cMaxRedundantTagDepth;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="放错位置的-html-或者-body-结束标记"><a href="#放错位置的-html-或者-body-结束标记" class="headerlink" title="放错位置的 html 或者 body 结束标记"></a><code>放错位置的 html 或者 body 结束标记</code></h5><p>同样，代码的注释已经说得很清楚了。</p><blockquote><p>支持格式非常糟糕的 HTML 代码。我们从不关闭 body 标记，因为一些愚蠢的网页会在实际文档结束之前就关闭。我们通过调用 end() 来执行关闭操作。</p></blockquote><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (t-&gt;tagName == htmlTag || t-&gt;tagName == bodyTag )</span><br><span class="line">        <span class="keyword">return</span>;</span><br></pre></td></tr></table></figure><p>所以网页作者需要注意，除非您想作为反面教材出现在 WebKit 容错代码段的示例中，否则还请编写格式正确的 HTML 代码。</p><h3 id="CSS-解析"><a href="#CSS-解析" class="headerlink" title="CSS 解析"></a>CSS 解析</h3><p>还记得简介中解析的概念吗？和 HTML 不同，CSS 是上下文无关的语法，可以使用简介中描述的各种解析器进行解析。事实上，CSS 规范定义了 CSS 的词法和语法。</p><p>让我们来看一些示例：<br>词法语法（词汇）是针对各个标记用正则表达式定义的：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">comment   \/\*[^*]*\*+([^/*][^*]*\*+)*\/</span><br><span class="line">num   [0-9]+|[0-9]*&quot;.&quot;[0-9]+</span><br><span class="line">nonascii  [\200-\377]</span><br><span class="line">nmstart   [_a-z]|&#123;nonascii&#125;|&#123;escape&#125;</span><br><span class="line">nmchar    [_a-z0-9-]|&#123;nonascii&#125;|&#123;escape&#125;</span><br><span class="line">name    &#123;nmchar&#125;+</span><br><span class="line">ident   &#123;nmstart&#125;&#123;nmchar&#125;*</span><br></pre></td></tr></table></figure><p>“ident”是标识符 (identifier) 的缩写，比如类名。“name”是元素的 ID（通过“#”来引用）。</p><p>语法是采用 BNF 格式描述的。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">ruleset</span><br><span class="line">  : selector [ &apos;,&apos; S* selector ]*</span><br><span class="line">    &apos;&#123;&apos; S* declaration [ &apos;;&apos; S* declaration ]* &apos;&#125;&apos; S*</span><br><span class="line">  ;</span><br><span class="line">selector</span><br><span class="line">  : simple_selector [ combinator selector | S+ [ combinator? selector ]? ]?</span><br><span class="line">  ;</span><br><span class="line">simple_selector</span><br><span class="line">  : element_name [ HASH | class | attrib | pseudo ]*</span><br><span class="line">  | [ HASH | class | attrib | pseudo ]+</span><br><span class="line">  ;</span><br><span class="line">class</span><br><span class="line">  : &apos;.&apos; IDENT</span><br><span class="line">  ;</span><br><span class="line">element_name</span><br><span class="line">  : IDENT | &apos;*&apos;</span><br><span class="line">  ;</span><br><span class="line">attrib</span><br><span class="line">  : &apos;[&apos; S* IDENT S* [ [ &apos;=&apos; | INCLUDES | DASHMATCH ] S*</span><br><span class="line">    [ IDENT | STRING ] S* ] &apos;]&apos;</span><br><span class="line">  ;</span><br><span class="line">pseudo</span><br><span class="line">  : &apos;:&apos; [ IDENT | FUNCTION S* [IDENT S*] &apos;)&apos; ]</span><br><span class="line">  ;</span><br></pre></td></tr></table></figure><p>解释：这是一个规则集的结构：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">div.error , a.error &#123;</span><br><span class="line">  color:red;</span><br><span class="line">  font-weight:bold;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>div.error 和 a.error 是选择器。大括号内的部分包含了由此规则集应用的规则。此结构的正式定义是这样的：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">ruleset</span><br><span class="line">  : selector [ &apos;,&apos; S* selector ]*</span><br><span class="line">    &apos;&#123;&apos; S* declaration [ &apos;;&apos; S* declaration ]* &apos;&#125;&apos; S*</span><br><span class="line">  ;</span><br></pre></td></tr></table></figure><p>这表示一个规则集就是一个选择器，或者由逗号和空格（S 表示空格）分隔的多个（数量可选）选择器。规则集包含了大括号，以及其中的一个或多个（数量可选）由分号分隔的声明。“声明”和“选择器”将由下面的 BNF 格式定义。</p><h4 id="WebKit-CSS-解析器"><a href="#WebKit-CSS-解析器" class="headerlink" title="WebKit CSS 解析器"></a>WebKit CSS 解析器</h4><p>WebKit 使用 Flex 和 Bison 解析器生成器，通过 CSS 语法文件自动创建解析器。正如我们之前在解析器简介中所说，Bison 会创建自下而上的移位归约解析器。Firefox 使用的是人工编写的自上而下的解析器。这两种解析器都会将 CSS 文件解析成 StyleSheet 对象，且每个对象都包含 CSS 规则。CSS 规则对象则包含选择器和声明对象，以及其他与 CSS 语法对应的对象。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image023.png" alt=""></p><center><strong>图：解析 CSS</strong></center><h3 id="处理脚本和样式表的顺序"><a href="#处理脚本和样式表的顺序" class="headerlink" title="处理脚本和样式表的顺序"></a>处理脚本和样式表的顺序</h3><h4 id="脚本"><a href="#脚本" class="headerlink" title="脚本"></a>脚本</h4><p>网络的模型是同步的。网页作者希望解析器遇到 <code>&lt;script&gt;</code> 标记时立即解析并执行脚本。文档的解析将停止，直到脚本执行完毕。如果脚本是外部的，那么解析过程会停止，直到从网络同步抓取资源完成后再继续。此模型已经使用了多年，也在 HTML4 和 HTML5 规范中进行了指定。作者也可以将脚本标注为“defer”，这样它就不会停止文档解析，而是等到解析结束才执行。HTML5 增加了一个选项，可将脚本标记为异步，以便由其他线程解析和执行。</p><h4 id="预解析"><a href="#预解析" class="headerlink" title="预解析"></a>预解析</h4><p>WebKit 和 Firefox 都进行了这项优化。在执行脚本时，其他线程会解析文档的其余部分，找出并加载需要通过网络加载的其他资源。通过这种方式，资源可以在并行连接上加载，从而提高总体速度。请注意，预解析器不会修改 DOM 树，而是将这项工作交由主解析器处理；预解析器只会解析外部资源（例如外部脚本、样式表和图片）的引用。</p><h4 id="样式表"><a href="#样式表" class="headerlink" title="样式表"></a>样式表</h4><p>另一方面，样式表有着不同的模型。理论上来说，应用样式表不会更改 DOM 树，因此似乎没有必要等待样式表并停止文档解析。但这涉及到一个问题，就是脚本在文档解析阶段会请求样式信息。如果当时还没有加载和解析样式，脚本就会获得错误的回复，这样显然会产生很多问题。这看上去是一个非典型案例，但事实上非常普遍。Firefox 在样式表加载和解析的过程中，会禁止所有脚本。而对于 WebKit 而言，仅当脚本尝试访问的样式属性可能受尚未加载的样式表影响时，它才会禁止该脚本。</p><h2 id="呈现树构建"><a href="#呈现树构建" class="headerlink" title="呈现树构建"></a>呈现树构建</h2><p>在 DOM 树构建的同时，浏览器还会构建另一个树结构：呈现树。这是由可视化元素按照其显示顺序而组成的树，也是文档的可视化表示。它的作用是让您按照正确的顺序绘制内容。</p><p>Firefox 将呈现树中的元素称为“框架”。WebKit 使用的术语是呈现器或呈现对象。<br>呈现器知道如何布局并将自身及其子元素绘制出来。<br>WebKits RenderObject 类是所有呈现器的基类，其定义如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">class RenderObject&#123;</span><br><span class="line">  virtual void layout();</span><br><span class="line">  virtual void paint(PaintInfo);</span><br><span class="line">  virtual void rect repaintRect();</span><br><span class="line">  Node* node;  //the DOM node</span><br><span class="line">  RenderStyle* style;  // the computed style</span><br><span class="line">  RenderLayer* containgLayer; //the containing z-index layer</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>每一个呈现器都代表了一个矩形的区域，通常对应于相关节点的 CSS 框，这一点在 CSS2 规范中有所描述。它包含诸如宽度、高度和位置等几何信息。<br>框的类型会受到与节点相关的“display”样式属性的影响（请参阅样式计算章节）。下面这段 WebKit 代码描述了根据 display 属性的不同，针对同一个 DOM 节点应创建什么类型的呈现器。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">RenderObject* RenderObject::createObject(Node* node, RenderStyle* style)</span><br><span class="line">&#123;</span><br><span class="line">    Document* doc = node-&gt;document();</span><br><span class="line">    RenderArena* arena = doc-&gt;renderArena();</span><br><span class="line">    ...</span><br><span class="line">    RenderObject* o = 0;</span><br><span class="line"></span><br><span class="line">    switch (style-&gt;display()) &#123;</span><br><span class="line">        case NONE:</span><br><span class="line">            break;</span><br><span class="line">        case INLINE:</span><br><span class="line">            o = new (arena) RenderInline(node);</span><br><span class="line">            break;</span><br><span class="line">        case BLOCK:</span><br><span class="line">            o = new (arena) RenderBlock(node);</span><br><span class="line">            break;</span><br><span class="line">        case INLINE_BLOCK:</span><br><span class="line">            o = new (arena) RenderBlock(node);</span><br><span class="line">            break;</span><br><span class="line">        case LIST_ITEM:</span><br><span class="line">            o = new (arena) RenderListItem(node);</span><br><span class="line">            break;</span><br><span class="line">       ...</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    return o;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>元素类型也是考虑因素之一，例如表单控件和表格都对应特殊的框架。<br>在 WebKit 中，如果一个元素需要创建特殊的呈现器，就会替换 createRenderer 方法。呈现器所指向的样式对象中包含了一些和几何无关的信息。</p><h3 id="呈现树和-DOM-树的关系"><a href="#呈现树和-DOM-树的关系" class="headerlink" title="呈现树和 DOM 树的关系"></a>呈现树和 DOM 树的关系</h3><p>呈现器是和 DOM 元素相对应的，但并非一一对应。非可视化的 DOM 元素不会插入呈现树中，例如“head”元素。如果元素的 display 属性值为“none”，那么也不会显示在呈现树中（但是 visibility 属性值为“hidden”的元素仍会显示）。<br>有一些 DOM 元素对应多个可视化对象。它们往往是具有复杂结构的元素，无法用单一的矩形来描述。例如，“select”元素有 3 个呈现器：一个用于显示区域，一个用于下拉列表框，还有一个用于按钮。如果由于宽度不够，文本无法在一行中显示而分为多行，那么新的行也会作为新的呈现器而添加。<br>另一个关于多呈现器的例子是格式无效的 HTML。根据 CSS 规范，inline 元素只能包含 block 元素或 inline 元素中的一种。如果出现了混合内容，则应创建匿名的 block 呈现器，以包裹 inline 元素。</p><p>有一些呈现对象对应于 DOM 节点，但在树中所在的位置与 DOM 节点不同。浮动定位和绝对定位的元素就是这样，它们处于正常的流程之外，放置在树中的其他地方，并映射到真正的框架，而放在原位的是占位框架。</p><p> <img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image025.png" alt=""></p><center><strong>图：呈现树及其对应的 DOM 树 (3.1)。初始容器 block 为“viewport”，而在 WebKit 中则为“RenderView”对象。</strong></center><h3 id="构建呈现树的流程"><a href="#构建呈现树的流程" class="headerlink" title="构建呈现树的流程"></a>构建呈现树的流程</h3><p>在 Firefox 中，系统会针对 DOM 更新注册展示层，作为侦听器。展示层将框架创建工作委托给 FrameConstructor，由该构造器解析样式（请参阅样式计算）并创建框架。</p><p>在 WebKit 中，解析样式和创建呈现器的过程称为“附加”。每个 DOM 节点都有一个“attach”方法。附加是同步进行的，将节点插入 DOM 树需要调用新的节点“attach”方法。</p><p>处理 html 和 body 标记就会构建呈现树根节点。这个根节点呈现对象对应于 CSS 规范中所说的容器 block，这是最上层的 block，包含了其他所有 block。它的尺寸就是视口，即浏览器窗口显示区域的尺寸。Firefox 称之为 ViewPortFrame，而 WebKit 称之为 RenderView。这就是文档所指向的呈现对象。呈现树的其余部分以 DOM 树节点插入的形式来构建。</p><p>请参阅关于处理模型的 CSS2 规范。</p><h3 id="样式计算"><a href="#样式计算" class="headerlink" title="样式计算"></a>样式计算</h3><p>构建呈现树时，需要计算每一个呈现对象的可视化属性。这是通过计算每个元素的样式属性来完成的。</p><p>样式包括来自各种来源的样式表、inline 样式元素和 HTML 中的可视化属性（例如“bgcolor”属性）。其中后者将经过转化以匹配 CSS 样式属性。</p><p>样式表的来源包括浏览器的默认样式表、由网页作者提供的样式表以及由浏览器用户提供的用户样式表（浏览器允许您定义自己喜欢的样式。以 Firefox 为例，用户可以将自己喜欢的样式表放在“Firefox Profile”文件夹下）。</p><p>样式计算存在以下难点：</p><ol><li>样式数据是一个超大的结构，存储了无数的样式属性，这可能造成内存问题。</li><li><p>如果不进行优化，为每一个元素查找匹配的规则会造成性能问题。要为每一个元素遍历整个规则列表来寻找匹配规则，这是一项浩大的工程。选择器会具有很复杂的结构，这就会导致某个匹配过程一开始看起来很可能是正确的，但最终发现其实是徒劳的，必须尝试其他匹配路径。</p><p>例如下面这个组合选择器：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">div div div div&#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这意味着规则适用于作为 3 个 div 元素的子代的 <div>。如果您要检查规则是否适用于某个指定的 <div> 元素，应选择树上的一条向上路径进行检查。您可能需要向上遍历节点树，结果发现只有两个 div，而且规则并不适用。然后，您必须尝试树中的其他路径。</div></div></p></li><li><p>应用规则涉及到相当复杂的层叠规则（用于定义这些规则的层次）。</p></li></ol><p>让我们来看看浏览器是如何处理这些问题的：</p><h4 id="共享样式数据"><a href="#共享样式数据" class="headerlink" title="共享样式数据"></a>共享样式数据</h4><p>WebKit 节点会引用样式对象 (RenderStyle)。这些对象在某些情况下可以由不同节点共享。这些节点是同级关系，并且：</p><ol><li>这些元素必须处于相同的鼠标状态（例如，不允许其中一个是“:hover”状态，而另一个不是）</li><li>任何元素都没有 ID</li><li>标记名称应匹配</li><li>类属性应匹配</li><li>映射属性的集合必须是完全相同的</li><li>链接状态必须匹配</li><li>焦点状态必须匹配</li><li>任何元素都不应受属性选择器的影响，这里所说的“影响”是指在选择器中的任何位置有任何使用了属性选择器的选择器匹配</li><li>元素中不能有任何 inline 样式属性</li><li>不能使用任何同级选择器。WebCore 在遇到任何同级选择器时，只会引发一个全局开关，并停用整个文档的样式共享（如果存在）。这包括 + 选择器以及 :first-child 和 :last-child 等选择器。</li></ol><h4 id="Firefox-规则树"><a href="#Firefox-规则树" class="headerlink" title="Firefox 规则树"></a>Firefox 规则树</h4><p>为了简化样式计算，Firefox 还采用了另外两种树：规则树和样式上下文树。WebKit 也有样式对象，但它们不是保存在类似样式上下文树这样的树结构中，只是由 DOM 节点指向此类对象的相关样式。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image035.png" alt=""></p><center><strong>图：Firefox 样式上下文树 (2.2)</strong></center><p>样式上下文包含端值。要计算出这些值，应按照正确顺序应用所有的匹配规则，并将其从逻辑值转化为具体的值。例如，如果逻辑值是屏幕大小的百分比，则需要换算成绝对的单位。规则树的点子真的很巧妙，它使得节点之间可以共享这些值，以避免重复计算，还可以节约空间。</p><p>所有匹配的规则都存储在树中。路径中的底层节点拥有较高的优先级。规则树包含了所有已知规则匹配的路径。规则的存储是延迟进行的。规则树不会在开始的时候就为所有的节点进行计算，而是只有当某个节点样式需要进行计算时，才会向规则树添加计算的路径。</p><p>这个想法相当于将规则树路径视为词典中的单词。如果我们已经计算出如下的规则树：</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/tree.png" alt=""><br>假设我们需要为内容树中的另一个元素匹配规则，并且找到匹配路径是 B - E - I（按照此顺序）。由于我们在树中已经计算出了路径 A - B - E - I - L，因此就已经有了此路径，这就减少了现在所需的工作量。<br>让我们看看规则树如何帮助我们减少工作。</p><h5 id="结构划分"><a href="#结构划分" class="headerlink" title="结构划分"></a>结构划分</h5><p>样式上下文可分割成多个结构。这些结构体包含了特定类别（如 border 或 color）的样式信息。结构中的属性都是继承的或非继承的。继承属性如果未由元素定义，则继承自其父代。非继承属性（也称为“重置”属性）如果未进行定义，则使用默认值。</p><p>规则树通过缓存整个结构（包含计算出的端值）为我们提供帮助。这一想法假定底层节点没有提供结构的定义，则可使用上层节点中的缓存结构。</p><h5 id="使用规则树计算样式上下文"><a href="#使用规则树计算样式上下文" class="headerlink" title="使用规则树计算样式上下文"></a>使用规则树计算样式上下文</h5><p>在计算某个特定元素的样式上下文时，我们首先计算规则树中的对应路径，或者使用现有的路径。然后我们沿此路径应用规则，在新的样式上下文中填充结构。我们从路径中拥有最高优先级的底层节点（通常也是最特殊的选择器）开始，并向上遍历规则树，直到结构填充完毕。如果该规则节点对于此结构没有任何规范，那么我们可以实现更好的优化：寻找路径更上层的节点，找到后指定完整的规范并指向相关节点即可。这是最好的优化方法，因为整个结构都能共享。这可以减少端值的计算量并节约内存。<br>如果我们找到了部分定义，就会向上遍历规则树，直到结构填充完毕。</p><p>如果我们找不到结构的任何定义，那么假如该结构是“继承”类型，我们会在上下文树中指向父代的结构，这样也可以共享结构。如果是 reset 类型的结构，则会使用默认值。</p><p>如果最特殊的节点确实添加了值，那么我们需要另外进行一些计算，以便将这些值转化成实际值。然后我们将结果缓存在树节点中，供子代使用。</p><p>如果某个元素与其同级元素都指向同一个树节点，那么它们就可以共享整个样式上下文。</p><p>让我们来看一个例子，假设我们有如下 HTML 代码：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"err"</span> <span class="attr">id</span>=<span class="string">"div1"</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">p</span>&gt;</span></span><br><span class="line">        this is a <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"big"</span>&gt;</span> big error <span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">        this is also a</span><br><span class="line">        <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"big"</span>&gt;</span> very  big  error<span class="tag">&lt;/<span class="name">span</span>&gt;</span> error</span><br><span class="line">      <span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"err"</span> <span class="attr">id</span>=<span class="string">"div2"</span>&gt;</span>another error<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><p>还有如下规则：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">div</span> &#123;<span class="attribute">margin</span>:<span class="number">5px</span>;<span class="attribute">color</span>:black&#125;</span><br><span class="line"><span class="selector-class">.err</span> &#123;<span class="attribute">color</span>:red&#125;</span><br><span class="line"><span class="selector-class">.big</span> &#123;<span class="attribute">margin-top</span>:<span class="number">3px</span>&#125;</span><br><span class="line"><span class="selector-tag">div</span> <span class="selector-tag">span</span> &#123;<span class="attribute">margin-bottom</span>:<span class="number">4px</span>&#125;</span><br><span class="line"><span class="selector-id">#div1</span> &#123;<span class="attribute">color</span>:blue&#125;</span><br><span class="line"><span class="selector-id">#div2</span> &#123;<span class="attribute">color</span>:green&#125;</span><br></pre></td></tr></table></figure><p>为了简便起见，我们只需要填充两个结构：color 结构和 margin 结构。color 结构只包含一个成员（即“color”），而 margin 结构包含四条边。<br>形成的规则树如下图所示（节点的标记方式为“节点名 : 指向的规则序号”）：</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image027.png" alt=""></p><center><strong>图：规则树</strong></center><p>上下文树如下图所示（节点名 : 指向的规则节点）：<br><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image029.png" alt=""></p><center><strong>图：上下文树</strong></center><p>假设我们解析 HTML 时遇到了第二个 <div> 标记，我们需要为此节点创建样式上下文，并填充其样式结构。<br>经过规则匹配，我们发现该 <div> 的匹配规则是第 1、2 和 6 条。这意味着规则树中已有一条路径可供我们的元素使用，我们只需要再为其添加一个节点以匹配第 6 条规则（规则树中的 F 节点）。<br>我们将创建样式上下文并将其放入上下文树中。新的样式上下文将指向规则树中的 F 节点。</div></div></p><p>现在我们需要填充样式结构。首先要填充的是 margin 结构。由于最后的规则节点 (F) 并没有添加到 margin 结构，我们需要上溯规则树，直至找到在先前节点插入中计算过的缓存结构，然后使用该结构。我们会在指定 margin 规则的最上层节点（即 B 节点）上找到该结构。</p><p>我们已经有了 color 结构的定义，因此不能使用缓存的结构。由于 color 有一个属性，我们无需上溯规则树以填充其他属性。我们将计算端值（将字符串转化为 RGB 等）并在此节点上缓存经过计算的结构。</p><p>第二个 <span> 元素处理起来更加简单。我们将匹配规则，最终发现它和之前的 span 一样指向规则 G。由于我们找到了指向同一节点的同级，就可以共享整个样式上下文了，只需指向之前 span 的上下文即可。</span></p><p>对于包含了继承自父代的规则的结构，缓存是在上下文树中进行的（事实上 color 属性是继承的，但是 Firefox 将其视为 reset 属性，并缓存到规则树上）。<br>例如，如果我们在某个段落中添加 font 规则：<br><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">p &#123;font-family:Verdana;font size:10px;font-weight:bold&#125;</span><br></pre></td></tr></table></figure></p><p>那么，该段落元素作为上下文树中的 div 的子代，就会共享与其父代相同的 font<br>结构（前提是该段落没有指定 font 规则）。</p><p>在 WebKit 中没有规则树，因此会对匹配的声明遍历 4 次。首先应用非重要高优先级的属性（由于作为其他属性的依据而应首先应用的属性，例如 display），接着是高优先级重要规则，然后是普通优先级非重要规则，最后是普通优先级重要规则。这意味着多次出现的属性会根据正确的层叠顺序进行解析。最后出现的最终生效。 </p><p>因此概括来说，共享样式对象（整个对象或者对象中的部分结构）可以解决问题 1 和问题 3。Firefox 规则树还有助于按照正确的顺序应用属性。</p><h4 id="对规则进行处理以简化匹配"><a href="#对规则进行处理以简化匹配" class="headerlink" title="对规则进行处理以简化匹配"></a>对规则进行处理以简化匹配</h4><p>样式规则有一些来源：</p><ul><li>外部样式表或样式元素中的 CSS 规则<br><code>p {color:blue}</code></li><li>inline 样式属性及类似内容<br><code>&lt;p style=&quot;color:blue&quot; /&gt;</code></li><li>HTML 可视化属性（映射到相关的样式规则）<br><code>&lt;p bgcolor=&quot;blue&quot; /&gt;</code></li></ul><p>后两种很容易和元素进行匹配，因为元素拥有样式属性，而且 HTML 属性可以使用元素作为键值进行映射。</p><p>我们之前在第 2 个问题中提到过，CSS 规则匹配可能比较棘手。为了解决这一难题，可以对 CSS 规则进行一些处理，以便访问。</p><p>样式表解析完毕后，系统会根据选择器将 CSS 规则添加到某个哈希表中。这些哈希表的选择器各不相同，包括 ID、类名称、标记名称等，还有一种通用哈希表，适合不属于上述类别的规则。如果选择器是 ID，规则就会添加到 ID 表中；如果选择器是类，规则就会添加到类表中，依此类推。<br>这种处理可以大大简化规则匹配。我们无需查看每一条声明，只要从哈希表中提取元素的相关规则即可。这种优化方法可排除掉 95% 以上规则，因此在匹配过程中根本就不用考虑这些规则了 (4.1)。</p><p>我们以如下的样式规则为例：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">p</span><span class="selector-class">.error</span> &#123;<span class="attribute">color</span>:red&#125;</span><br><span class="line"><span class="selector-id">#messageDiv</span> &#123;<span class="attribute">height</span>:<span class="number">50px</span>&#125;</span><br><span class="line"><span class="selector-tag">div</span> &#123;<span class="attribute">margin</span>:<span class="number">5px</span>&#125;</span><br></pre></td></tr></table></figure><p>第一条规则将插入类表，第二条将插入 ID 表，而第三条将插入标记表。<br>对于下面的 HTML 代码段：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"error"</span>&gt;</span>an error occurred <span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"messageDiv"</span>&gt;</span>this is a message<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><p>我们首先会为 p 元素寻找匹配的规则。类表中有一个“error”键，在下面可以找到“p.error”的规则。div 元素在 ID 表（键为 ID）和标记表中有相关的规则。剩下的工作就是找出哪些根据键提取的规则是真正匹配的了。<br>例如，如果 div 的对应规则如下：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">table</span> <span class="selector-tag">div</span> &#123;<span class="attribute">margin</span>:<span class="number">5px</span>&#125;</span><br></pre></td></tr></table></figure><p> 这条规则仍然会从标记表中提取出来，因为键是最右边的选择器，但这条规则并不匹配我们的 div 元素，因为 div 没有 table 祖先。<br>WebKit 和 Firefox 都进行了这一处理。</p><h4 id="以正确的层叠顺序应用规则"><a href="#以正确的层叠顺序应用规则" class="headerlink" title="以正确的层叠顺序应用规则"></a>以正确的层叠顺序应用规则</h4><p>样式对象具有与每个可视化属性一一对应的属性（均为 CSS 属性但更为通用）。如果某个属性未由任何匹配规则所定义，那么部分属性就可由父代元素样式对象继承。其他属性具有默认值。</p><p>如果定义不止一个，就会出现问题，需要通过层叠顺序来解决。</p><h5 id="样式表层叠顺序"><a href="#样式表层叠顺序" class="headerlink" title="样式表层叠顺序"></a>样式表层叠顺序</h5><p>某个样式属性的声明可能会出现在多个样式表中，也可能在同一个样式表中出现多次。这意味着应用规则的顺序极为重要。这称为“层叠”顺序。根据 CSS2 规范，层叠的顺序为（优先级从低到高）：</p><ol><li>浏览器声明</li><li>用户普通声明</li><li>作者普通声明</li><li>作者重要声明</li><li>用户重要声明</li></ol><p>浏览器声明是重要程度最低的，而用户只有将该声明标记为“重要”才可以替换网页作者的声明。同样顺序的声明会根据特异性进行排序，然后再是其指定顺序。HTML 可视化属性会转换成匹配的 CSS 声明。它们被视为低优先级的网页作者规则。</p><h5 id="特异性"><a href="#特异性" class="headerlink" title="特异性"></a>特异性</h5><p>选择器的特异性由 CSS2 规范定义如下：</p><ul><li>如果声明来自于“style”属性，而不是带有选择器的规则，则记为 1，否则记为 0 (= a)</li><li>记为选择器中 ID 属性的个数 (= b)</li><li>记为选择器中其他属性和伪类的个数 (= c)</li><li>记为选择器中元素名称和伪元素的个数 (= d)</li></ul><p>将四个数字按 a-b-c-d 这样连接起来（位于大数进制的数字系统中），构成特异性。<br>您使用的进制取决于上述类别中的最高计数。<br>例如，如果 a=14，您可以使用十六进制。如果 a=17，那么您需要使用十七进制；当然不太可能出现这种情况，除非是存在如下的选择器：html body div div p …（在选择器中出现了 17 个标记，这样的可能性极低）。</p><p>一些示例：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">*             &#123;&#125;  /* a=0 b=0 c=0 d=0 -&gt; specificity = 0,0,0,0 */</span><br><span class="line">li            &#123;&#125;  /* a=0 b=0 c=0 d=1 -&gt; specificity = 0,0,0,1 */</span><br><span class="line">li:first-line &#123;&#125;  /* a=0 b=0 c=0 d=2 -&gt; specificity = 0,0,0,2 */</span><br><span class="line">ul li         &#123;&#125;  /* a=0 b=0 c=0 d=2 -&gt; specificity = 0,0,0,2 */</span><br><span class="line">ul ol+li      &#123;&#125;  /* a=0 b=0 c=0 d=3 -&gt; specificity = 0,0,0,3 */</span><br><span class="line">h1 + *[rel=up]&#123;&#125;  /* a=0 b=0 c=1 d=1 -&gt; specificity = 0,0,1,1 */</span><br><span class="line">ul ol li.red  &#123;&#125;  /* a=0 b=0 c=1 d=3 -&gt; specificity = 0,0,1,3 */</span><br><span class="line">li.red.level  &#123;&#125;  /* a=0 b=0 c=2 d=1 -&gt; specificity = 0,0,2,1 */</span><br><span class="line">#x34y         &#123;&#125;  /* a=0 b=1 c=0 d=0 -&gt; specificity = 0,1,0,0 */</span><br><span class="line">style=&quot;&quot;          /* a=1 b=0 c=0 d=0 -&gt; specificity = 1,0,0,0 */</span><br></pre></td></tr></table></figure><h5 id="规则排序"><a href="#规则排序" class="headerlink" title="规则排序"></a>规则排序</h5><p>找到匹配的规则之后，应根据级联顺序将其排序。WebKit 对于较小的列表会使用冒泡排序，而对较大的列表则使用归并排序。对于以下规则，WebKit 通过替换“&gt;”运算符来实现排序：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">static bool operator &gt;(CSSRuleData&amp; r1, CSSRuleData&amp; r2)</span><br><span class="line">&#123;</span><br><span class="line">    int spec1 = r1.selector()-&gt;specificity();</span><br><span class="line">    int spec2 = r2.selector()-&gt;specificity();</span><br><span class="line">    return (spec1 == spec2) : r1.position() &gt; r2.position() : spec1 &gt; spec2;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="渐进式处理"><a href="#渐进式处理" class="headerlink" title="渐进式处理"></a>渐进式处理</h3><p>WebKit 使用一个标记来表示是否所有的顶级样式表（包括 @imports）均已加载完毕。如果在附加过程中尚未完全加载样式，则使用占位符，并在文档中进行标注，等样式表加载完毕后再重新计算。</p><h2 id="布局"><a href="#布局" class="headerlink" title="布局"></a>布局</h2><p>呈现器在创建完成并添加到呈现树时，并不包含位置和大小信息。计算这些值的过程称为布局或重排。</p><p>HTML 采用基于流的布局模型，这意味着大多数情况下只要一次遍历就能计算出几何信息。处于流中靠后位置元素通常不会影响靠前位置元素的几何特征，因此布局可以按从左至右、从上至下的顺序遍历文档。但是也有例外情况，比如 HTML 表格的计算就需要不止一次的遍历 (3.5)。</p><p>坐标系是相对于根框架而建立的，使用的是上坐标和左坐标。</p><p>布局是一个递归的过程。它从根呈现器（对应于 HTML 文档的 <html> 元素）开始，然后递归遍历部分或所有的框架层次结构，为每一个需要计算的呈现器计算几何信息。</html></p><p>根呈现器的位置左边是 0,0，其尺寸为视口（也就是浏览器窗口的可见区域）。<br>所有的呈现器都有一个“layout”或者“reflow”方法，每一个呈现器都会调用其需要进行布局的子代的 layout 方法。</p><h3 id="Dirty-位系统"><a href="#Dirty-位系统" class="headerlink" title="Dirty 位系统"></a>Dirty 位系统</h3><p>为避免对所有细小更改都进行整体布局，浏览器采用了一种“dirty 位”系统。如果某个呈现器发生了更改，或者将自身及其子代标注为“dirty”，则需要进行布局。</p><p>有两种标记：“dirty”和“children are dirty”。“children are dirty”表示尽管呈现器自身没有变化，但它至少有一个子代需要布局。</p><h3 id="全局布局和增量布局"><a href="#全局布局和增量布局" class="headerlink" title="全局布局和增量布局"></a>全局布局和增量布局</h3><p>全局布局是指触发了整个呈现树范围的布局，触发原因可能包括：</p><ol><li>影响所有呈现器的全局样式更改，例如字体大小更改。</li><li>屏幕大小调整。</li></ol><p>布局可以采用增量方式，也就是只对 dirty 呈现器进行布局（这样可能存在需要进行额外布局的弊端）。<br>当呈现器为 dirty 时，会异步触发增量布局。例如，当来自网络的额外内容添加到 DOM 树之后，新的呈现器附加到了呈现树中。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/reflow.png" alt=""></p><center><strong>图：增量布局 - 只有 dirty 呈现器及其子代进行布局 (3.6)。</strong></center><h3 id="异步布局和同步布局"><a href="#异步布局和同步布局" class="headerlink" title="异步布局和同步布局"></a>异步布局和同步布局</h3><p>增量布局是异步执行的。Firefox 将增量布局的“reflow 命令”加入队列，而调度程序会触发这些命令的批量执行。WebKit 也有用于执行增量布局的计时器：对呈现树进行遍历，并对 dirty 呈现器进行布局。<br>请求样式信息（例如“offsetHeight”）的脚本可同步触发增量布局。<br>全局布局往往是同步触发的。<br>有时，当初始布局完成之后，如果一些属性（如滚动位置）发生变化，布局就会作为回调而触发。</p><h3 id="优化"><a href="#优化" class="headerlink" title="优化"></a>优化</h3><p>如果布局是由“大小调整”或呈现器的位置（而非大小）改变而触发的，那么可以从缓存中获取呈现器的大小，而无需重新计算。<br>在某些情况下，只有一个子树进行了修改，因此无需从根节点开始布局。这适用于在本地进行更改而不影响周围元素的情况，例如在文本字段中插入文本（否则每次键盘输入都将触发从根节点开始的布局）。</p><h3 id="布局处理"><a href="#布局处理" class="headerlink" title="布局处理"></a>布局处理</h3><p>布局通常具有以下模式：</p><ol><li>父呈现器确定自己的宽度。</li><li>父呈现器依次处理子呈现器，并且：<ol><li>放置子呈现器（设置 x,y 坐标）。</li><li>如果有必要，调用子呈现器的布局（如果子呈现器是 dirty 的，或者这是全局布局，或出于其他某些原因），这会计算子呈现器的高度。</li></ol></li><li>父呈现器根据子呈现器的累加高度以及边距和补白的高度来设置自身高度，此值也可供父呈现器的父呈现器使用。</li><li>将其 dirty 位设置为 false。</li></ol><p>Firefox 使用“state”对象 (nsHTMLReflowState) 作为布局的参数（称为“reflow”），这其中包括了父呈现器的宽度。<br>Firefox 布局的输出为“metrics”对象 (nsHTMLReflowMetrics)，其包含计算得出的呈现器高度。</p><h3 id="宽度计算"><a href="#宽度计算" class="headerlink" title="宽度计算"></a>宽度计算</h3><p>呈现器宽度是根据容器块的宽度、呈现器样式中的“width”属性以及边距和边框计算得出的。<br>例如以下 div 的宽度：</p><p><code>&lt;div style=&quot;width:30%&quot;/&gt;</code> 将由 WebKit 计算如下（BenderBox 类，calcWidth 方法）：</p><ul><li>容器的宽度取容器的 availableWidth 和 0 中的较大值。availableWidth 在本例中相当于 contentWidth，计算公式如下：<br><code>clientWidth() - paddingLeft() - paddingRight()</code><br>clientWidth 和 clientHeight 表示一个对象的内部（除去边框和滚动条）。</li><li>元素的宽度是“width”样式属性。它会根据容器宽度的百分比计算得出一个绝对值。</li><li>然后加上水平方向的边框和补白。</li></ul><p>现在计算得出的是“preferred width”。然后需要计算最小宽度和最大宽度。<br>如果首选宽度大于最大宽度，那么应使用最大宽度。如果首选宽度小于最小宽度（最小的不可破开单位），那么应使用最小宽度。<br>这些值会缓存起来，以用于需要布局而宽度不变的情况。</p><h3 id="换行"><a href="#换行" class="headerlink" title="换行"></a>换行</h3><p>如果呈现器在布局过程中需要换行，会立即停止布局，并告知其父代需要换行。父代会创建额外的呈现器，并对其调用布局。</p><h2 id="绘制"><a href="#绘制" class="headerlink" title="绘制"></a>绘制</h2><p>在绘制阶段，系统会遍历呈现树，并调用呈现器的“paint”方法，将呈现器的内容显示在屏幕上。绘制工作是使用用户界面基础组件完成的。</p><h2 id="全局绘制和增量绘制"><a href="#全局绘制和增量绘制" class="headerlink" title="全局绘制和增量绘制"></a>全局绘制和增量绘制</h2><p>和布局一样，绘制也分为全局（绘制整个呈现树）和增量两种。在增量绘制中，部分呈现器发生了更改，但是不会影响整个树。更改后的呈现器将其在屏幕上对应的矩形区域设为无效，这导致 OS 将其视为一块“dirty 区域”，并生成“paint”事件。OS 会很巧妙地将多个区域合并成一个。在 Chrome 浏览器中，情况要更复杂一些，因为 Chrome 浏览器的呈现器不在主进程上。Chrome 浏览器会在某种程度上模拟 OS 的行为。展示层会侦听这些事件，并将消息委托给呈现根节点。然后遍历呈现树，直到找到相关的呈现器，该呈现器会重新绘制自己（通常也包括其子代）。</p><h3 id="绘制顺序"><a href="#绘制顺序" class="headerlink" title="绘制顺序"></a>绘制顺序</h3><p>CSS2 规范定义了绘制流程的顺序。绘制的顺序其实就是元素进入堆栈样式上下文的顺序。这些堆栈会从后往前绘制，因此这样的顺序会影响绘制。块呈现器的堆栈顺序如下：</p><ol><li>背景颜色</li><li>背景图片</li><li>边框</li><li>子代</li><li>轮廓</li></ol><h3 id="Firefox-显示列表"><a href="#Firefox-显示列表" class="headerlink" title="Firefox 显示列表"></a>Firefox 显示列表</h3><p>Firefox 遍历整个呈现树，为绘制的矩形建立一个显示列表。列表中按照正确的绘制顺序（先是呈现器的背景，然后是边框等等）包含了与矩形相关的呈现器。这样等到重新绘制的时候，只需遍历一次呈现树，而不用多次遍历（绘制所有背景，然后绘制所有图片，再绘制所有边框等等）。<br>Firefox 对此过程进行了优化，也就是不添加隐藏的元素，例如被不透明元素完全遮挡住的元素。</p><h3 id="WebKit-矩形存储"><a href="#WebKit-矩形存储" class="headerlink" title="WebKit 矩形存储"></a>WebKit 矩形存储</h3><p>在重新绘制之前，WebKit 会将原来的矩形另存为一张位图，然后只绘制新旧矩形之间的差异部分。 </p><h2 id="动态变化"><a href="#动态变化" class="headerlink" title="动态变化"></a>动态变化</h2><p>在发生变化时，浏览器会尽可能做出最小的响应。因此，元素的颜色改变后，只会对该元素进行重绘。元素的位置改变后，只会对该元素及其子元素（可能还有同级元素）进行布局和重绘。添加 DOM 节点后，会对该节点进行布局和重绘。一些重大变化（例如增大“html”元素的字体）会导致缓存无效，使得整个呈现树都会进行重新布局和绘制。</p><h2 id="呈现引擎的线程"><a href="#呈现引擎的线程" class="headerlink" title="呈现引擎的线程"></a>呈现引擎的线程</h2><p>呈现引擎采用了单线程。几乎所有操作（除了网络操作）都是在单线程中进行的。在 Firefox 和 Safari 中，该线程就是浏览器的主线程。而在 Chrome 浏览器中，该线程是标签进程的主线程。<br>网络操作可由多个并行线程执行。并行连接数是有限的（通常为 2 至 6 个，以 Firefox 3 为例是 6 个）。</p><h3 id="事件循环"><a href="#事件循环" class="headerlink" title="事件循环"></a>事件循环</h3><p>浏览器的主线程是事件循环。它是一个无限循环，永远处于接受处理状态，并等待事件（如布局和绘制事件）发生，并进行处理。这是 Firefox 中关于主事件循环的代码：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">while (!mExiting)</span><br><span class="line">    NS_ProcessNextEvent(thread);</span><br></pre></td></tr></table></figure></p><h2 id="CSS2-可视化模型"><a href="#CSS2-可视化模型" class="headerlink" title="CSS2 可视化模型"></a>CSS2 可视化模型</h2><h3 id="画布"><a href="#画布" class="headerlink" title="画布"></a>画布</h3><p>根据 CSS2 规范，“画布”这一术语是指“用来呈现格式化结构的空间”，也就是供浏览器绘制内容的区域。画布的空间尺寸大小是无限的，但是浏览器会根据视口的尺寸选择一个初始宽度。</p><p>根据 <a href="http://www.w3.org/TR/CSS2/zindex.html" target="_blank" rel="noopener">www.w3.org/TR/CSS2/zindex.html</a>，画布如果包含在其他画布内，就是透明的；否则会由浏览器指定一种颜色。</p><h3 id="CSS-框模型"><a href="#CSS-框模型" class="headerlink" title="CSS 框模型"></a>CSS 框模型</h3><p>CSS 框模型描述的是针对文档树中的元素而生成，并根据可视化格式模型进行布局的矩形框。<br>每个框都有一个内容区域（例如文本、图片等），还有可选的周围补白、边框和边距区域。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image046.jpg" alt=""></p><center><strong>图：CSS2 框模型</strong></center><p>每一个节点都会生成 0..n 个这样的框。<br>所有元素都有一个“display”属性，决定了它们所对应生成的框类型。示例：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">block  - generates a block box.</span><br><span class="line">inline - generates one or more inline boxes.</span><br><span class="line">none - no box is generated.</span><br></pre></td></tr></table></figure></p><p> 默认值是 inline，但是浏览器样式表设置了其他默认值。例如，“div”元素的 display 属性默认值是 block。<br>您可以在这里找到默认样式表示例：<a href="http://www.w3.org/TR/CSS2/sample.html" target="_blank" rel="noopener">www.w3.org/TR/CSS2/sample.html</a></p><h3 id="定位方案"><a href="#定位方案" class="headerlink" title="定位方案"></a>定位方案</h3><p>有三种定位方案：</p><ol><li>普通：根据对象在文档中的位置进行定位，也就是说对象在呈现树中的位置和它在 DOM 树中的位置相似，并根据其框类型和尺寸进行布局。</li><li>浮动：对象先按照普通流进行布局，然后尽可能地向左或向右移动。</li><li>绝对：对象在呈现树中的位置和它在 DOM 树中的位置不同。</li></ol><p>定位方案是由“position”属性和“float”属性设置的。</p><ul><li>如果值是 static 和 relative，就是普通流</li><li>如果值是 absolute 和 fixed，就是绝对定位</li></ul><p>static 定位无需定义位置，而是使用默认定位。对于其他方案，网页作者需要指定位置：top、bottom、left、right。<br>框的布局方式是由以下因素决定的：</p><ul><li>框类型</li><li>框尺寸</li><li>定位方案</li><li>外部信息，例如图片大小和屏幕大小</li></ul><h3 id="框类型"><a href="#框类型" class="headerlink" title="框类型"></a>框类型</h3><p>block 框：形成一个 block，在浏览器窗口中拥有其自己的矩形区域。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image057.png" alt=""></p><center><strong>图：block 框</strong></center><p>inline 框：没有自己的 block，但是位于容器 block 内。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image059.png" alt=""></p><center><strong>图：inline 框</strong></center><p>block 采用的是一个接一个的垂直格式，而 inline 采用的是水平格式。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image061.png" alt=""></p><center><strong>图：block 和 inline 格式</strong></center><p>inline 框放置在行中或“行框”中。这些行至少和最高的框一样高，还可以更高，当框根据“底线”对齐时，这意味着元素的底部需要根据其他框中非底部的位置对齐。如果容器的宽度不够，inline 元素就会分为多行放置。在段落中经常发生这种情况。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image063.png" alt=""></p><center><strong>图：行</strong></center><h3 id="定位"><a href="#定位" class="headerlink" title="定位"></a>定位</h3><h4 id="相对"><a href="#相对" class="headerlink" title="相对"></a>相对</h4><p>相对定位：先按照普通方式定位，然后根据所需偏移量进行移动。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image065.png" alt=""></p><center><strong>图：相对定位</strong></center><h4 id="浮动"><a href="#浮动" class="headerlink" title="浮动"></a>浮动</h4><p>浮动框会移动到行的左边或右边。有趣的特征在于，其他框会浮动在它的周围。下面这段 HTML 代码：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">img</span> <span class="attr">style</span>=<span class="string">"float:right"</span> <span class="attr">src</span>=<span class="string">"images/image.gif"</span> <span class="attr">width</span>=<span class="string">"100"</span> <span class="attr">height</span>=<span class="string">"100"</span>&gt;</span></span><br><span class="line">  Lorem ipsum dolor sit amet, consectetuer...</span><br><span class="line"><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure><p> 显示效果如下：<br><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image067.png" alt=""></p><center><strong>图：浮动</strong></center><h4 id="绝对定位和固定定位"><a href="#绝对定位和固定定位" class="headerlink" title="绝对定位和固定定位"></a>绝对定位和固定定位</h4><p>这种布局是准确定义的，与普通流无关。元素不参与普通流。尺寸是相对于容器而言的。在固定定位中，容器就是可视区域。</p><p><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image069.png" alt=""></p><center><strong>图：固定定位</strong></center><p>请注意，即使在文档滚动时，固定框也不会移动。</p><h3 id="分层展示"><a href="#分层展示" class="headerlink" title="分层展示"></a>分层展示</h3><p>这是由 z-index CSS 属性指定的。它代表了框的第三个维度，也就是沿“z 轴”方向的位置。</p><p>这些框分散到多个堆栈（称为堆栈上下文）中。在每一个堆栈中，会首先绘制后面的元素，然后在顶部绘制前面的元素，以便更靠近用户。如果出现重叠，新绘制的元素就会覆盖之前的元素。<br>堆栈是按照 z-index 属性进行排序的。具有“z-index”属性的框形成了本地堆栈。视口具有外部堆栈。</p><p>示例：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span><span class="undefined"></span></span><br><span class="line"><span class="undefined">      div &#123;</span></span><br><span class="line"><span class="undefined">        position: absolute;</span></span><br><span class="line"><span class="undefined">        left: 2in;</span></span><br><span class="line"><span class="undefined">        top: 2in;</span></span><br><span class="line"><span class="undefined">      &#125;</span></span><br><span class="line"><span class="undefined"></span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span></span></span><br><span class="line"><span class="tag">         <span class="attr">style</span>=<span class="string">"z-index: 3;background-color:red; width: 1in; height: 1in; "</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span></span></span><br><span class="line"><span class="tag">         <span class="attr">style</span>=<span class="string">"z-index: 1;background-color:green;width: 2in; height: 2in;"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"> <span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure><p>结果如下：<br><img src="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/image071.png" alt=""></p><center><strong>图：固定定位</strong></center><p>虽然红色 div 在标记中的位置比绿色 div 靠前（按理应该在常规流程中优先绘制），但是 z-index 属性的优先级更高，因此它移动到了根框所保持的堆栈中更靠前的位置。</p>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;原文地址： &lt;a href=&quot;https://www.html5rocks.com/en/tutorials/internals/howbrowserswork/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;How Browsers Work: Behind the scenes of modern web browsers&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;center&gt;&lt;strong&gt;序言&lt;/strong&gt;&lt;/center&gt;

&lt;p&gt;这是一篇全面介绍 WebKit 和 Gecko 内部操作的入门文章，是以色列开发人员塔利·加希尔大量研究的成果。在过去的几年中，她查阅了所有公开发布的关于浏览器内部机制的数据（请参见资源），并花了很多时间来研读网络浏览器的源代码。她写道：&lt;/p&gt;
&lt;p&gt;在 IE 占据 90% 市场份额的年代，我们除了把浏览器当成一个“黑箱”，什么也做不了。但是现在，开放源代码的浏览器拥有了过半的市场份额，因此，是时候来揭开神秘的面纱，一探网络浏览器的内幕了。呃，里面只有数以百万行计的 C++ 代码…&lt;/p&gt;
&lt;p&gt;塔利在她的网站上公布了自己的研究成果，但是我们觉得它值得让更多的人来了解，所以我们在此重新整理并公布。&lt;/p&gt;
&lt;p&gt;作为一名网络开发人员，学习浏览器的内部工作原理将有助于您作出更明智的决策，并理解那些最佳开发实践的个中缘由。尽管这是一篇相当长的文档，但是我们建议您花些时间来仔细阅读；读完之后，您肯定会觉得所费不虚。&lt;/p&gt;
&lt;p align=&quot;right&quot;&gt;保罗·爱丽诗 (Paul Irish)，Chrome 浏览器开发人员事务部&lt;/p&gt;

&lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;网络浏览器很可能是使用最广的软件。在这篇入门文章中，我将会介绍它们的幕后工作原理。我们会了解到，从您在地址栏输入 google.com 直到您在浏览器屏幕上看到 Google 首页的整个过程中都发生了些什么。&lt;/p&gt;
    
    </summary>
    
      <category term="essay" scheme="https://developerdoc.com/categories/essay/"/>
    
      <category term="转载" scheme="https://developerdoc.com/categories/essay/%E8%BD%AC%E8%BD%BD/"/>
    
    
      <category term="前端" scheme="https://developerdoc.com/tags/%E5%89%8D%E7%AB%AF/"/>
    
  </entry>
  
  <entry>
    <title>GitHub精选分享第一期</title>
    <link href="https://developerdoc.com/weekly-share/GitHub%E7%B2%BE%E9%80%89%E5%88%86%E4%BA%AB%E7%AC%AC%E4%B8%80%E6%9C%9F/"/>
    <id>https://developerdoc.com/weekly-share/GitHub精选分享第一期/</id>
    <published>2018-11-23T06:41:14.000Z</published>
    <updated>2018-11-27T15:35:23.274Z</updated>
    
    <content type="html"><![CDATA[<p>记录这周值得分享的事，主要是GitHub精选 前端相关~</p><h3 id="向你的Hexo里放上一只萌萌哒二次元看板娘"><a href="#向你的Hexo里放上一只萌萌哒二次元看板娘" class="headerlink" title="向你的Hexo里放上一只萌萌哒二次元看板娘!"></a><a href="https://github.com/EYHN/hexo-helper-live2d" target="_blank" rel="noopener">向你的Hexo里放上一只萌萌哒二次元看板娘!</a></h3><p><a href="https://github.com/EYHN/hexo-helper-live2d" target="_blank" rel="noopener"><img src="https://huaji8.top/img/hexo%E7%9A%84live2d%E6%8F%92%E4%BB%B6/live2d%20z16.jpg" alt=""></a></p><blockquote class="blockquote-center"><p>可以在hexo博客右下角添加live2D角色<br><a href="https://huaji8.top/post/live2d-plugin-2.0/" target="_blank" rel="noopener">看板娘列表</a></p></blockquote><a id="more"></a><h3 id="How-JavaScript-works"><a href="#How-JavaScript-works" class="headerlink" title="How JavaScript works"></a><a href="https://blog.sessionstack.com/tagged/tutorial" target="_blank" rel="noopener">How JavaScript works</a></h3><p><a href="https://blog.sessionstack.com/tagged/tutorial" target="_blank" rel="noopener"><img src="https://cdn-images-1.medium.com/max/800/1*lMBu87MtEsVFqqbfMum-kA.png" alt=""></a></p><p>系列博客包含：语言引擎，运行时和调用栈概述；如何在 V8 引擎中书写最优代码的 5 条小技巧；内存管理及如何处理 4 类常见的内存泄漏；事件循环及异步编程的出现和 5 种更好的 async/await 编程方式；深入理解 WebSockets 和带有 SSE 机制的HTTP/2 以及正确的使用姿势；WebAssembly 对比 JavaScript 及其使用场景；Web Workers 分类及 5 个使用场景；Service Workers，生命周期及其使用场景；网页消息推送通知机制；使用 MutationObserver 监测 DOM 变化；渲染引擎及性能优化小技巧；解析，语法抽象树及最小化解析时间的 5 条小技巧 ……</p><h3 id="构建时预渲染：网页首帧优化实践"><a href="#构建时预渲染：网页首帧优化实践" class="headerlink" title="构建时预渲染：网页首帧优化实践"></a><a href="https://tech.meituan.com/first_contentful_paint_practice.html" target="_blank" rel="noopener">构建时预渲染：网页首帧优化实践</a></h3><p><a href="https://tech.meituan.com/first_contentful_paint_practice.html" target="_blank" rel="noopener"><img src="https://tech.meituan.com/img/shanghanyang/03.png" alt=""></a></p><blockquote class="blockquote-center"><p>美团技术团队在“首屏白屏”问题上的优化实践</p></blockquote><h3 id="style2paints-自动上色"><a href="#style2paints-自动上色" class="headerlink" title="style2paints 自动上色"></a><a href="https://github.com/lllyasviel/style2paints" target="_blank" rel="noopener">style2paints 自动上色</a></h3><p><a href="https://github.com/lllyasviel/style2paints" target="_blank" rel="noopener"><img src="https://raw.githubusercontent.com/lllyasviel/style2paints/master/temps/show/5.jpg" alt=""></a></p><p>AI 漫画稿在线上色工具，通过人工智能，将线稿的风格转换为彩色漫画风格。该项目同时提供了在线体验网站根据不同的涂色风格，获得各种上色图片。特别值得关注的是该项目由苏州大学发布，是中国科研机构在本列表中排名最高的项目。</p><h3 id="深度学习助力前端开发：自动生成GUI图代码"><a href="#深度学习助力前端开发：自动生成GUI图代码" class="headerlink" title="深度学习助力前端开发：自动生成GUI图代码"></a><a href="https://github.com/tonybeltramelli/pix2code" target="_blank" rel="noopener">深度学习助力前端开发：自动生成GUI图代码</a></h3><p><a href="https://github.com/tonybeltramelli/pix2code" target="_blank" rel="noopener"><img src="http://mmbiz.qpic.cn/mmbiz_png/KmXPKA19gW9RgiagIibfo1J7QcMtpOpVmrnM15s2ynV7Vbd2QnRrhibUYWFibSicaJTSNYxVyxHoxQO9tfN7OXWOM2A/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt=""></a></p><p>哥本哈根的一家初创公司 UIzard Technologies 训练了一个神经网络，能够把图形用户界面的截图转译成代码行，成功为开发者们分担了部分网站设计流程。令人惊叹的是，同一个模型能跨平台工作，包括 iOS、Android 和 Web 界面，从目前的研发水平来看，该算法的准确率达到了 77%。 详细可以看这篇文章<a href="https://mp.weixin.qq.com/s?__biz=MzA3MzI4MjgzMw==&amp;mid=2650728064&amp;idx=1&amp;sn=adb744e599299916faa23545c2ab436e&amp;chksm=871b22feb06cabe83ba0e3268a7a8c0c486cf6f42427ab41814e69242f919b25bc7de06ea258&amp;scene=21#wechat_redirect" target="_blank" rel="noopener">机器之心-深度学习助力前端开发：自动生成GUI图代码（附试用地址）</a></p><h3 id="深度学习500问"><a href="#深度学习500问" class="headerlink" title="深度学习500问"></a><a href="https://github.com/scutan90/DeepLearning-500-questions" target="_blank" rel="noopener">深度学习500问</a></h3><p><a href="https://github.com/scutan90/DeepLearning-500-questions" target="_blank" rel="noopener"><img src="/res/weeklyshare/DeepLearning-500-questions.png" alt=""></a></p><p>深度学习500问，以问答形式对常用的概率知识、线性代数、机器学习、深度学习、计算机视觉等热点问题进行阐述，以帮助自己及有需要的读者。 全书分为18个章节，近30万字。由于水平有限，书中不妥之处恳请广大读者批评指正。 未完待续…..</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;记录这周值得分享的事，主要是GitHub精选 前端相关~&lt;/p&gt;
&lt;h3 id=&quot;向你的Hexo里放上一只萌萌哒二次元看板娘&quot;&gt;&lt;a href=&quot;#向你的Hexo里放上一只萌萌哒二次元看板娘&quot; class=&quot;headerlink&quot; title=&quot;向你的Hexo里放上一只萌萌哒二次元看板娘!&quot;&gt;&lt;/a&gt;&lt;a href=&quot;https://github.com/EYHN/hexo-helper-live2d&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;向你的Hexo里放上一只萌萌哒二次元看板娘!&lt;/a&gt;&lt;/h3&gt;&lt;p&gt;&lt;a href=&quot;https://github.com/EYHN/hexo-helper-live2d&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;&lt;img src=&quot;https://huaji8.top/img/hexo%E7%9A%84live2d%E6%8F%92%E4%BB%B6/live2d%20z16.jpg&quot; alt=&quot;&quot;&gt;&lt;/a&gt;&lt;/p&gt;
&lt;blockquote class=&quot;blockquote-center&quot;&gt;&lt;p&gt;可以在hexo博客右下角添加live2D角色&lt;br&gt;&lt;a href=&quot;https://huaji8.top/post/live2d-plugin-2.0/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;看板娘列表&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="每周分享" scheme="https://developerdoc.com/categories/%E6%AF%8F%E5%91%A8%E5%88%86%E4%BA%AB/"/>
    
    
  </entry>
  
</feed>
